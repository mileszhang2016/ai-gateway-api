# 存储层设计（storage/rdb）

## 1. 概述

`ai-gateway-api` 的存储层位于 `storage/rdb`，采用**三层架构**中的最底层：

- **接口层**（`endpoints/openapi_v1`）：处理 HTTP 请求、参数校验，调用模型层。
- **模型层**（`model/*`）：处理业务逻辑、事务管理，调用存储层接口。
- **存储层**（`storage/rdb`）：负责关系型数据库的读写，采用 **DAO + Storage** 两层结构。

存储层向上暴露一组接口（定义在 `model/*` 各子包中），由模型层的 `Manager` 通过事务抽象 `model/itxn.TxnStorager` 调用；向下通过 `storage/rdb/internal/dao` 中的 DAO 函数操作具体的数据库表。

## 2. DAO 层设计

### 2.1 DAO 文件清单

所有 DAO 文件均位于 `storage/rdb/internal/dao/`，按数据库表一一对应：

```
storage/rdb/internal/dao/
├── table_ai_route_rules.go # ai_route_rules 表（AI 路由规则）
├── table_api_key_tokens.go # api_key_tokens 表（API-Key Token）
├── table_api_keys.go # api_keys 表（API-Key）
├── table_bfe_clusters.go # bfe_clusters 表（BFE 集群）
├── table_certificates.go # certificates 表（TLS 证书）
├── table_clusters.go # clusters 表（后端集群）
├── table_config_versions.go # config_versions 表（配置版本）
├── table_domains.go # domains 表（域名）
├── table_entities.go # entities 表（Entity）
├── table_entity_types.go # entity_types 表（Entity 类型）
├── table_extra_files.go # extra_files 表（附加文件）
├── table_lb_matrix.go # lb_matrices 表（负载均衡矩阵）
├── table_model_prices.go # model_prices 表（模型定价）
├── table_providers.go # providers 表（Provider）
├── table_pools.go # pools 表（实例池）
├── table_products.go # products 表（产品线）
├── table_quota_balances.go # quota_balances 表（配额余额）
├── table_quota_plans.go # quota_plans 表（配额计划）
├── table_rate_limit_policies.go # rate_limit_policies 表（限流策略）
├── table_route_advance_rules.go # route_advance_rules 表（高级路由规则）
├── table_route_basic_rules.go # route_basic_rules 表（基础路由规则）
├── table_route_default_rules.go # route_default_rules 表（默认路由规则）
├── table_route_rules.go # route_rules 表（AI 路由规则：api_key/entity/global）
├── table_sub_clusters.go # sub_clusters 表（子集群）
├── table_user_products.go # user_products 表（用户-产品线授权）
└── table_users.go # users 表（用户/Session Key）
```

### 2.2 DAO 通用设计模式

每个 DAO 文件遵循统一的代码模板：

1. **表名常量**：`const t<Table>TableName = "<table_name>"`
2. **结果结构体**：`T<Table>`，字段使用 `db:"column_name"` 标签，与表字段一一对应。
3. **参数结构体**：`T<Table>Param`，字段均为指针类型，同样使用 `db:"column_name"` 标签；同时包含两个控制字段：
   - `OrderBy *string `db:"_orderby"`` — 排序
   - `Limit []uint `db:"_limit"`` — 分页（部分表支持）
4. **CRUD 函数**：
   - `T<Table>One(dbCtx, where)` — 查询单条，不存在时返回 `(nil, nil)`
   - `T<Table>List(dbCtx, where)` — 查询多条，不存在时返回 `(nil, nil)`
   - `T<Table>Create(dbCtx, data...)` — 单条/批量插入，自动填充 `CreatedAt`
   - `T<Table>Update(dbCtx, val, where)` — 按条件更新
   - `T<Table>Delete(dbCtx, where)` — 按条件删除

底层统一使用 `storage/rdb/internal/dao/internal` 包中的 `QueryOne`、`QueryList`、`Create`、`Update`、`Delete`，基于 `github.com/didi/gendry` 构建 SQL 并扫描结果。

### 2.3 DAO 结构示例

#### 2.3.1 entity_types 表

```go
const tEntityTypeTableName = "entity_types"

type TEntityType struct {
    ID int64 `db:"id"`
    TypeName string `db:"type_name"`
    Description string `db:"description"`
    Level int `db:"level"`
    CreatedAt time.Time `db:"created_at"`
    UpdatedAt time.Time `db:"updated_at"`
}

type TEntityTypeParam struct {
    ID *int64 `db:"id"`
    TypeName *string `db:"type_name"`
    Description *string `db:"description"`
    Level *int `db:"level"`
    CreatedAt *time.Time `db:"created_at"`
    UpdatedAt *time.Time `db:"updated_at"`

    OrderBy *string `db:"_orderby"`
    Limit []uint `db:"_limit"`
}

func TEntityTypeOne(dbCtx lib.DBContexter, where *TEntityTypeParam) (*TEntityType, error)
func TEntityTypeList(dbCtx lib.DBContexter, where *TEntityTypeParam) ([]*TEntityType, error)
func TEntityTypeCreate(dbCtx lib.DBContexter, data ...*TEntityTypeParam) (int64, error)
func TEntityTypeUpdate(dbCtx lib.DBContexter, val, where *TEntityTypeParam) (int64, error)
func TEntityTypeDelete(dbCtx lib.DBContexter, where *TEntityTypeParam) (int64, error)
```

#### 2.3.2 api_keys 表

```go
const tAPIKeyTableName = "api_keys"

type TAPIKey struct {
    InnerID int64 `db:"inner_id"`
    ID string `db:"id"`
    Enable bool `db:"enable"`
    Key string `db:"api_key"`
    Description string `db:"description"`
    UnlimitedQuota bool `db:"unlimited_quota"`
    ProductName string `db:"product_name"`
    ExpiredTime int64 `db:"expired_time"`
    AllowedModels string `db:"allowed_models"`
    Subnet string `db:"subnet"`
    EntityID *string `db:"entity_id"`
    QuotaPlanID *int64 `db:"quota_plan_id"`
    RateLimitPolicyID *int64 `db:"rate_limit_policy_id"`
    RouteRulesID *int64 `db:"route_rules_id"`
    CreatedAt time.Time `db:"created_at"`
    UpdatedAt time.Time `db:"updated_at"`
}

type TAPIKeyParam struct {
    InnerID *int64 `db:"inner_id"`
    ID *string `db:"id"`
    Enable *bool `db:"enable"`
    Key *string `db:"api_key"`
    Description *string `db:"description"`
    UnlimitedQuota *bool `db:"unlimited_quota"`
    ProductName *string `db:"product_name"`
    ExpiredTime *int64 `db:"expired_time"`
    AllowedModels *string `db:"allowed_models"`
    Subnet *string `db:"subnet"`
    EntityID *string `db:"entity_id"`
    QuotaPlanID *int64 `db:"quota_plan_id"`
    RateLimitPolicyID *int64 `db:"rate_limit_policy_id"`
    RouteRulesID *int64 `db:"route_rules_id"`
    CreatedAt *time.Time `db:"created_at"`
    UpdatedAt *time.Time `db:"updated_at"`

    OrderBy *string `db:"_orderby"`
    Limit []uint `db:"_limit"`
}
```

### 2.4 特殊 DAO 实现

部分 DAO 在通用 CRUD 之外提供了额外能力：

| 文件 | 特殊点 | 说明 |
|------|--------|------|
| `table_products.go` | `TProductDeleteByProductID` | 使用原生 SQL 一次性删除 `products`、`domains`、`route_basic_rules`、`route_advance_rules`、`route_default_rules` 等与产品线关联的多张表数据 |
| `table_route_advance_rules.go` | `ModeForUpdate = "exclusive"`、`_lockMode` | 支持 `SELECT ... FOR UPDATE` 行锁 |
| `table_pools.go` | `PoolsList2Map` | 将 `pools` 列表转换为以 `Id` 为 key 的 map |
| `table_domains.go` | `DomainTypeGeneral` | 定义通用域名类型常量 |
| `table_clusters.go` | `IDs []int64`、`Names []string` | 支持 `IN` 查询 |
| `table_sub_clusters.go` | `IDs`、`Names`、`ClusterIDs`、`PoolsIDs` | 支持多字段 `IN` 查询 |
| `table_extra_files.go` | `Names []string` | 支持按名称批量 `IN` 查询 |
| `table_users.go` | `IDs []int64`、`Types []int8` | 支持 `IN` 查询 |
| `table_user_products.go` | `UserIDs []int64` | 支持 `IN` 查询 |

## 3. Storage 层设计

### 3.1 Storage 文件清单

Storage 实现按业务域组织，位于 `storage/rdb/` 下：

```
storage/rdb/
├── ai_route/
│ └── ai_route.go # AI 路由规则存储
├── api_key/
│ └── api_key.go # API-Key 存储
├── auth/
│ ├── authentication.go # 用户/Token 认证存储
│ └── authorization.go # 用户/Token-产品线授权存储
├── basic/
│ ├── bfe_cluster.go # BFE 集群存储
│ ├── extra_file.go # 附加文件存储
│ └── product.go # 产品线存储
├── cluster_conf/
│ ├── cluster.go # 集群/子集群/LB 矩阵存储
│ ├── model_price.go # ModelPrice 存储读取（供 AIConf.ModelTable 使用）
│ ├── pool.go # 实例池存储；数据来自 Provider，按 cluster 生成 BFE 配置
│ └── sub_cluster.go # 子集群存储
├── provider/
│ └── provider.go # Provider 存储
├── entity/
│ ├── entity.go # Entity 存储
│ └── entity_type.go # EntityType 存储
├── protocol/
│ └── certificate.go # TLS 证书存储
├── quota/
│ ├── quota_balance.go # QuotaBalance 存储
│ └── quota_plan.go # QuotaPlan 存储
├── rate_limit_policy/
│ └── rate_limit_policy.go # RateLimitPolicy 存储
├── route_conf/
│ ├── domain.go # 域名存储
│ └── route_rule.go # 产品级路由规则存储
├── route_rules/
│ └── route_rules.go # RouteRules 存储（实现 model/shared 接口）
├── txn/
│ └── txn.go # 事务抽象实现
└── version_control/
    └── version_control.go # 配置版本控制存储
```

### 3.2 Storage 通用设计模式

每个 Storage 实现遵循以下模式：

1. **依赖注入**：通过构造函数接收 `lib.DBContextFactory`，用于从 `context.Context` 获取 `lib.DBContexter`。
2. **接口断言**：使用 `var _ <Interface> = &<Storage>{}` 确保实现对应 model 层接口。
3. **类型转换**：在 model 参数/过滤条件与 DAO 参数/结果之间做转换。
4. **JSON 序列化**：复杂结构通过 `encoding/json` 序列化为字符串后存入数据库。
5. **时间戳处理**：使用 `lib.PTimeNow()` 等工具函数填充 `CreatedAt` / `UpdatedAt`。

示例：

```go
type EntityTypeStorager struct {
    dbCtxFactory lib.DBContextFactory
}

func NewEntityTypeStorager(dbCtxFactory lib.DBContextFactory) *EntityTypeStorager {
    return &EntityTypeStorager{dbCtxFactory: dbCtxFactory}
}

var _ entity.EntityTypeStorager = &EntityTypeStorager{}

func (s *EntityTypeStorager) CreateEntityType(
    ctx context.Context,
    param *entity.EntityTypeParam,
) (int64, error) {
    dbCtx, err := s.dbCtxFactory(ctx)
    if err != nil {
        return 0, err
    }

    data := &dao.TEntityTypeParam{
        TypeName: param.TypeName,
        Description: param.Description,
        Level: param.Level,
        CreatedAt: lib.PTimeNow(),
    }

    return dao.TEntityTypeCreate(dbCtx, data)
}
```

### 3.3 各域 Storage 实现

#### 3.3.1 entity / rate_limit_policy / route_rules / quota 域

实现文件按业务模型拆分到多个子包，对应接口定义如下：

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `entity/entity_type.go` | `EntityTypeStorager` | `NewEntityTypeStorager` | `entity.EntityTypeStorager` | `CreateEntityType`、`FetchEntityType`、`FetchEntityTypeList`、`UpdateEntityType`、`DeleteEntityType` |
| `entity/entity.go` | `EntityStorager` | `NewEntityStorager` | `entity.EntityStorager` | `CreateEntity`、`FetchEntity`、`FetchEntityList`、`FetchEntityByRouteRulesID`、`UpdateEntity`、`DeleteEntity` |
| `rate_limit_policy/rate_limit_policy.go` | `RateLimitPolicyStorager` | `NewRateLimitPolicyStorager` | `rate_limit_policy.RateLimitPolicyStorager` | `CreateRateLimitPolicy`、`FetchRateLimitPolicy`、`FetchRateLimitPolicyList`、`UpdateRateLimitPolicy`、`DeleteRateLimitPolicy` |
| `route_rules/route_rules.go` | `RouteRulesStorager` | `NewRouteRulesStorager` | `shared.RouteRulesStorager` | `CreateRouteRules`、`FetchRouteRules`、`FetchRouteRulesByID`、`FetchRouteRulesList`、`UpdateRouteRules`、`DeleteRouteRules` |
| `quota/quota_plan.go` | `QuotaPlanStorager` | `NewQuotaPlanStorager` | `quota.QuotaPlanStorager` | `CreateQuotaPlan`、`FetchQuotaPlan`、`FetchQuotaPlanList`、`UpdateQuotaPlan`、`DeleteQuotaPlan`；`quota` 字段按 `DECIMAL(18,8)` 读写 |
| `quota/quota_balance.go` | `QuotaBalanceStorager` | `NewQuotaBalanceStorager` | `quota.QuotaBalanceStorager` | `CreateQuotaBalance`、`FetchQuotaBalance`、`FetchQuotaBalanceList`、`UpdateQuotaBalance`、`DeleteQuotaBalance`；`used` / `remaining` 按 `DECIMAL(18,8)` 读写 |

##### 接口示例

```go
// model/entity/entity_type.go
type EntityTypeStorager interface {
    CreateEntityType(ctx context.Context, param *EntityTypeParam) (int64, error)
    FetchEntityType(ctx context.Context, filter *EntityTypeFilter) (*EntityTypeParam, error)
    FetchEntityTypeList(ctx context.Context, filter *EntityTypeFilter) ([]*EntityTypeParam, error)
    UpdateEntityType(ctx context.Context, filter *EntityTypeFilter, param *EntityTypeParam) (int64, error)
    DeleteEntityType(ctx context.Context, filter *EntityTypeFilter) error
}

// model/shared/types.go
type RouteRulesStorager interface {
    CreateRouteRules(ctx context.Context, ruleType string, owner *string, param *RouteRulesParam) (int64, error)
    FetchRouteRules(ctx context.Context, ruleType string, owner *string) (*RouteRulesParam, error)
    FetchRouteRulesList(ctx context.Context, filter *RouteRulesFilter) ([]*RouteTableParam, int64, error)
    UpdateRouteRules(ctx context.Context, id int64, param *RouteRulesParam) (int64, error)
    DeleteRouteRules(ctx context.Context, id int64) error
    FetchRouteRulesByID(ctx context.Context, id int64) (*RouteRulesParam, error)
}
```

##### JSON 序列化示例（RateLimitPolicy）

```go
func rateLimitPolicyDataToParam(param *rate_limit_policy.RateLimitPolicyParam) *dao.TRateLimitPolicyParam {
    data := &dao.TRateLimitPolicyParam{
        Enabled: param.Enabled,
        MaxConcurrency: param.MaxConcurrency,
    }

    if len(param.TpmConfigs) > 0 {
        tpmConfigsJSON, _ := json.Marshal(param.TpmConfigs)
        data.TpmConfigs = lib.PString(string(tpmConfigsJSON))
    } else {
        data.TpmConfigs = lib.PString("[]")
    }

    if len(param.RpmConfigs) > 0 {
        rpmConfigsJSON, _ := json.Marshal(param.RpmConfigs)
        data.RpmConfigs = lib.PString(string(rpmConfigsJSON))
    } else {
        data.RpmConfigs = lib.PString("[]")
    }

    return data
}
```

#### 3.3.2 api_key 域

实现文件位于 `storage/rdb/api_key/`，对应接口定义在 `model/api_key`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `api_key/api_key.go` | `APIKeyStorager` | `NewAPIKeyStorager` | `api_key.APIKeyStorager` | `CreateAPIKey`、`FetchAPIKeyList`、`UpdateAPIKey`、`DeleteAPIKey`、`CreateAPIKeyToken`、`UpdateAPIKeyToken`、`FetchAPIKeyTokenList` |

`APIKeyStorager` 在 `api_keys` 表基础上，对 `allowed_models` 和 `subnet` 两个 JSON 字段做序列化/反序列化：

- 写入时，若入参为空则默认存储 `["*"]`。
- 读取时，若数据库字段为空则反序列化为 `["*"]`。

#### 3.3.3 cluster_conf 域

实现文件位于 `storage/rdb/cluster_conf/`，对应接口定义在 `model/icluster_conf`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `cluster.go` | `RDBClusterStorager` | `NewRDBClusterStorager` | `icluster_conf.ClusterStorager` | `ClusterCreate`、`ClusterUpdate`、`ClusterDelete`、`FetchCluster`、`FetchClusterList`、`FetchLBMatrixList`、`BindSubCluster`；创建/更新时按 Provider `instance_pool` 同步实例池 |
| `model_price.go` | `ModelPriceStorager` | `NewModelPriceStorager` | `imodel_price.ModelPriceStorager` | `ListByProvider` 等，供 `AIConf.ModelTable` 填充 |
| `pool.go` | `RDBPoolStorager` | `NewRDBPoolStorager` | `icluster_conf.PoolStorager` | `CreatePool`、`FetchPool`、`FetchPools`、`UpdatePool`、`DeletePool`；实例详情来自 Provider |
| `sub_cluster.go` | `RDBSubClusterStorager` | `NewRDBSubClusterStorager` | `icluster_conf.SubClusterStorager` | `CreateSubCluster`、`FetchSubClusterList`、`UpdateSubCluster`、`DeleteSubCluster` |
| `provider.go` | `RDBProviderStorager` | `NewRDBProviderStorager` | `iprovider.ProviderStorager` | `CreateProvider`、`FetchProvider`、`FetchProviderList`、`UpdateProvider`、`DeleteProvider` |

#### 3.3.4 route_conf 域

实现文件位于 `storage/rdb/route_conf/`，对应接口定义在 `model/iroute_conf`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `domain.go` | `DomainStorager` | `NewDomainStorager` | `iroute_conf.DomainStorager` | `FetchDomains`、`CreateDomain`、`DeleteDomain` |
| `route_rule.go` | `RouteRuleStorager` | `NewRouteRuleStorager` | `iroute_conf.RouteRuleStorager` | `FetchProductRule`、`FetchRoutRules`、`UpsertProductRule` |

#### 3.3.5 auth 域

实现文件位于 `storage/rdb/auth/`，对应接口定义在 `model/iauth`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `authentication.go` | `RDBAuthenticateStorager` | `NewAuthenticateStorager` | `iauth.AuthenticateStorager` | `FetchUserList`、`FetchUser`、`UpdateUser`、`CreateUser`、`DeleteUser`、`FetchTokens`、`CreateToken`、`DeleteToken` |
| `authorization.go` | `RDBAuthorizeStorager` | `NewAuthorizeStorager` | `iauth.AuthorizeStorager` | 用户-产品线/Token-产品线的绑定、解绑、查询、授权校验 |

#### 3.3.6 basic 域

实现文件位于 `storage/rdb/basic/`，对应接口定义在 `model/ibasic`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `product.go` | `RDBProductStorager` | `NewProductManager` | `ibasic.ProductStorager` | `FetchProducts`、`CreateProduct`、`UpdateProduct`、`DeleteProduct` |
| `bfe_cluster.go` | `RDBBFEClusterStorager` | `NewRDBBFEClusterStorager` | `ibasic.BFEClusterStorager` | `FetchBFEClusters`、`CreateBFECluster`、`DeleteBFECluster` |
| `extra_file.go` | `RDBExtraFileStorager` | `NewRDBExtraFileStorager` | `ibasic.ExtraFileStorager` | `FetchExtraFiles`、`CreateExtraFile`、`DeleteExtraFile` |

#### 3.3.7 protocol 域

实现文件位于 `storage/rdb/protocol/`，对应接口定义在 `model/iprotocol`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `certificate.go` | `RDBCertificateStorager` | `NewCertificateStorager` | `iprotocol.CertificateStorager` | `FetchCertificates`、`CreateCertificate`、`UpdateCertificate`、`DeleteCertificate` |

#### 3.3.8 ai_route 域

实现文件位于 `storage/rdb/ai_route/`，对应接口定义在 `model/iai_route`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `ai_route.go` | `RDBAIRouteRuleStorager` | `NewRDBAIRouteRuleStorager` | `iai_route.AIRouteRuleStorager` | `FetchAIRouteRules`、`CreateAIRouteRules`、`ConvertToRule`、`ConvertToTAIRouteRule` |

#### model_price 域

实现文件可位于 `storage/rdb/cluster_conf/model_price.go`（供 `model/icluster_conf` 使用）或独立的 `storage/rdb/model_price/`（若 `model/imodel_price` 独立成包）。对应接口定义在 `model/imodel_price`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `model_price.go` | `ModelPriceStorager` | `NewModelPriceStorager` | `imodel_price.ModelPriceStorager` | `CreateModelPrice`、`FetchModelPrice`、`FetchModelPriceList`、`UpdateModelPrice`、`DeleteModelPrice`、`ListByProvider`、`ListProviders` |

`model_prices` 表的 `capabilities`、`supported_parameters`、`limits`、`prices`、`metadata` 字段以 JSON 字符串存储，Storage 层负责序列化与反序列化；`provider` 字段仅作为价格归集标识，不强制引用 `providers.name`，供 InnerAPI 导出 `AIConf.ModelTable` 与管理面查询 price provider 列表。`provider` 不再进行存在性校验。

#### 3.3.9 version_control 域

实现文件位于 `storage/rdb/version_control/`，对应接口定义在 `model/iversion_control`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `version_control.go` | `VersionControlStorager` | `NewVersionControllerStorage` | `iversion_control.VersionControlStorager` | `UpsertConfigLastExportedVersion` |

#### 3.3.10 txn 域

实现文件位于 `storage/rdb/txn/`，对应接口定义在 `model/itxn`。

| 文件 | Storage 结构 | 构造函数 | 实现接口 | 主要方法 |
|------|--------------|----------|----------|----------|
| `txn.go` | `RDBTxnStorager` | `NewRDBTxnStorager` | `itxn.TxnStorager` | `AtomExecute` |

```go
// model/itxn/txn.go
type TxnStorager interface {
    AtomExecute(ctx context.Context, f func(context.Context) error) error
}
```

### 3.4 特殊 Storage 实现

#### 3.4.1 RouteRules 列表分页

`storage/rdb/route_rules/route_rules.go` 中的 `FetchRouteRulesList` 未使用 SQL `LIMIT`，而是在应用层做内存分页：

1. 先查询全部满足条件的记录（无分页）。
2. 计算总数 `total`。
3. 根据 `Page` / `PageSize` 计算偏移量，再次查询时加上 `Limit`。
4. 返回结果只包含 `type`、`owner`、`enabled`，不返回 `rules` 字段。

默认分页：`page = 1`，`pageSize = 20`，最大 `pageSize = 100`。

#### 3.4.2 产品级路由规则全量替换

`storage/rdb/route_conf/route_rule.go` 中的 `UpsertProductRule`：

1. 对 `route_advance_rules` 使用 `SELECT ... FOR UPDATE` 加锁。
2. 删除该产品线下的所有 `route_advance_rules` 和 `route_basic_rules`。
3. 批量插入新的规则。

#### 3.4.3 集群与子集群协同

`storage/rdb/cluster_conf/cluster.go`：

- 创建/更新/删除集群时，同时处理 `clusters`、`sub_clusters`、`lb_matrices` 三张表。
- `LLMConfig` 以 JSON 字符串存储在 `clusters` 表；多 API-Key 改造后，`llm_config` 中新增 `keys` 数组与 `key_policy` 对象，旧单 `key` 字段不再使用，但数据库表结构保持不变。
- `lb_matrices` 的 `lb_matrix` 字段以 JSON 字符串存储。

#### 3.4.4 AI 路由规则全量替换

`storage/rdb/ai_route/ai_route.go` 中的 `CreateAIRouteRules`：

1. 先删除 `ai_route_rules` 全表数据。
2. 再批量插入新的规则，并自动生成 `IDX`。

#### 3.4.5 配置版本控制

`storage/rdb/version_control/version_control.go` 中的 `UpsertConfigLastExportedVersion`：

- 比较 `config_versions` 中最新记录的 `data_sign`。
- 若签名不同则新增一条记录，`version` 递增；若相同则不插入。

### 3.5 存储层变更说明

#### 3.5.1 Cluster 对外模型与存储模型解耦

管理面 Cluster 接口对外隐藏 `ready`、`sub_clusters`、`scheduler`、`instance_pool` 等内部概念，但数据面导出仍依赖这些底层数据。Provider 与 Cluster 分离后，`instance_pool` 下沉到 `providers` 表，Cluster 创建/更新时由模型层读取 Provider 的 `instance_pool` 并同步生成 `pools`/`sub_clusters`。存储层表结构变化：

- 新增 `providers` 表，存储 Provider 接入能力（`model_endpoint`、`models`、`keys`、`instance_pool`、`model_protocols`）。
- `clusters` 表结构不变，`llm_config` JSON 中移除 `model_endpoint`、`provider_type`，`keys` 改为 `{name, weight}` 引用，`provider` 改为必填且须引用 `providers.name`。
- `pools` 表继续保存实例池数据，但数据源头由 Cluster 顶层下沉为 Provider `instance_pool`；映射与过滤由接口层/模型层完成。
- 对外模型：`basic`、`sticky_sessions`、`passive_health_check`、`llm_config`。
- 存储模型：`clusters` 表字段、`sub_clusters` 表、`pools` 表、`lb_matrices` 表、`providers` 表。

#### 3.5.2 API-Key 全局唯一性索引建议

支持创建 API-Key 时传入外部 `key`，要求全局唯一。当前 `api_key_tokens` 表已有唯一索引 `idx_key`，建议为 `api_keys.api_key` 字段也补充唯一索引：

```sql
ALTER TABLE api_keys ADD UNIQUE KEY uk_api_key (api_key);
```

#### 3.5.3 数据库表结构变更说明

Provider 与 Cluster 概念分离及模型定价改造引入了以下 schema 变更：

- 新增 `providers` 表，存储 Provider 接入能力；
- 新增 `model_prices` 表；
- `quota_plans.quota`、`quota_balances.used`、`quota_balances.remaining` 由 `BIGINT` 调整为 `DECIMAL(18,8)`；
- `clusters` 表结构不变，`llm_config` JSON 中 `provider` 改为必填引用，`model_endpoint` 与 `provider_type` 移除，`keys` 改为 `{name, weight}` 引用结构，由接口层映射与参数校验实现；
- `pools` 表结构不变，实例池数据源头由 Cluster 顶层下沉为 Provider `instance_pool`。

例如 Cluster 多 API-Key 支持：将 `llm_config` 中的单 `key` 字段扩展为 `keys` 数组与 `key_policy` 对象；Provider 分离后，`keys` 元素进一步改为 `{name, weight}` 引用结构，但 `clusters` 表结构（含 `llm_config` text 字段）完全不变，仅修改接口层映射、模型层结构与校验规则。

## 4. 数据映射与序列化约定

### 4.1 JSON 字段

以下字段在数据库中以 JSON 字符串形式存储，Storage 层负责序列化与反序列化：

| 表 | 字段 | 说明 |
|----|------|------|
| `api_keys` | `allowed_models` | 允许的模型列表（默认 `["*"]`） |
| `api_keys` | `subnet` | 允许的客户端子网列表（默认 `["*"]`） |
| `entities` | `allow_models` | 允许的模型白名单 |
| `entities` | `block_models` | 禁止的模型黑名单 |
| `rate_limit_policies` | `tpm_configs` | TPM 限流配置数组 |
| `rate_limit_policies` | `rpm_configs` | RPM 限流配置数组 |
| `route_rules` | `rules` | AI 路由规则数组 |
| `route_basic_rules` | `host_names` | 主机名列表 |
| `route_basic_rules` | `paths` | 路径列表 |
| `providers` | `model_endpoint` | 模型发现端点 |
| `providers` | `models` | 支持的模型列表 |
| `providers` | `keys` | API Key 明文列表 |
| `providers` | `instance_pool` | 后端实例池 |
| `providers` | `model_protocols` | 支持的模型访问协议 |
| `clusters` | `llm_config` | LLM 转发策略（含 Key 引用 `keys`、`key_policy`、`provider`） |
| `lb_matrices` | `lb_matrix` | 负载均衡矩阵 |
| `model_prices` | `capabilities` | 能力列表 |
| `model_prices` | `supported_parameters` | 支持的请求参数列表 |
| `model_prices` | `limits` | 限制对象 |
| `model_prices` | `prices` | 价格对象 |
| `model_prices` | `metadata` | 元数据 |
| `pools` | `instance_detail` | 实例详情 |
| `pools` | `epp_server` | EPP 服务配置 |
| `ai_route_rules` | `basic` | AI 路由规则基础信息 |

### 4.2 时间字段

- `created_at`：记录创建时间，DAO 层在创建时自动填充 `internal.PTimeNow()`。
- `updated_at`：记录更新时间，由数据库 `ON UPDATE CURRENT_TIMESTAMP` 或 Storage 层显式填充。

### 4.3 分页字段

分页通过 `T<Table>Param.Limit` 实现，格式为 `[]uint{offset, pageSize}`。部分 DAO 支持，具体由 Storage 层根据 model 层的 `Page` / `PageSize` 计算偏移量后传入。

## 5. 事务与上下文

### 5.1 事务抽象

模型层不直接依赖数据库事务，而是通过 `model/itxn.TxnStorager`：

```go
type TxnStorager interface {
    AtomExecute(ctx context.Context, f func(context.Context) error) error
}
```

`storage/rdb/txn/txn.go` 提供基于 `lib.DBContextFactory` 的实现：`RDBTxnStorager.AtomExecute` 会在同一个事务上下文中执行传入的函数。

### 5.2 DBContextFactory

所有 Storage 实现通过 `lib.DBContextFactory` 获取 `lib.DBContexter`：

```go
type DBContextFactory func(ctx context.Context) (DBContexter, error)
```

当调用链已处于事务中时，返回的 `DBContexter` 会复用同一个事务；否则使用普通连接。

### 5.3 跨表操作的事务边界

跨表操作通常由**模型层 Manager** 在 `AtomExecute` 回调内编排，例如：

- `EntityManager.CreateEntity`：在一个事务中依次创建 QuotaPlan、RateLimitPolicy、RouteRules、Entity。
- `APIKeyManager.DeleteAPIKey`：在一个事务中依次删除 QuotaBalance、QuotaPlan、RateLimitPolicy、RouteRules、API-Key。
- `RouteRuleStorager.UpsertProductRule`：在一个事务中删除并重建产品的路由规则。

Storage 层本身一般不开启事务，仅负责单表或同域内几张表的读写转换。
