# 接口层设计文档

## 1. 概述

本文档描述 `ai-gateway-api` 接口层（`endpoints` 包）的当前实现结构，包括管理面 OpenAPI（`/open-api/v1`）和数据面 Inner-API（`/inner-api/v1`）的路由组织、中间件链、子包划分以及典型接口的实现模式。

接口层采用统一的 `xreq.Endpoint` 抽象：

```go
type Endpoint struct {
    Path string
    Method string
    Handler Handler
    Authorizer func(*http.Request) error
    RegisterHandler func(*mux.Router) *mux.Route
}
```

每个子包导出自己的 Endpoint 变量切片，最终在 `endpoints/openapi_v1/endpoints.go` 或 `endpoints/innerapi_v1/endpoints.go` 中统一合并注册到 `gorilla/mux` 路由器。

---

## 2. 目录结构

```
ai-gateway-api/endpoints/
├── router.go # 根路由注册：静态文件、openapi_v1、innerapi_v1、全局中间件
├── middleware/ # HTTP 中间件
│ ├── access_logger.go # 访问日志
│ ├── convert.go # 请求/响应转换
│ ├── cors.go # 跨域处理
│ ├── product_probe.go # 产品线上下文解析
│ ├── recovery.go # panic 恢复
│ └── user_probe.go # 用户/Token 鉴权
├── openapi_v1/ # 管理面 OpenAPI
│ ├── endpoints.go # 统一合并注册各子包 Endpoints
│ ├── api_key/ # API-Key 管理（/api-keys）
│ ├── auth/ # 认证授权（/auth、/meta）
│ ├── bfe_pool/ # ALB 池（/alb-pool）
│ ├── certificate/ # 证书（/certificates）
│ ├── domain/ # 域名（当前未注册）
│ ├── entity/ # Entity 管理（/entities）
│ ├── entity_type/ # Entity-Type 管理（/entity-types）
│ ├── global_route_rules/ # Global 路由表（/global-route-rules）
│ ├── model_price/ # 模型定价管理（/model-prices）
│ ├── product_cluster/ # 集群管理（/clusters）
│ ├── provider/ # Provider 管理（/providers）
│ ├── route/ # 表达式校验（/expression/verify）
│ ├── route_tables/ # 路由表列表（/route-tables）
│ ├── subcluster/ # 子集群（当前未注册）
│ └── traffic/ # 调度器（当前未注册）
└── innerapi_v1/ # 数据面 Inner-API（BFE 配置拉取）
    ├── endpoints.go # 统一注册 Inner-API 导出接口
    ├── ai_route/
    ├── extra_file/
    ├── gslb_data/
    ├── mod_api_key/
    ├── mod_body_process/
    ├── protocol/
    ├── rate_limit_policy/
    ├── server_data/
    └── export_util/
```

---

## 3. 中间件设计

全局中间件在 `endpoints/router.go` 的 `RegisterRouters` 中注册，执行顺序为：

```go
router.Use(middleware.MCRecovery)
router.Use(middleware.MCLogger)
router.Use(middleware.MCCors)
```

各路由子树再按需挂载业务中间件：

- `/open-api/v1`：`McProductProbe` + `McUserProbe`
- `/inner-api/v1`：仅 `McUserProbe`

| 中间件 | 文件 | 作用 |
|--------|------|------|
| MCRecovery | recovery.go | 捕获 panic，返回统一错误响应 |
| MCLogger | access_logger.go | 记录请求/响应日志 |
| MCCors | cors.go | 处理 CORS 预检和响应头 |
| McProductProbe | product_probe.go | 从请求头解析产品线上下文 |
| McUserProbe | user_probe.go | 从 Session Key 或 Token 解析用户身份，完成权限校验 |
| Convert | convert.go | 负责 `xreq` 与标准 `http.Handler` 的适配 |

`McUserProbe` 会调用每个 Endpoint 上配置的 `Authorizer`（由 `iauth.FA` 或 `iauth.FAP` 生成），根据 Feature + Action 判断是否放行。

---

## 4. OpenAPI v1 接口层

路由前缀：`/open-api/v1`

### 4.1 路由注册

```go
// endpoints/openapi_v1/endpoints.go
func RegisterEndpoints(router *mux.Router) *mux.Router {
    openAPIV1Router := router.PathPrefix("/open-api/v1").Subrouter()
    openAPIV1Router.Use(middleware.McProductProbe, middleware.McUserProbe)
    for _, one := range endpoints() {
        one.Register(openAPIV1Router)
    }
    return openAPIV1Router
}

func endpoints() []*xreq.Endpoint {
    return merge(
        product.Routers, // 保留（产品线管理）
        product_cluster.Endpoints, // 精简：移除 model-providers、models
        certificate.Endpoints, // 新增证书详情
        product_pool.Endpoints, // 保留
        subcluster.Endpoints, // 保留但对外空切片（当前未注册）
        bfe_pool.Endpoints, // 保留
        auth.Endpoints, // 精简：移除 product 相关端点
        traffic.Endpoints, // 保留
        bfe_cluster.Endpoints, // 保留但对外空切片
        route.Endpoints, // 保留（表达式校验）
        domain.Endpoints, // 保留但对外空切片
        api_key.Endpoints, // 移除 generate 端点
        entity_type.Endpoints, // 保留
        entity.Endpoints, // 保留
        global_route_rules.Endpoints, // 保留
        route_tables.Endpoints, // 保留
        model_price.Endpoints, // 新增：模型定价管理
        provider.Endpoints, // 新增：Provider 管理
    )
}
```

> 说明：`product_pool`、`subcluster`、`traffic`、`bfe_cluster`、`domain` 子包当前导出的 `Endpoints` 切片为空，对应接口未实际注册（代码文件保留但已注释掉或为空）。

### 4.1.5 通用响应与错误码

- 返回值中**不再包含 `WorkMode` 字段**，统一返回 `{ErrNum, Data, ErrMsg}`。
- **新增错误码 `401` 表示鉴权失败**；`402` 表示没有调用权限；`510` 未启用。

### 4.2 子包与接口清单

#### 4.2.1 认证授权（auth）

代码位置：`endpoints/openapi_v1/auth/`

> 说明：权限模型收敛，用户 `is_admin` 仅支持 `true`（System 权限），Token `scope` 仅保留 `System`/`Support`，产品线授权相关接口已移除。

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| POST | `/auth/session-keys` | 通过密码创建 Session Key | 无（公开） |
| DELETE | `/auth/session-keys/{session_key}` | 销毁 Session Key | 无（公开） |
| POST | `/auth/tokens` | 创建 Token（`scope` 仅支持 `System`/`Support`） | `FeatureToken + ActionCreate` |
| DELETE | `/auth/tokens/{token_name}` | 删除 Token | `FeatureToken + ActionDelete` |
| GET | `/auth/tokens` | Token 列表 | `FeatureToken + ActionReadAll` |
| GET | `/auth/tokens/{token_name}` | Token 详情 | `FeatureToken + ActionReadAll` |
| POST | `/auth/users` | 创建用户（`password` 必填，`is_admin` 仅支持 `true`） | `FeatureUser + ActionCreate` |
| DELETE | `/auth/users/{user_name}` | 删除用户 | `FeatureUser + ActionDelete` |
| GET | `/auth/users` | 用户列表 | `FeatureUser + ActionReadAll` |
| GET | `/auth/users/{user_name}` | 用户详情 | `FeatureUser + ActionReadAll` |
| PATCH | `/auth/users/{user_name}/is_admin` | 设置管理员（固定为 `true`） | `FeatureUser + ActionUpdate` |
| PATCH | `/auth/users/{user_name}/passwd` | 修改密码 | `FeatureUser + ActionUpdate` |
| GET | `/meta` | 导航元数据 | 无 |

#### 4.2.2 Entity-Type（entity_type）

代码位置：`endpoints/openapi_v1/entity_type/`

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| POST | `/entity-types` | 创建 Entity 类型 | `FeatureEntityType + ActionCreate` |
| GET | `/entity-types` | Entity 类型列表 | `FeatureEntityType + ActionReadAll` |
| GET | `/entity-types/{type_name}` | Entity 类型详情 | `FeatureEntityType + ActionRead` |
| PATCH | `/entity-types/{type_name}` | 更新 Entity 类型 | `FeatureEntityType + ActionUpdate` |
| DELETE | `/entity-types/{type_name}` | 删除 Entity 类型 | `FeatureEntityType + ActionDelete` |

#### 4.2.3 Entity（entity）

代码位置：`endpoints/openapi_v1/entity/`

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| POST | `/entities` | 创建 Entity | `FeatureEntity + ActionCreate` |
| GET | `/entities` | Entity 列表 | `FeatureEntity + ActionReadAll` |
| GET | `/entities/{id}` | Entity 详情 | `FeatureEntity + ActionRead` |
| PUT | `/entities/{id}` | 全量更新 Entity | `FeatureEntity + ActionUpdate` |
| PATCH | `/entities/{id}` | 部分更新 Entity | `FeatureEntity + ActionUpdate` |
| DELETE | `/entities/{id}` | 删除 Entity | `FeatureEntity + ActionDelete` |
| GET | `/entities/{id}/quota-plan` | 查询 Entity 配额计划 | `FeatureEntity + ActionRead` |
| POST | `/entities/{id}/quota-plan/reset` | 重置 Entity 配额 | `FeatureEntity + ActionUpdate` |

#### 4.2.4 API-Key（api_key）

代码位置：`endpoints/openapi_v1/api_key/`

> 说明：创建 API-Key 时支持传入 `key`（全局唯一，更新时忽略），长度限制为 1-128 字符，与数据库 `api_keys.api_key varchar(128)` 保持一致；`GET /api-keys` 与 `GET /api-keys/{id}` 返回的 `quota_plan` 均包含 `balance`。

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| POST | `/api-keys` | 创建 API-Key | `FeatureAPIKey + ActionCreate` |
| GET | `/api-keys` | API-Key 列表（分页，`quota_plan` 含 `balance`） | `FeatureAPIKey + ActionReadAll` |
| GET | `/api-keys/{id}` | API-Key 详情（`quota_plan` 含 `balance`） | `FeatureAPIKey + ActionRead` |
| PUT | `/api-keys/{id}` | 全量更新 API-Key | `FeatureAPIKey + ActionUpdate` |
| PATCH | `/api-keys/{id}` | 部分更新 API-Key | `FeatureAPIKey + ActionUpdate` |
| DELETE | `/api-keys/{id}` | 删除 API-Key | `FeatureAPIKey + ActionDelete` |
| GET | `/api-keys/{id}/quota-plan` | 查询配额计划（含 `balance`） | `FeatureAPIKey + ActionRead` |
| POST | `/api-keys/{id}/quota-plan/reset` | 重置配额 | `FeatureAPIKey + ActionUpdate` |

#### 4.2.5 路由规则相关

> 说明：`/ai-route-rules` 模块已从 OpenAPI 中移除，AI 路由规则统一由 API-Key/Entity/Global 三级 `RouteRules` 管理。

**Global 路由表（global_route_rules）**：`endpoints/openapi_v1/global_route_rules/`

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| GET | `/global-route-rules` | 查询 Global 路由表 | `FeatureRoute + ActionRead` |
| PUT | `/global-route-rules` | 更新 Global 路由表 | `FeatureRoute + ActionUpdate` |

**路由表列表（route_tables）**：`endpoints/openapi_v1/route_tables/`

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| GET | `/route-tables` | 分页查询路由表 | `FeatureRoute + ActionReadAll` |

**表达式校验（route）**：`endpoints/openapi_v1/route/`

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| PATCH | `/expression/verify` | 校验 BFE 条件表达式 | `FeatureRoute + ActionRead` |

#### 4.2.6 Provider（provider）、集群（product_cluster）、模型定价与模型访问协议

代码位置：`endpoints/openapi_v1/provider/`、`endpoints/openapi_v1/product_cluster/`、`endpoints/openapi_v1/model_price/`

> 说明：Provider 与 Cluster 概念分离后，`/providers` 集中管理接入能力（`model_endpoint`、`models`、`keys`、`instance_pool`、`model_protocols`），并通过 `time_zone` / `tiers` 描述高峰/闲时时段规则；`/clusters` 只保留转发策略。`llm_config.provider` 必填，且必须引用 `/providers` 中已存在的 Provider；`llm_config.models` 须为 Provider `models` 的子集；`llm_config.keys` 元素结构变为 `{name, weight}`，通过 `name` 引用 Provider 中的 key，不再返回 key 明文。`llm_config.model_endpoint`、`llm_config.provider_type` 已删除。`llm_config.model_mappings` 字段仍使用 `source_model`/`target_model` 描述模型映射；`llm_config.key_policy` 用于声明选择策略、请求内重试与退避参数。InnerAPI 据此自动填充 `AIConf.ModelTable`：从 Provider 取 `time_zone` / `tiers`，从 `model-prices` 取 `prices` / `tier_prices`，并将 Provider 的 `model_protocols` 透传为 `AIConf.ModelProtocols`；`llm_config.model_table` 不在 OpenAPI `/clusters` 中展示。集群实例 `weight=0` 表示不接收流量，不会被后端强制改为默认值。

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| POST | `/providers` | 创建 Provider | `FeatureProvider + ActionCreate` |
| GET | `/providers` | Provider 列表 | `FeatureProvider + ActionRead` |
| GET | `/providers/{provider_name}` | Provider 详情 | `FeatureProvider + ActionRead` |
| PATCH | `/providers/{provider_name}` | 更新 Provider | `FeatureProvider + ActionUpdate` |
| DELETE | `/providers/{provider_name}` | 删除 Provider | `FeatureProvider + ActionDelete` |
| PUT | `/providers/{provider_name}/pricing-tiers` | 设置/更新 Provider 高峰/闲时模板（支持 JSON / YAML） | `FeatureProvider + ActionUpdate` |
| POST | `/providers/tools/discover-models` | 触发模型发现 | `FeatureProvider + ActionUpdate` |
| GET | `/providers/actions/get-provider-names` | 获取所有 Provider 名称列表 | `FeatureProvider + ActionRead` |
| POST | `/clusters` | 创建集群（通过引用 Provider 自动创建实例池 + 子集群） | `FeatureProductCluster + ActionCreate` |
| GET | `/clusters` | 集群列表 | `FeatureProductCluster + ActionRead` |
| GET | `/clusters/{cluster_name}` | 集群详情 | `FeatureProductCluster + ActionRead` |
| DELETE | `/clusters/{cluster_name}` | 删除集群 | `FeatureProductCluster + ActionDelete` |
| PATCH | `/clusters/{cluster_name}` | 更新集群基础信息 | `FeatureProductCluster + ActionUpdate` |
| POST | `/model-prices/import` | 整表导入 `model-list.yaml`（multipart/form-data） | `FeatureModelPrice + ActionCreate` |
| POST | `/model-prices` | 新增单条模型定价记录 | `FeatureModelPrice + ActionCreate` |
| GET | `/model-prices` | 分页列表查询 / 按组合键查询 | `FeatureModelPrice + ActionRead` |
| GET | `/model-prices/{id}` | 按 `id` 查询单条模型定价记录 | `FeatureModelPrice + ActionRead` |
| PUT | `/model-prices/{id}` | 按 `id` 修改单条模型定价记录 | `FeatureModelPrice + ActionUpdate` |
| PUT | `/model-prices` | 按 `(provider, model, mode)` 组合修改 | `FeatureModelPrice + ActionUpdate` |
| DELETE | `/model-prices/{id}` | 按 `id` 删除单条模型定价记录 | `FeatureModelPrice + ActionDelete` |
| DELETE | `/model-prices` | 按 `(provider, model, mode)` 组合删除 | `FeatureModelPrice + ActionDelete` |
| GET | `/model-prices/actions/get-providers` | 查询所有 price provider 名称列表 | `FeatureModelPrice + ActionRead` |

> 说明：`/model-prices/import` 使用 `multipart/form-data` 接收 `file`（`model-list.yaml`）与 `mode`（`replace` / `merge`，默认 `replace`）两个字段，仅允许管理员调用。`replace` 模式会清空 `model_prices` 表后全量写入；`merge` 模式对已有 `(provider, model, mode)` 记录更新、新增记录插入。导入时支持 `tier_prices` 字段（**初期 tier name 只支持 `peak`**）。导入时不再校验 `provider` 是否已存在于 `/providers`，未知 provider 可正常写入。

#### 4.2.7 证书（certificate）

代码位置：`endpoints/openapi_v1/certificate/`

> 说明：创建/更新证书时仅需上传证书与密钥内容（文件路径），不再要求提供文件名；`expired_date` 由服务端从证书内容解析，接口只读返回。

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| GET | `/certificates` | 证书列表 | `FeatureCert + ActionReadAll` |
| POST | `/certificates` | 上传证书 | `FeatureCert + ActionCreate` |
| GET | `/certificates/{cert_name}` | 证书详情 | `FeatureCert + ActionRead` |
| PATCH | `/certificates/{cert_name}/default` | 设置默认证书 | `FeatureCert + ActionUpdate` |
| DELETE | `/certificates/{cert_name}` | 删除证书 | `FeatureCert + ActionDelete` |

#### 4.2.8 ALB 池（bfe_pool）

代码位置：`endpoints/openapi_v1/bfe_pool/`

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| GET | `/alb-pool` | 查询默认 ALB 池 | `FeatureBFEPool + ActionReadAll` |
| PATCH | `/alb-pool` | 更新默认 ALB 池 | `FeatureBFEPool + ActionUpdate` |

### 4.3 典型接口实现

#### 4.3.1 Entity-Type 创建

```go
// endpoints/openapi_v1/entity_type/create.go
var EntityTypeCreateRoute = &xreq.Endpoint{
    Path: "/entity-types",
    Method: http.MethodPost,
    Handler: xreq.Convert(EntityTypeCreateAction),
    Authorizer: iauth.FA(iauth.FeatureEntityType, iauth.ActionCreate),
}

func EntityTypeCreateAction(req *http.Request) (interface{}, error) {
    param := &entity.EntityTypeParam{}
    if err := xreq.BindJSON(req, param); err != nil {
        return nil, err
    }

    if param.TypeName == nil || *param.TypeName == "" {
        return nil, xerror.WrapParamErrorWithMsg("type_name is required")
    }
    if param.Level == nil {
        return nil, xerror.WrapParamErrorWithMsg("level is required")
    }
    if *param.Level < 1 || *param.Level > 5 {
        return nil, xerror.WrapParamErrorWithMsg("level must be between 1 and 5")
    }

    existing, err := container.EntityTypeManager.FetchEntityType(req.Context(), &entity.EntityTypeFilter{
        TypeName: param.TypeName,
    })
    if err != nil {
        return nil, err
    }
    if existing != nil {
        return nil, xerror.WrapDuplicateData("entity type")
    }

    if _, err := container.EntityTypeManager.CreateEntityType(req.Context(), param); err != nil {
        return nil, err
    }

    return container.EntityTypeManager.FetchEntityType(req.Context(), &entity.EntityTypeFilter{
        TypeName: param.TypeName,
    })
}
```

#### 4.3.2 API-Key 创建

```go
// endpoints/openapi_v1/api_key/create.go
var APIKeyCreateRoute = &xreq.Endpoint{
    Path: "/api-keys",
    Method: http.MethodPost,
    Handler: xreq.Convert(APIKeyCreateAction),
    Authorizer: iauth.FA(iauth.FeatureAPIKey, iauth.ActionCreate),
}

func APIKeyCreateAction(req *http.Request) (interface{}, error) {
    param := &api_key.APIKeyParam{}
    if err := xreq.BindJSON(req, param); err != nil {
        return nil, err
    }
    return APIKeyCreateProcess(req.Context(), param, defaultProduct())
}

func APIKeyCreateProcess(ctx context.Context, param *api_key.APIKeyParam, product *ibasic.Product) (*api_key.APIKeyParam, error) {
    if param.Key == nil || *param.Key == "" {
        generatedKey, err := generateAPIKeyValue(product.Name)
        if err != nil {
            return nil, err
        }
        param.Key = &generatedKey
    }

    if err := checkCreateAPIKey(param, product.Name); err != nil {
        return nil, xerror.WrapParamError(err)
    }

    if param.EntityID != nil && *param.EntityID != "" {
        entity, err := container.EntityStorager.FetchEntity(ctx, &entity.EntityFilter{EntityID: param.EntityID})
        if err != nil {
            return nil, err
        }
        if entity == nil {
            return nil, xerror.WrapRecordNotExist("Entity")
        }
    }

    apiKeyID, err := generateAPIKeyID(ctx, product.Name)
    if err != nil {
        return nil, err
    }

    // 未提供配额/限流/路由时填充默认值
    if param.QuotaPlan == nil {
        param.QuotaPlan = &shared.QuotaPlanParam{Unlimited: lib.PBool(true)}
    }
    if param.RateLimitPolicy == nil {
        param.RateLimitPolicy = &shared.RateLimitPolicyParam{
            Enabled: lib.PBool(false),
            Rules: &shared.RateLimitRules{
                TpmConfigs: []shared.TPMConfig{},
                RpmConfigs: []shared.RPMConfig{},
                MaxConcurrency: lib.PInt(0),
            },
        }
    }
    if param.RouteRules == nil {
        param.RouteRules = &shared.RouteRulesParam{
            Enabled: lib.PBool(false),
            Rules: []*shared.AiRouteRuleParam{},
        }
    }

    err = container.APIKeyManager.CreateAPIKey(ctx, &api_key.APIKeyParam{
        ID: lib.PString(apiKeyID),
        Enable: param.Enable,
        Key: param.Key,
        ProductName: &product.Name,
        QuotaPlan: param.QuotaPlan,
        RateLimitPolicy: param.RateLimitPolicy,
        RouteRules: param.RouteRules,
        // ...
    })
    if err != nil {
        return nil, err
    }

    return container.APIKeyManager.FetchAPIKey(ctx, &api_key.APIKeyFilter{
        ID: &apiKeyID,
        ProductName: &product.Name,
    })
}
```

#### 4.3.3 Global 路由表

```go
// endpoints/openapi_v1/global_route_rules/endpoints.go
var GlobalRouteRulesGetRoute = &xreq.Endpoint{
    Path: "/global-route-rules",
    Method: http.MethodGet,
    Handler: xreq.Convert(GlobalRouteRulesGetAction),
    Authorizer: iauth.FA(iauth.FeatureRoute, iauth.ActionRead),
}

var GlobalRouteRulesUpdateRoute = &xreq.Endpoint{
    Path: "/global-route-rules",
    Method: http.MethodPut,
    Handler: xreq.Convert(GlobalRouteRulesUpdateAction),
    Authorizer: iauth.FA(iauth.FeatureRoute, iauth.ActionUpdate),
}

func GlobalRouteRulesGetAction(req *http.Request) (interface{}, error) {
    rules, err := container.RouteRulesManager.GetGlobalRouteRules(req.Context())
    if err != nil {
        return nil, err
    }
    if rules == nil {
        return nil, nil
    }
    rules.ID = nil // 隐藏内部 id
    return rules, nil
}

func GlobalRouteRulesUpdateAction(req *http.Request) (interface{}, error) {
    param := &shared.RouteRulesParam{}
    if err := xreq.BindJSON(req, param); err != nil {
        return nil, err
    }
    if param.Enabled == nil {
        enabled := true
        param.Enabled = &enabled
    }
    if param.Rules == nil {
        param.Rules = []*shared.AiRouteRuleParam{}
    }

    updated, err := container.RouteRulesManager.SetGlobalRouteRules(req.Context(), param)
    if err != nil {
        return nil, err
    }
    if updated == nil {
        return nil, xerror.WrapRecordNotExist("global route rules")
    }
    updated.ID = nil
    return updated, nil
}
```

---

## 4.4 参数合法性条件与公共类型

接口层参数校验遵循 **"文档先行、代码后补"** 的原则：每个 OpenAPI 接口的输入参数在 `design-docs/api-define/OpenAPI接口定义/*.md` 中都有明确的 `合法性条件` 列，复杂或复用的参数类型进一步收敛到 `00-common.md` 中统一描述。

### 4.4.1 校验分层

| 校验层级 | 负责内容 | 典型位置 |
|----------|----------|----------|
| 框架层 | 字段类型、必填、基本格式（由 `xreq.BindJSON`/`validate` 标签完成） | `endpoints/*/xxx.go` |
| 公共类型层 | 跨接口复用的原子类型规则（如 Hostname、IP、Port、CIDR、AIModel、ClusterName 等） | 文档：`00-common.md`；代码：各 Manager/Checker |
| 业务层 | 跨字段约束、资源引用存在性、业务状态一致性 | `model/*` Manager |

### 4.4.2 公共类型示例

`00-common.md` 中定义的公共类型包括：

- **网络原子类型**：`Hostname`、`IP Address`、`Port`、`CIDR`
- **业务命名类型**：`AIModel`、`ClusterName`、`ProviderName`、`EntityTypeName`、`UserName`、`Password`、`TokenName`
- **复杂配置类型**：`RouteRule`、`RouteRules`、`QuotaPlan`、`RateLimitPolicy`、`TPMConfig`、`RPMConfig`

接口文档中通过相对链接引用公共类型，例如：

```markdown
| 参数名 | 类型 | 参数含义 | 必填 | 补充描述 | 合法性条件 |
|--------|------|----------|------|----------|------------|
| name | string | 集群名 | Y | 全局唯一 | 必填；类型为 [ClusterName](./00-common.md#15-集群名称clustername) |
```

### 4.4.3 代码实现建议

后续增强代码校验时，建议：

1. 在 `model/*/checker.go` 或 `lib/validate` 中实现与 `00-common.md` 对应的公共校验函数；
2. 各 Manager 在参数入口处调用公共校验，再执行业务校验；
3. 新增接口必须同步更新接口定义文档的 `合法性条件`，优先引用已有公共类型。

---

## 5. InnerAPI v1 接口层

路由前缀：`/inner-api/v1`

### 5.1 路由注册

```go
// endpoints/innerapi_v1/endpoints.go
func RegisterRouter(router *mux.Router) *mux.Router {
    innerAPIV1Router := router.PathPrefix("/inner-api/v1").Subrouter()
    innerAPIV1Router.Use(middleware.McUserProbe)

    for _, one := range endpoints() {
        one.Register(innerAPIV1Router)
    }

    return innerAPIV1Router
}

func endpoints() []*xreq.Endpoint {
    return []*xreq.Endpoint{
        server_data.ExportEndpoint,
        gslb_data.ExportGSLBEndpoint,
        gslb_data.ExportClusterTableEndpoint,
        protocol.ServertCertExportEndpoint,
        extra_file.ExportExtraFileEndpoint,
        mod_api_key.ExportRoute,
        mod_body_process.ExportRoute,
        rate_limit_policy.ExportRoute,
        ai_route.ExportRoute,
    }
}
```

### 5.2 接口清单

| Method | Path | 用途 | Authorizer |
|--------|------|------|------------|
| GET | `/configs/tls_conf/server_data_conf` | 导出 TLS/Server/路由规则配置；`ClusterConf.Config.<cluster>.AIConf` 包含 `Provider`、`ModelTable` 与 `ModelProtocols` | `FeatureRoute + ActionExport` |
| GET | `/configs/gslb_data/gslb` | 导出 GSLB 调度配置（需 `bfe_cluster` 参数） | `FeatureRoute + ActionExport` |
| GET | `/configs/gslb_data/cluster_table` | 导出集群表配置 | `FeatureRoute + ActionExport` |
| GET | `/configs/protocol/server_cert_conf` | 导出证书配置 | `FeatureCert + ActionExport` |
| GET | `/configs/extra_files/{filename}` | 导出额外文件（PathPrefix） | `FeatureExtraFile + ActionExport` |
| GET | `/configs/mod-api-key` | 导出 API-Key 及配额配置 | `FeatureAPIKey + ActionExport` |
| GET | `/configs/mod-body-process` | 导出请求体处理配置 | `FeatureAPIKey + ActionExport` |
| GET | `/configs/rate-limit-policy` | 导出限流策略配置 | `FeatureRateLimitPolicy + ActionExport` |
| GET | `/configs/ai-route` | 导出 AI 路由配置 | `FeatureRoute + ActionExport` |

所有 Inner-API 导出接口均支持 `version` 查询参数，通过 `export_util.NewExportFromReq` 解析，并交给对应 Manager 的 `ConfigExport` 方法处理。当配置未变化时返回 `Data: nil`。

### 5.3 典型导出接口实现

#### 5.3.1 mod-api-key 导出

```go
// endpoints/innerapi_v1/mod_api_key/export.go
var ExportRoute = &xreq.Endpoint{
    Path: "/configs/mod-api-key",
    Method: http.MethodGet,
    Handler: xreq.Convert(ExportAction),
    Authorizer: iauth.FA(iauth.FeatureAPIKey, iauth.ActionExport),
}

func ExportAction(req *http.Request) (interface{}, error) {
    param, err := export_util.NewExportFromReq(req)
    if err != nil {
        return nil, err
    }
    return container.APIKeyRuleManager.ConfigExport(req.Context(), param.Version)
}
```

#### 5.3.2 rate-limit-policy 导出

```go
// endpoints/innerapi_v1/rate_limit_policy/export.go
var ExportRoute = &xreq.Endpoint{
    Path: "/configs/rate-limit-policy",
    Method: http.MethodGet,
    Handler: xreq.Convert(ExportAction),
    Authorizer: iauth.FA(iauth.FeatureRateLimitPolicy, iauth.ActionExport),
}

func ExportAction(req *http.Request) (interface{}, error) {
    param, err := export_util.NewExportFromReq(req)
    if err != nil {
        return nil, err
    }
    return container.RateLimitPolicyManager.ConfigExport(req.Context(), param.Version)
}
```

#### 5.3.3 额外文件导出（PathPrefix 模式）

```go
// endpoints/innerapi_v1/extra_file/export.go
var ExportExtraFileEndpoint = &xreq.Endpoint{
    RegisterHandler: func(router *mux.Router) *mux.Route {
        return router.PathPrefix("/configs/extra_files/").Methods(http.MethodGet)
    },
    Handler: xreq.RawConvert(ExportExtraFileAction),
    Authorizer: iauth.FA(iauth.FeatureExtraFile, iauth.ActionExport),
}

func ExportExtraFileAction(req *http.Request) (interface{}, error) {
    fileName := strings.SplitN(req.URL.Path, "extra_files/", 2)[1]
    extraFile, err := container.ExtraFileManager.FetchExtraFile(req.Context(), fileName)
    if err != nil {
        return nil, err
    }
    if extraFile == nil {
        return nil, xerror.WrapRecordNotExist()
    }
    return extraFile.Content, nil
}
```

---

## 6. 设计特点与约定

1. **统一 Endpoint 抽象**：所有接口无论 OpenAPI 还是 InnerAPI，都通过 `xreq.Endpoint` 描述，便于统一注册、鉴权和中间件处理。
2. **子包自治**：每个业务子包独立维护自己的 Endpoint 变量和 Action 函数，`endpoints.go` 仅做合并，不处理业务逻辑。
3. **鉴权粒度**：OpenAPI 使用 `iauth.FA`（普通鉴权）或 `iauth.FAP`（带产品线鉴权）；InnerAPI 统一使用 `iauth.FA` 配合 `ActionExport`。
4. **参数绑定**：通过 `xreq.BindJSON`、`xreq.BindQuery`、`xreq.BindForm`、`xreq.BindURI` 绑定请求参数，Action 函数内部再补充业务校验。
5. **模型层调用**：Action 函数不直接访问存储层，统一通过 `stateful/container` 中暴露的 Manager 完成业务处理。
6. **导出接口版本控制**：InnerAPI 通过 `version` 参数实现增量同步，由 `model/iversion_control` 统一管理配置版本。
