# Rainway AI Gateway - 模型层设计文档

## 1. 概述

本文档描述 `ai-gateway-api` 当前代码实现中 `model/` 层的设计，包括子包职责、核心结构体、接口定义、事务管理、典型业务流程以及与其他层的交互方式。

模型层采用 **Manager + Storager 接口** 的分层模式：
- **Manager**：封装业务逻辑、参数校验、事务编排与级联操作。
- **Storager 接口**：定义持久化操作契约，由 `storage/rdb/` 各子包实现。
- **Param / Filter**：分别用于写入入参与查询条件，字段多为指针类型以区分“未传”与“零值”。

模型层不直接依赖底层数据库，只依赖同包或 `model/shared` 中定义的 `XxxStorager` 接口，从而实现业务逻辑与存储实现的解耦。

---

## 2. 模型包结构

```
model/
├── api_key/
│ ├── api_key.go # APIKey 模型与 Manager
│ └── api_key_test.go # APIKey 单元测试
├── entity/
│ ├── entity_type.go # EntityType 模型
│ ├── entity_type_manager.go # EntityTypeManager
│ ├── entity.go # Entity 模型
│ └── entity_manager.go # EntityManager
├── ibasic/
│ ├── product.go # Product 模型
│ ├── bfe_cluster.go # BFECluster 模型
│ └── extra_file.go # ExtraFile 模型
├── iauth/
│ ├── authentication.go # 用户/Token 认证
│ ├── authorization.go # 权限校验
│ └── features.go # Feature-Action 权限定义
├── icluster_conf/
│ ├── cluster.go # Cluster 模型；LLMConfig 只保留转发策略，后端能力通过 provider 引用获取
│ ├── sub_cluster.go # SubCluster 模型
│ ├── pool.go # Pool 模型；实例池数据来自 Provider，按 cluster 生成 BFE 配置
│ ├── model_price.go # ModelPrice 模型（按 provider 查询，供 AIConf.ModelTable 使用）
│ └── exporter.go # 集群配置导出
├── iprovider/
│ ├── provider.go # Provider 实体与 CRUD
│ └── discover.go # 模型发现逻辑
├── imodel_price/
│ ├── model_price.go # ModelPrice 实体与 CRUD
│ └── import.go # model-list.yaml 解析与导入
├── iroute_conf/
│ ├── route_rule.go # 基础/高级路由规则
│ ├── domain.go # Domain 模型
│ └── exporter.go # 路由配置导出
├── iai_route/
│ ├── ai_route.go # AI 路由规则模型
│ ├── checker.go # AI 路由校验
│ └── common.go # 公共类型
├── iprotocol/
│ ├── certificate.go # Certificate 模型
│ └── exporter.go # 证书配置导出
├── iversion_control/
│ └── version_control.go # 配置版本控制
├── imods/
│ ├── mod_api_key_rule.go # mod-api-key 配置
│ ├── mod_body_process.go # mod-body-process 配置
│ ├── ai_route_exporter.go # AI 路由模块导出
│ └── exporter.go # 模块配置总入口
├── quota/
│ ├── quota_plan.go # QuotaPlan 模型
│ ├── quota_plan_manager.go # QuotaPlanManager
│ ├── quota_balance.go # QuotaBalance 模型
│ ├── balance_sync.go # BalanceSyncManager
│ └── scheduler.go # QuotaResetScheduler
├── rate_limit_policy/
│ ├── rate_limit_policy.go # RateLimitPolicy 模型
│ └── rate_limit_policy_manager.go # RateLimitPolicyManager
├── quotacache/
│   ├── quota_cache.go # QuotaCache 接口定义
│   └── quota_cache_redis.go # 基于 Redis 的实现
├── shared/
│ ├── types.go # 跨包共享类型
│ └── route_rules.go # RouteRules 模型（跨包共用）
├── route_rules/
│ └── route_rules.go # RouteRulesManager
└── itxn/
    └── txn.go # 事务抽象接口
```

> 说明：`model/quotacache` 是独立包，封装 API-Key / Entity 的实时配额缓存操作。`endpoints` 层不再直接调用 Redis，而是通过 `QuotaCache` 接口由 model 层统一维护缓存一致性。

---

## 3. 通用设计模式

### 3.1 四层结构

每个业务模型基本遵循 **Param / Filter / Storager / Manager** 四层结构：

| 类型 | 说明 |
|------|------|
| `XxxParam` | 创建/更新入参，字段多为指针类型。 |
| `XxxFilter` | 查询条件，包含分页字段 `Page` / `PageSize`。 |
| `XxxStorager` | 定义持久化操作的接口。 |
| `XxxManager` | 业务逻辑实现，聚合 `itxn.TxnStorager` 与其它 `Storager`。 |

### 3.2 函数命名与参数风格

| 操作 | 命名 | 参数 | 返回值 |
|------|------|------|--------|
| 创建 | `CreateXxx(ctx, param)` | `context.Context, *XxxParam` | `(int64, error)` 或 `error` |
| 查询单条 | `FetchXxx(ctx, filter)` | `context.Context, *XxxFilter` | `(*Xxx, error)` |
| 查询列表 | `FetchXxxList(ctx, filter)` | `context.Context, *XxxFilter` | `([]*Xxx, error)` |
| 更新 | `UpdateXxx(ctx, filter, param)` | `context.Context, *XxxFilter, *XxxParam` | `(int64, error)` |
| 删除 | `DeleteXxx(ctx, filter)` | `context.Context, *XxxFilter` | `error` |
| 校验 | `validateXxx`、`CheckXxx`、`XxxBeUsed` | 视业务而定 | `error` |

分页默认值一般为 `page=1, pageSize=20`，最大 100。

### 3.3 事务管理

`model/itxn/txn.go` 定义事务抽象：

```go
type TxnStorager interface {
    AtomExecute(ctx context.Context, do func(context.Context) error) error
}
```

`storage/rdb/txn/txn.go` 提供基于 `database/sql` 的实现：

```go
func (ps *RDBTxnStorager) AtomExecute(ctx context.Context, do func(context.Context) error) error {
    dbCtx, err := ps.dbCtxFactory(ctx, lib.OpenTxn())
    if err != nil {
        return err
    }
    return lib.RDBTxnExecute(dbCtx, do)
}
```

Manager 中的业务方法通常包裹在 `AtomExecute` 中：

```go
func (m *XxxManager) CreateXxx(ctx context.Context, param *XxxParam) error {
    return m.txn.AtomExecute(ctx, func(ctx context.Context) error {
        // 1. 参数校验
        // 2. 唯一性检查
        // 3. 调用 storager 写入
        return nil
    })
}
```

若传入的 `ctx` 已经是 `*lib.DBContext`，工厂会复用同一事务，从而支持嵌套事务场景。

### 3.4 模型层与存储层交互

`model/` 只依赖同包或 `model/shared` 中的 `XxxStorager` 接口，`storage/rdb/` 各子包提供具体实现：

```go
// storage/rdb/basic/product.go
var _ ibasic.ProductStorager = &ProductStorage{}

type ProductStorage struct {
    dbCtxFactory lib.DBContextFactory
}

func NewProductStorage(dbCtxFactory lib.DBContextFactory) *ProductStorage {
    return &ProductStorage{dbCtxFactory: dbCtxFactory}
}
```

`stateful/container/rdb/components.go` 负责依赖注入与单例初始化：

```go
container.TxnStoragerSingleton = txn.NewRDBTxnStorager(stateful.NewBFEDBContext)
container.ProductStoragerSingleton = basic.NewProductStorage(stateful.NewBFEDBContext)
container.QuotaCacheSingleton = quotacache.NewRedisQuotaCache(
    stateful.DefaultClientSet.RedisClient,
)

container.EntityTypeManager = entity.NewEntityTypeManager(
    container.TxnStoragerSingleton,
    container.EntityTypeStorager)

container.EntityManager = entity.NewEntityManager(
    container.TxnStoragerSingleton,
    container.EntityStorager,
    container.EntityTypeStorager,
    quota.NewQuotaPlanStoragerAdapter(container.QuotaPlanStorager),
    rate_limit_policy.NewRateLimitPolicyStoragerAdapter(container.RateLimitPolicyStorager),
    container.RouteRulesStorager,
    quota.NewQuotaBalanceStoragerAdapter(container.QuotaBalanceStorager),
    container.QuotaCacheSingleton)

container.RateLimitPolicyManager = rate_limit_policy.NewRateLimitPolicyManager(
    container.TxnStoragerSingleton,
    container.RateLimitPolicyStorager,
    container.APIKeyStorager,
    container.EntityStorager,
    container.VersionControlManager)

container.APIKeyManager = api_key.NewAPIKeyManager(
    container.TxnStoragerSingleton,
    container.APIKeyStorager,
    quota.NewQuotaPlanStoragerAdapter(container.QuotaPlanStorager),
    quota.NewRateLimitPolicyStoragerAdapter(container.RateLimitPolicyStorager),
    container.RouteRulesStorager,
    quota.NewEntityStoragerAdapter(container.EntityStorager),
    quota.NewQuotaBalanceStoragerAdapter(container.QuotaBalanceStorager),
    container.QuotaCacheSingleton,
)
```

### 3.5 适配器模式

`quota/` 子包除了定义自身的 `QuotaPlanStorager` / `QuotaBalanceStorager` 接口外，还通过 `NewXxxStoragerAdapter` 将 `entity.EntityStorager`、`rate_limit_policy.RateLimitPolicyStorager`、`quota.QuotaPlanStorager`、`quota.QuotaBalanceStorager` 适配为 `model/shared` 中定义的通用接口，供 `APIKeyManager` / `EntityManager` 统一调用：

```go
quota.NewQuotaPlanStoragerAdapter(quotaPlanStorager)                 // 输入：quota.QuotaPlanStorager
quota.NewRateLimitPolicyStoragerAdapter(rateLimitPolicyStorager)     // 输入：rate_limit_policy.RateLimitPolicyStorager
quota.NewEntityStoragerAdapter(entityStorager)                       // 输入：entity.EntityStorager
quota.NewQuotaBalanceStoragerAdapter(quotaBalanceStorager)           // 输入：quota.QuotaBalanceStorager
```

---

## 4. 各子包模型定义

### 4.1 `model/ibasic`

基础资源模型，包含产品、BFE 集群、附加文件。

#### 4.1.1 Product 模型

```go
// model/ibasic/product.go
type Product struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    MailList string `json:"mail_list"`
    ContactPerson string `json:"contact_person"`
    SmsList string `json:"sms_list"`
    Description string `json:"description"`
    CreateTime int64 `json:"create_time"`
    UpdateTime int64 `json:"update_time"`
}

type ProductParam struct {
    ID *int64 `json:"id"`
    Name *string `json:"name"`
    MailList *string `json:"mail_list"`
    ContactPerson *string `json:"contact_person"`
    SmsList *string `json:"sms_list"`
    Description *string `json:"description"`
}

type ProductFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}

type ProductStorager interface {
    CreateProduct(ctx context.Context, param *ProductParam) (int64, error)
    FetchProduct(ctx context.Context, filter *ProductFilter) (*Product, error)
    FetchProductList(ctx context.Context, filter *ProductFilter) ([]*Product, int64, error)
    UpdateProduct(ctx context.Context, filter *ProductFilter, param *ProductParam) (int64, error)
    DeleteProduct(ctx context.Context, filter *ProductFilter) error
}

type ProductManager struct {
    txn itxn.TxnStorager
    storager ProductStorager
}
```

`ProductManager.DeleteProduct` 会级联删除该产品关联的域名、集群、子集群、实例池、负载均衡矩阵、路由规则、用户产品绑定、附加文件等。

#### 4.1.2 BFECluster 模型

```go
// model/ibasic/bfe_cluster.go
type BFECluster struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    PoolName string `json:"pool_name"`
    Capacity int64 `json:"capacity"`
    Enabled bool `json:"enabled"`
    GtcEnabled bool `json:"gtc_enabled"`
    GtcManualEnabled bool `json:"gtc_manual_enabled"`
    ExemptTrafficCheck bool `json:"exempt_traffic_check"`
}

type BFEClusterParam struct {
    ID *int64 `json:"id"`
    Name *string `json:"name"`
    PoolName *string `json:"pool_name"`
    Capacity *int64 `json:"capacity"`
    Enabled *bool `json:"enabled"`
    GtcEnabled *bool `json:"gtc_enabled"`
    GtcManualEnabled *bool `json:"gtc_manual_enabled"`
    ExemptTrafficCheck *bool `json:"exempt_traffic_check"`
}

type BFEClusterFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}
```

#### 4.1.3 ExtraFile 模型

```go
// model/ibasic/extra_file.go
type ExtraFile struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    ProductID int64 `json:"product_id"`
    Description string `json:"description"`
    MD5 string `json:"md5"`
    Content string `json:"content"`
}

type ExtraFileParam struct {
    ID *int64 `json:"id"`
    Name *string `json:"name"`
    ProductID *int64 `json:"product_id"`
    Description *string `json:"description"`
    MD5 *string `json:"md5"`
    Content *string `json:"content"`
}

type ExtraFileFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    ProductID *int64 `form:"product_id"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}
```

---

### 4.2 `model/iauth`

认证授权模型。`users` 表同时存储用户与 Token，通过 `type` 字段区分。

```go
// model/iauth/authentication.go
type User struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    Type int `json:"type"`
    Password string `json:"-"`
    Ticket string `json:"ticket"`
    TicketCreatedAt int64 `json:"ticket_created_at"`
    Scopes string `json:"scopes"`
}

type Token struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    Type int `json:"type"`
    Ticket string `json:"ticket"`
    TicketCreatedAt int64 `json:"ticket_created_at"`
    Scopes string `json:"scopes"`
}

type AuthenticateParam struct {
    Type *string `json:"type"`
    Name *string `json:"name"`
    Password *string `json:"password"`
    Ticket *string `json:"ticket"`
}

type AuthenticateStorager interface {
    FetchUser(ctx context.Context, filter *UserFilter) (*User, error)
    FetchToken(ctx context.Context, filter *TokenFilter) (*Token, error)
    CreateUser(ctx context.Context, param *UserParam) (int64, error)
    CreateToken(ctx context.Context, param *TokenParam) (int64, error)
    UpdateUser(ctx context.Context, filter *UserFilter, param *UserParam) (int64, error)
    UpdateToken(ctx context.Context, filter *TokenFilter, param *TokenParam) (int64, error)
    DeleteUser(ctx context.Context, filter *UserFilter) error
    DeleteToken(ctx context.Context, filter *TokenFilter) error
}

type AuthenticateManager struct {
    storager AuthenticateStorager
}
```

认证方式包括 `Password`、`Session`、`Token`、`Skip`。密码登录成功后生成 Session Key：`crypto/rand` 生成随机字节，再经 `base64.URLEncoding` 编码。

```go
// model/iauth/authorization.go
type AuthorizeParam struct {
    Visitor *Visitor
    Feature Feature
    Action Action
    ValidateProduct bool
    ProductID *int64
}

type AuthorizeStorager interface {
    FetchUserProduct(ctx context.Context, userID, productID int64) (bool, error)
}

type AuthorizeManager struct {
    storager AuthorizeStorager
}
```

权限模型：
- 作用域：`ScopeSystem`、`ScopeSupport`（移除 `ScopeProduct`）。
- 用户创建参数 `UserCreateParam`：`password` 必填，`is_admin` 仅支持 `true`，不再包含 `type` 字段。
- Token 创建参数 `TokenCreateParam`：`scope` 仅支持 `System`/`Support`，删除 `product_name`。
- `scope2permission` 定义每个作用域对 Feature 的 Action 位图。
- `AuthorizeManager.Authorizate` 校验 Feature Action；`ValidateProduct` 路径仍保留在代码中，但 OpenAPI 不再使用产品线绑定。

---

### 4.3 `model/icluster_conf`

集群配置模型，包括 AI 集群、子集群、实例池。

#### 4.3.1 Cluster 模型

```go
// model/icluster_conf/cluster.go
type Cluster struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    Description string `json:"description"`
    ProductID int64 `json:"product_id"`
    Protocol string `json:"protocol"`
    MaxIdleConnPerHost int `json:"max_idle_conn_per_host"`
    TimeoutConnServ int `json:"timeout_conn_serv"`
    TimeoutResponseHeader int `json:"timeout_response_header"`
    TimeoutReadbodyClient int `json:"timeout_readbody_client"`
    TimeoutReadClientAgain int `json:"timeout_read_client_again"`
    TimeoutWriteClient int `json:"timeout_write_client"`
    HealthcheckSchem string `json:"healthcheck_schem"`
    HealthcheckInterval int `json:"healthcheck_interval"`
    HealthcheckFailnum int `json:"healthcheck_failnum"`
    HealthcheckHost string `json:"healthcheck_host"`
    HealthcheckURI string `json:"healthcheck_uri"`
    HealthcheckStatuscode int `json:"healthcheck_statuscode"`
    ClientipCarry int `json:"clientip_carry"`
    PortCarry bool `json:"port_carry"`
    MaxRetryInCluster int `json:"max_retry_in_cluster"`
    MaxRetryCrossCluster int `json:"max_retry_cross_cluster"`
    Ready bool `json:"ready"`
    HashStrategy int `json:"hash_strategy"`
    CookieKey string `json:"cookie_key"`
    HashHeader string `json:"hash_header"`
    SessionSticky bool `json:"session_sticky"`
    ReqWriteBufferSize int `json:"req_write_buffer_size"`
    ReqFlushInterval int `json:"req_flush_interval"`
    ResFlushInterval int `json:"res_flush_interval"`
    CancelOnClientClose bool `json:"cancel_on_client_close"`
    FailureStatus bool `json:"failure_status"`
    MaxConnsPerHost int `json:"max_conns_per_host"`
    LLMConfig string `json:"llm_config"`
}

type ClusterParam struct {
    Name *string `json:"name"`
    Description *string `json:"description"`
    ProductID *int64 `json:"product_id"`
    Protocol *string `json:"protocol"`
    MaxIdleConnPerHost *int `json:"max_idle_conn_per_host"`
    // ... 其他字段
    LLMConfig *string `json:"llm_config"`
}

type ClusterFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    ProductID *int64 `form:"product_id"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}

type ClusterStorager interface {
    CreateCluster(ctx context.Context, param *ClusterParam) (int64, error)
    FetchCluster(ctx context.Context, filter *ClusterFilter) (*Cluster, error)
    FetchClusterList(ctx context.Context, filter *ClusterFilter) ([]*Cluster, int64, error)
    UpdateCluster(ctx context.Context, filter *ClusterFilter, param *ClusterParam) (int64, error)
    DeleteCluster(ctx context.Context, filter *ClusterFilter) error
}

type ClusterManager struct {
    txn itxn.TxnStorager
    storager ClusterStorager
    subClusterStorager SubClusterStorager
    bfeClusterStorager ibasic.BFEClusterStorager
    poolStorager PoolStorager
    providerStorager iprovider.ProviderStorager
    versionControlManager *iversion_control.VersionControlManager
    deleteCheckers map[string]func(context.Context, *ibasic.Product, *Cluster) error
}
```

`ClusterManager.CreateCluster` / `UpdateCluster` 会校验 `llm_config.provider` 引用已存在的 Provider，且 `llm_config.models` 为 Provider `models` 的子集、`llm_config.keys` 中的 `name` 均存在于 Provider `keys` 中；创建/更新时根据 Provider 的 `instance_pool` 自动生成或更新实例池与子集群。

`ClusterManager.DeleteCluster` 会执行引用检查：若该集群被产品级路由规则（Product Route Rules）或 AI 路由规则（AI Route Rules，含 Global / Entity / API-Key 三级）引用，则删除失败；其中 AI 路由规则检查由 `model/route_rules/route_rules.go` 的 `RouteRulesManager.ClusterDeleteChecker` 实现。通过引用检查后，遍历并级联删除子集群及其关联实例池，最后删除集群与负载均衡矩阵。

**对外模型**

Cluster 对外模型与存储模型解耦，不再暴露 `ready`、`sub_clusters`、`scheduler`、`instance_pool` 等内部概念，对外仅保留转发相关配置：

```json
{
    "name": "my-cluster",
    "description": "示例集群",
    "basic": {
        "protocol": "http",
        "connection": {
            "max_idle_conn_per_rs": 0,
            "cancel_on_client_close": false
        },
        "retries": {
            "max_retry_in_cluster": 2
        },
        "buffers": {
            "req_write_buffer_size": 512
        },
        "timeouts": {
            "timeout_conn_serv": 50000,
            "timeout_response_header": 50000,
            "timeout_readbody_client": 30000,
            "timeout_read_client_again": 30000,
            "timeout_write_client": 60000
        }
    },
    "sticky_sessions": {
        "enabled": false,
        "hash_strategy": "CLIENT_IP_ONLY",
        "hash_header": ""
    },
    "passive_health_check": {
        "interval": 1000,
        "failnum": 3,
        "host": "",
        "uri": "/",
        "statuscode": 0
    },
    "llm_config": {
        "models": ["deepseek-chat", "deepseek-coder"],
        "model_mappings": [{"source_model": "gpt-4", "target_model": "deepseek-chat"}],
        "keys": [
            {
                "name": "key-primary",
                "weight": 70
            },
            {
                "name": "key-secondary",
                "weight": 30
            }
        ],
        "key_policy": {
            "strategy": "weighted_random",
            "max_retries": 3,
            "retry_backoff_initial": 500,
            "retry_backoff_max": 5000
        },
        "provider": "deepseek",
        "match_prefix": "deepseek/",
        "strip_prefix": true
    }
}
```

主要变更：

| 字段 | 变更 |
|------|------|
| `ready` | 对外隐藏 |
| `sub_clusters` | 对外隐藏，每个集群固定一个子集群 |
| `scheduler` | 对外隐藏，固定 `GSLB_BLACKHOLE=0` |
| `instance_pool` | **从 cluster 移除**，下沉到 Provider；cluster 通过 `llm_config.provider` 引用获取后端实例池 |
| `instance.tags` | 删除 |
| `basic.retries.max_retry_in_subcluster` | 重命名为 `max_retry_in_cluster` |
| `basic.retries.max_retry_cross_subcluster` | 删除 |
| `sticky_sessions.session_sticky_type` | 删除，默认实例级会话保持 |
| `sticky_sessions.enabled` | 新增，默认 `false` |
| `passive_health_check.*` | 改为选填，提供默认值 |
| `llm_config.service_name` / `group` | 删除 |
| `llm_config.model_endpoint` | **删除**，下沉到 Provider |
| `llm_config.provider_type` | **删除**，由 Provider 的 `model_protocols` 替代 |
| `llm_config.model_mappings[].key/value` | 重命名为 `source_model` / `target_model` |
| `llm_config.key` | **删除**；替换为 `llm_config.keys`（Key 引用与权重数组）+ `llm_config.key_policy`（Key 路由策略） |
| `llm_config.keys` | 元素结构改为 `{name, weight}`，通过 `name` 引用 Provider 中的 key，不再保存 key 明文 |
| `llm_config.provider` | **改为必填**，必须引用 `/providers` 中已存在的 Provider；用于获取后端实例池、key 明文、填充 `AIConf.ModelTable` |
| `llm_config.models` | 须为所属 Provider `models` 的子集 |
| `llm_config.match_prefix` / `strip_prefix` | 保留：用于 OpenRouter 等聚合 provider 场景，按前缀匹配并决定是否裁剪请求 `model` 字段前缀 |

创建集群时，`basic`、`sticky_sessions`、`passive_health_check` 改为可选；`llm_config` 必填，且 `llm_config.provider` 必须引用已存在的 Provider。

#### LLMConfig 数据模型

`model/icluster_conf/cluster.go` 中 `LLMConfig` 结构体对应 OpenAPI `/clusters` 的 `llm_config` 字段，只保留转发策略：

```go
type ClusterKeyRef struct {
    Name   *string `json:"name"`   // 必填；须对应 Provider keys 中存在的 name；同一 keys 数组内唯一
    Weight *int    `json:"weight"` // 必填；[0,100]
}

type KeyPolicy struct {
    Strategy            *string `json:"strategy"`              // 默认 weighted_random
    MaxRetries          *int    `json:"max_retries"`           // 默认 0
    RetryBackoffInitial *int    `json:"retry_backoff_initial"` // 默认 500
    RetryBackoffMax     *int    `json:"retry_backoff_max"`     // 默认 5000
}

type LLMConfig struct {
    Models         []string         `json:"models"`
    ModelMappings  []ModelMapping   `json:"model_mappings"`
    Keys           []ClusterKeyRef  `json:"keys"`          // Key 引用与权重；默认空数组
    KeyPolicy      *KeyPolicy       `json:"key_policy"`
    Provider       *string          `json:"provider"`      // 必填：引用 /providers 中已存在的 Provider
    MatchPrefix    *string          `json:"match_prefix"`  // 需要匹配的 provider/model 前缀，必须以 "/" 结尾
    StripPrefix    *bool            `json:"strip_prefix"`  // 是否裁剪 MatchPrefix
}
```

校验要点：

- `provider` 必填，且必须引用 `/providers` 中已存在的 Provider；
- `models` 必填，至少 1 个元素；每个模型名必须存在于所属 Provider 的 `models` 中；
- `keys` 非必填，默认值为空数组 `[]`；
- 若 `keys` 非空，所有元素 `name` 必填且必须对应 Provider `keys` 中存在的 name，同一数组内 `name` 唯一，`weight` ∈ `[0,100]`，且所有 `weight` 之和必须等于 `100`；
- `key_policy` 若传入，`strategy` 仅允许 `weighted_random`，`max_retries`、`retry_backoff_initial`、`retry_backoff_max` 均须 `>=0`，且 `retry_backoff_max >= retry_backoff_initial`；
- `match_prefix` 非必填，若传入且非空则必须以 `/` 结尾；
- `strip_prefix` 为 `true` 时，`match_prefix` 必填且非空。

#### Cluster 配置导出（AIConf）

`model/icluster_conf/exporter.go` 在导出 `server_data_conf` 时，将 `LLMConfig` 映射为 BFE `AIConf` 结构。Provider 与 Cluster 分离后，`AIConf` 的实例池来自 Provider、`Keys` 通过按 `name` join Provider 的 key 明文与 Cluster 的 key 权重生成：

```go
type AIKey struct {
    Name   string
    Key    string
    Weight int
}

type AIKeyPolicy struct {
    Strategy            string
    MaxRetries          int
    RetryBackoffInitial int
    RetryBackoffMax     int
}

type ModelPriceEntry struct {
    Provider            string
    Model               string
    BaseModel           string
    Mode                string
    Capabilities        []string
    SupportedParameters []string
    Limits              map[string]any
    Prices              map[string]any            // 默认价格；未命中 tier 时作为 fallback
    TierPrices          map[string]map[string]any // tier name -> 价格对象
}

type ModelTable struct {
    Currency string              // 固定为 "RMB"
    TimeZone string              // 计算时段所使用的时区；默认 "Asia/Shanghai"
    Tiers    []PriceTier         // 时段 tier 定义列表
    Models   []ModelPriceEntry
}

type PriceTier struct {
    Name       string      // 初期只支持 "peak"
    TimeRanges []TimeRange // 命中任意一个即属于该 tier
}

type TimeRange struct {
    Weekdays []int  // 0=周日，1=周一，...，6=周六；为空表示每天
    Start    string // "HH:MM"
    End      string // "HH:MM"；必须 > Start；跨午夜请拆成两段
}

type AIConf struct {
    Type           int
    ModelMapping   *map[string]string
    Provider       string          // 对应 LLMConfig.Provider
    Keys           []AIKey
    KeyPolicy      *AIKeyPolicy
    MatchPrefix    string          // 对应 LLMConfig.MatchPrefix
    StripPrefix    bool            // 对应 LLMConfig.StripPrefix
    ModelProtocols []string        // 对应 Provider.ModelProtocols，供 BFE 做协议风格匹配
    ModelTable     *ModelTable     // 按 Provider 从 model_prices 查询填充
}
```

导出时 `KeyPolicy` 字段若未配置则填充默认值：`Strategy="weighted_random"`、`MaxRetries=0`、`RetryBackoffInitial=500`、`RetryBackoffMax=5000`。

导出逻辑：

1. 根据 `cluster.LLMConfig.Provider` 查询 Provider，读取 `instance_pool` 生成 BFE 实例池/子集群/集群配置（结构与原 cluster 直接持有 `instance_pool` 时一致）。
2. 根据 `cluster.LLMConfig.Keys` 中的 `name`，从 Provider `keys` 中查找对应 `key` 明文，按 `weight` 组装为 `AIConf.Keys`。
3. 将 Provider 的 `model_protocols` 原样透传到 `AIConf.ModelProtocols`，供 BFE 判断请求协议风格是否被当前集群支持。若 Provider 未设置 `model_protocols`，`AIConf.ModelProtocols` 为空数组，BFE 兜底为仅支持 `openai`。
4. 若 `Provider` 非空：
   - 读取 Provider 的 `time_zone` 和 `tiers`，填充到 `AIConf.ModelTable.TimeZone` / `Tiers`；`time_zone` 为空时默认 `"Asia/Shanghai"`；`tiers` 为空时 `Tiers` 为空数组。
   - 调用 `model/imodel_price` 按 `provider` 查询 `model_prices` 记录，将 `prices` 和 `tier_prices` 分别填充到 `AIConf.ModelTable.Models[].Prices` / `TierPrices`。
   - `Currency` 固定填充为 `"RMB"`。
   - 若 `Provider` 为空或该 provider 无记录，`ModelTable.Models` 为空列表。

`model_prices` 变更不会同步触发 InnerAPI 推送，由 BFE/Conf Agent 按自身周期拉取配置。

#### 4.3.2 Provider 模型

Provider 模型位于 `model/iprovider/`，与 Cluster 解耦后独立管理下游模型提供方的接入能力。

```go
// model/iprovider/provider.go
type Provider struct {
    ID              int64    `json:"id"`
    Name            string   `json:"name"`
    Description     string   `json:"description"`
    ModelEndpoint   string   `json:"model_endpoint"`   // JSON：{schema, uri}
    Models          string   `json:"models"`           // JSON：模型名数组
    Keys            string   `json:"keys"`             // JSON：ProviderKey 数组
    InstancePool    string   `json:"instance_pool"`    // JSON：Instance 数组
    ModelProtocols  string   `json:"model_protocols"`  // JSON：协议枚举数组
    TimeZone        string   `json:"time_zone"`        // 计算时段所使用的时区；默认 "Asia/Shanghai"
    Tiers           string   `json:"tiers"`            // JSON：PricingTier 数组
    CreatedAt       time.Time `json:"created_at"`
    UpdatedAt       time.Time `json:"updated_at"`
}

type ProviderParam struct {
    Name           *string `json:"name"`
    Description    *string `json:"description"`
    ModelEndpoint  *string `json:"model_endpoint"`
    Models         *string `json:"models"`
    Keys           *string `json:"keys"`
    InstancePool   *string `json:"instance_pool"`
    ModelProtocols *string `json:"model_protocols"`
    TimeZone       *string `json:"time_zone"`
    Tiers          *string `json:"tiers"`
}

type ProviderFilter struct {
    ID       *int64  `form:"id"`
    Name     *string `form:"name"`
    Page     *int    `form:"page"`
    PageSize *int    `form:"page_size"`
}

type ProviderStorager interface {
    CreateProvider(ctx context.Context, param *ProviderParam) (int64, error)
    FetchProvider(ctx context.Context, filter *ProviderFilter) (*Provider, error)
    FetchProviderList(ctx context.Context, filter *ProviderFilter) ([]*Provider, int64, error)
    UpdateProvider(ctx context.Context, filter *ProviderFilter, param *ProviderParam) (int64, error)
    DeleteProvider(ctx context.Context, filter *ProviderFilter) error
}

type ProviderManager struct {
    txn                   itxn.TxnStorager
    storager              ProviderStorager
    clusterStorager       icluster_conf.ClusterStorager
    modelPriceStorager    imodel_price.ModelPriceStorager
}
```

`ProviderManager` 提供与通用模型一致的 CRUD，并额外处理引用关系：

- `CreateProvider`：校验 `name` 全局唯一、`instance_pool` 合法、`model_protocols` 合法；若未传 `model_endpoint` 则使用默认值；若携带 `tiers` 则按 PricingTier 结构校验（**初期 `name` 只支持 `peak`**）。
- `UpdateProvider`：允许更新 `instance_pool`、`keys`、`models`、`time_zone`、`tiers` 等；若 `keys` 中的 `name` 被 cluster 引用，更新后所有引用该 `name` 的 cluster 在导出时会自动使用新 key 明文（由 `AIConf` 导出逻辑按 name join 实现）。
- `UpdatePricingTiers`：接收 `provider_name`、`time_zone`、`tiers`（JSON 或 YAML 解析后），校验 `time_zone` 为合法 IANA 时区名、`tiers` 中每个 tier 的 `name` 非空且 **初期只支持 `peak`**、`time_ranges` 格式合法且不重叠，然后更新 provider 的 `time_zone` / `tiers` 字段。
- `DeleteProvider`：删除前校验无 `/clusters` 引用，否则返回错误；`/model-prices` 中的同名 provider 不再作为阻塞条件。通过校验后删除 provider。
- `DiscoverModels`：接收 `model_protocol`、`schema`、`addr`、`port`、`uri`、`apikey` 参数；构造请求 URL 并携带认证头调用模型列表接口；按 `model_protocol` 选择对应响应解析器（如 `openai` 用 `data[].id`，`anthropic` 用 `models[].model_id`），解析并返回模型名列表。

#### 4.3.3 SubCluster 模型

```go
// model/icluster_conf/sub_cluster.go
type SubCluster struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    ClusterID int64 `json:"cluster_id"`
    ProductID int64 `json:"product_id"`
    Description string `json:"description"`
    PoolsID int64 `json:"pools_id"`
    Enabled bool `json:"enabled"`
    Role string `json:"role"`
}

type SubClusterParam struct {
    Name *string `json:"name"`
    ClusterID *int64 `json:"cluster_id"`
    ProductID *int64 `json:"product_id"`
    Description *string `json:"description"`
    PoolsID *int64 `json:"pools_id"`
    Enabled *bool `json:"enabled"`
    Role *string `json:"role"`
}

type SubClusterFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    ClusterID *int64 `form:"cluster_id"`
    ProductID *int64 `form:"product_id"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}
```

#### 4.3.4 Pool 模型

```go
// model/icluster_conf/pool.go
type Pool struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    ProductID int64 `json:"product_id"`
    Ready bool `json:"ready"`
    InstanceDetail string `json:"instance_detail"`
    Type int `json:"type"`
    Tag int `json:"tag"`
    Role string `json:"role"`
    EppServer string `json:"epp_server"`
}

type PoolParam struct {
    Name *string `json:"name"`
    ProductID *int64 `json:"product_id"`
    Ready *bool `json:"ready"`
    InstanceDetail *string `json:"instance_detail"`
    Type *int `json:"type"`
    Tag *int `json:"tag"`
    Role *string `json:"role"`
    EppServer *string `json:"epp_server"`
}

type PoolFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    ProductID *int64 `form:"product_id"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}
```

#### 4.3.5 APIKey 模型（`model/api_key`）

```go
// model/api_key/api_key.go
import "github.com/rainway-ai-gateway/ai-gateway-api/model/shared"

type APIKeyParam struct {
    ID *string `json:"id"`
    Enable *bool `json:"enabled"`
    Status *string `json:"status,omitempty"`
    CreateTime *int64 `json:"create_time,omitempty"`
    UpdatedTime *int64 `json:"update_time,omitempty"`
    KeyCreateAt *time.Time `json:"-"`

    Key *string `json:"key"` // 创建时可传入，用于导入外部 API-Key；更新时忽略
    Description *string `json:"description,omitempty"`
    UnlimitedQuota *bool `json:"unlimited_quota,omitempty"`
    ExpiredTime *int64 `json:"expired_time,omitempty"`
    Models []string `json:"models,omitempty"`
    Subnet []string `json:"subnet,omitempty"`
    EntityID *string `json:"entity_id,omitempty"`
    QuotaPlanID *int64 `json:"quota_plan_id,omitempty"`
    RateLimitPolicyID *int64 `json:"rate_limit_policy_id,omitempty"`
    RouteRulesID *int64 `json:"route_rules_id,omitempty"`
    ProductName *string `json:"-"`
    InnerID *int64 `json:"-"`
    RemainingQuota *int64 `json:"remaining_quota,omitempty"`

    QuotaPlan *shared.QuotaPlanParam `json:"quota_plan,omitempty"` // 列表/详情中已包含 balance
    RateLimitPolicy *shared.RateLimitPolicyParam `json:"rate_limit_policy,omitempty"`
    RouteRules *shared.RouteRulesParam `json:"route_rules,omitempty"`
    Entity *shared.EntitySummary `json:"entity,omitempty"`
}

type APIKeyFilter struct {
    ProductName *string
    ProductNames []string
    ALBGroupName *string
    ID *string
    InnerID *int64
    QuotaPlanID *int64
    RouteRulesID *int64
    Page *int
    PageSize *int
    Enabled *bool
    EntityID *string
    UnlimitedQuota *bool
}

type APIKeyStorager interface {
    CreateAPIKey(ctx context.Context, param *APIKeyParam) (int64, error)
    FetchAPIKey(ctx context.Context, filter *APIKeyFilter) (*APIKeyParam, error)
    FetchAPIKeyList(ctx context.Context, filter *APIKeyFilter) ([]*APIKeyParam, int64, error)
    UpdateAPIKey(ctx context.Context, filter *APIKeyFilter, param *APIKeyParam) (int64, error)
    DeleteAPIKey(ctx context.Context, filter *APIKeyFilter) error
}

type APIKeyManager struct {
    storager                APIKeyStorager
    txn                     itxn.TxnStorager
    quotaPlanStorager       shared.QuotaPlanStorager
    rateLimitPolicyStorager shared.RateLimitPolicyStorager
    routeRulesStorager      shared.RouteRulesStorager
    entityStorager          shared.EntityStorager
    quotaBalanceStorager    shared.QuotaBalanceStorager
    quotaCache              quotacache.QuotaCache  // 新增：实时配额缓存
}
```

`APIKeyManager.CreateAPIKey` 核心流程：

```go
func (m *APIKeyManager) CreateAPIKey(ctx context.Context, param *APIKeyParam) error {
    return m.txn.AtomExecute(ctx, func(ctx context.Context) error {
        // 1. 检查同产品下 ID 是否重复
        list, err := m.storager.FetchAPIKeyList(ctx, &APIKeyFilter{
            ID: param.ID,
            ProductName: param.ProductName,
        })
        if err != nil {
            return err
        }
        if len(list) > 0 {
            return xerror.WrapParamErrorWithMsg(fmt.Sprintf("Duplicate id with product:%s", *param.ProductName))
        }

        // 2. 验证 Entity（如果指定）
        if param.EntityID != nil && *param.EntityID != "" && m.entityStorager != nil {
            entity, err := m.entityStorager.FetchEntity(ctx, &shared.EntityFilter{EntityID: param.EntityID})
            if err != nil {
                return err
            }
            if entity == nil {
                return xerror.WrapParamErrorWithMsg(fmt.Sprintf("Entity not found: %s", *param.EntityID))
            }
        }

        // 3. 创建 QuotaPlan（如果有）
        if param.QuotaPlan != nil && m.quotaPlanStorager != nil {
            quotaPlanID, err := m.quotaPlanStorager.CreateQuotaPlan(ctx, param.QuotaPlan)
            if err != nil {
                return err
            }
            param.QuotaPlanID = &quotaPlanID
            if param.QuotaPlan.Unlimited == nil || !*param.QuotaPlan.Unlimited {
                if m.quotaBalanceStorager != nil && param.QuotaPlan.Quota != nil {
                    if err := m.quotaBalanceStorager.CreateQuotaBalance(ctx, quotaPlanID, param.QuotaPlan.Quota); err != nil {
                        return err
                    }
                }
            }
        }

        // 4. 创建 RateLimitPolicy（如果有）
        if param.RateLimitPolicy != nil && m.rateLimitPolicyStorager != nil {
            rateLimitPolicyID, err := m.rateLimitPolicyStorager.CreateRateLimitPolicy(ctx, param.RateLimitPolicy)
            if err != nil {
                return err
            }
            param.RateLimitPolicyID = &rateLimitPolicyID
        }

        // 5. 创建 RouteRules（如果有）
        if param.RouteRules != nil && m.routeRulesStorager != nil {
            routeRulesID, err := m.routeRulesStorager.CreateRouteRules(
                ctx, shared.RouteRulesTypeAPIKey, param.Key, param.RouteRules)
            if err != nil {
                return err
            }
            param.RouteRulesID = &routeRulesID
        }

        // 6. 创建 APIKey
        _, err = m.storager.CreateAPIKey(ctx, param)
        return err
    })
    if err != nil {
        return err
    }

    // 7. 同步 Redis 剩余量（事务外，最终一致；失败不影响 DB 已提交结果）
    if param.Key != nil && param.QuotaPlan != nil &&
        (param.QuotaPlan.Unlimited == nil || !*param.QuotaPlan.Unlimited) &&
        param.QuotaPlan.Quota != nil && m.quotaCache != nil {
        if err := m.quotaCache.SetRemaining(ctx, *param.Key, param.QuotaPlan.Quota, param.QuotaPlan.Unit); err != nil {
            // 记录日志，不阻塞接口返回
            stateful.AccessLogger.Warn("failed to set quota cache for api_key %s: %v", *param.Key, err)
        }
    }
    return nil
}
```

`APIKeyManager.CreateAPIKey` 新增对传入 `key` 的全局唯一性校验（覆盖 `api_keys.api_key` 与 `api_key_tokens.api_key`），若冲突返回 422。

`APIKeyManager.DeleteAPIKey` 会级联删除 `quota_balance`、`quota_plan`、`rate_limit_policy`、`route_rules`，最后删除 `api_key`。

---

### 4.4 `model/imodel_price`

模型定价管理模型，对应数据库 `model_prices` 表，是 InnerAPI `AIConf.ModelTable` 的唯一数据源。

#### 4.4.1 ModelPrice 实体

```go
// model/imodel_price/model_price.go
type ModelPrice struct {
    ID                  int64          `json:"id"`
    Provider            string         `json:"provider"`              // Provider / Cluster 标识；仅作为价格归集标识，不强制引用 /providers 中已存在的 Provider
    Model               string         `json:"model"`                 // 模型名
    BaseModel           string         `json:"base_model"`            // 归一化模型名
    Mode                string         `json:"mode"`                  // 请求模式
    Capabilities        []string       `json:"capabilities"`          // 能力列表
    SupportedParameters []string       `json:"supported_parameters"`  // 支持的请求参数列表
    Limits              map[string]any `json:"limits"`                // 限制对象
    Prices              map[string]any `json:"prices"`                // 默认价格对象；未命中 tier 时作为 fallback
    TierPrices          map[string]map[string]any `json:"tier_prices"` // tier name -> 价格对象
    PriceCurrency       string         `json:"price_currency"`        // 当前固定 RMB
    Metadata            map[string]any `json:"metadata"`              // 元数据
    CreatedAt           time.Time      `json:"created_at"`
    UpdatedAt           time.Time      `json:"updated_at"`
}

type ModelPriceParam struct {
    ID                  *int64          `json:"id"`
    Provider            *string         `json:"provider"`
    Model               *string         `json:"model"`
    BaseModel           *string         `json:"base_model"`
    Mode                *string         `json:"mode"`
    Capabilities        []string        `json:"capabilities"`
    SupportedParameters []string        `json:"supported_parameters"`
    Limits              map[string]any  `json:"limits"`
    Prices              map[string]any  `json:"prices"`
    TierPrices          map[string]map[string]any `json:"tier_prices"`
    PriceCurrency       *string         `json:"price_currency"`
    Metadata            map[string]any  `json:"metadata"`
}

type ModelPriceFilter struct {
    ID       *int64  `form:"id"`
    Provider *string `form:"provider"`
    Model    *string `form:"model"`
    Mode     *string `form:"mode"`
    Page     *int    `form:"page"`
    PageSize *int    `form:"page_size"`
}

type ModelPriceStorager interface {
    CreateModelPrice(ctx context.Context, param *ModelPriceParam) (int64, error)
    FetchModelPrice(ctx context.Context, filter *ModelPriceFilter) (*ModelPriceParam, error)
    FetchModelPriceList(ctx context.Context, filter *ModelPriceFilter) ([]*ModelPriceParam, int64, error)
    UpdateModelPrice(ctx context.Context, filter *ModelPriceFilter, param *ModelPriceParam) (int64, error)
    DeleteModelPrice(ctx context.Context, filter *ModelPriceFilter) error
    ListByProvider(ctx context.Context, provider string) ([]*ModelPriceParam, error)
    ListProviders(ctx context.Context) ([]string, error)
}

type ModelPriceManager struct {
    txn      itxn.TxnStorager
    storager ModelPriceStorager
}
```

唯一索引：`(provider, model, mode)`。

#### 4.4.2 CRUD 操作

`ModelPriceManager` 提供与通用模型一致的 CRUD：

- `CreateModelPrice`：新增单条记录，校验必填字段与唯一键；`provider` 不强制引用已存在的 Provider；`price_currency` 由系统固定填充为 `RMB`；若携带 `tier_prices`，校验 tier name（**初期只支持 `peak`**）及内部价格键名。
- `FetchModelPrice` / `FetchModelPriceList`：支持按 `id` 或 `(provider, model, mode)` 查询单条；列表查询支持按 `provider`、`mode` 分页过滤。
- `UpdateModelPrice`：支持部分字段更新，未传字段保持原值；按 `id` 或 `(provider, model, mode)` 定位记录；`provider` 更新时不校验存在性。
- `DeleteModelPrice`：按 `id` 或 `(provider, model, mode)` 删除。
- `ListProviders`：返回 `model_prices` 表中所有 `provider` 名称的去重列表，按字典序排列。

校验要点：

- `provider`、`model`、`base_model`、`mode` 必填；
- `provider` 仅作为价格归集标识，不强制引用 `/providers` 中已存在的 Provider；
- `(provider, model, mode)` 不能重复；
- `prices` 必填且至少包含一个价格字段，所有价格字段非负；
- `tier_prices` 非必填；若传入，tier name **初期只支持 `peak`**，内部价格键名须为 `prices` 枚举，所有 tier 价格字段非负；
- `mode` 必须取自预定义枚举；
- `capabilities`、`supported_parameters`、`limits`、`prices`、`tier_prices.<tier>`、`metadata` 的元素/键名必须取自对应枚举（非枚举值不可接受）。

#### 4.4.3 YAML 导入逻辑

`POST /v1/model-prices/import` 通过 `multipart/form-data` 上传 `model-list.yaml`：

```go
// model/imodel_price/import.go
type ImportMode string

const (
    ImportModeReplace ImportMode = "replace" // 全量替换：先清空 model_prices，再写入
    ImportModeMerge   ImportMode = "merge"   // 增量合并：更新已有记录，插入新记录
)

type ImportResult struct {
    ImportedCount int      `json:"imported_count"`
    SkippedCount  int      `json:"skipped_count"`
    Errors        []string `json:"errors"`
}
```

处理流程：

1. 解析 YAML，校验 `version` 与 `default_currency`（必须为 `RMB`）；
2. 校验每条记录的 `(provider, model, mode)` 唯一性；
3. 校验必填字段：`provider`、`model`、`base_model`、`mode`、`prices`；
4. 校验 `prices` 中至少包含一个价格字段且所有价格字段非负；
5. 若记录包含 `tier_prices`：校验 tier name（**初期只支持 `peak`**），内部价格键名须为 `prices` 枚举，所有 tier 价格字段非负；
6. `replace` 模式：清空 `model_prices` 表后批量写入；
7. `merge` 模式：对已有 `(provider, model, mode)` 记录执行更新，新增记录执行插入。

> 说明：v0.4 仅支持人民币（RMB），不扩展美元、欧元等多币种。

#### 4.4.4 按 provider 查询（InnerAPI 导出）

`model/icluster_conf/cluster.go` 中的 `NewBfeClusterConf` 在构造 `AIConf` 时，通过 `ModelPriceStorager.ListByProvider(provider)` 查询该 cluster 对应的全部模型定价：

```go
if cluster.LLMConfig != nil && cluster.LLMConfig.Provider != nil && *cluster.LLMConfig.Provider != "" {
    provider := *cluster.LLMConfig.Provider
    modelPrices, err := modelPriceStorager.ListByProvider(ctx, provider)
    if err != nil {
        // 记录日志，不影响导出
    }

    modelTable := &cluster_conf.ModelTable{
        Currency: "RMB",
        TimeZone: providerRecord.TimeZone, // 为空时 BFE 默认 "Asia/Shanghai"
        Tiers:    providerRecord.Tiers,    // []cluster_conf.PriceTier
        Models:   make([]cluster_conf.ModelPriceEntry, 0, len(modelPrices)),
    }
    for _, mp := range modelPrices {
        modelTable.Models = append(modelTable.Models, cluster_conf.ModelPriceEntry{
            Provider:            mp.Provider,
            Model:               mp.Model,
            BaseModel:           mp.BaseModel,
            Mode:                mp.Mode,
            Capabilities:        mp.Capabilities,
            SupportedParameters: mp.SupportedParameters,
            Limits:              mp.Limits,
            Prices:              mp.Prices,
            TierPrices:          mp.TierPrices,
        })
    }

    aiConf.Provider = provider
    aiConf.ModelTable = modelTable
}
```

若 `llm_config.provider` 为空，`AIConf.ModelTable.Models` 为空列表。

> 说明：`providerRecord.Tiers` 为空时，`ModelTable.Tiers` 为空数组，BFE 按固定价格处理。`TierPrices` 中引用了未在 `Tiers` 中定义的 tier name 时，BFE 不报错，对应价格仅永不命中。

#### 4.4.5 查询所有 price provider 名称

`GET /model-prices/actions/get-providers` 由 `model/imodel_price/Manager.ListProviders(ctx)` 实现：

```go
providers, err := modelPriceManager.ListProviders(ctx)
```

实现要点：

- 调用 `ModelPriceStorager.ListProviders(ctx)` 从 `model_prices` 表按 `provider` 字段聚合去重。
- 返回按字典序排列的 `[]string`。
- 该接口仅用于管理面发现当前已存在价格记录的 provider 名称，便于与 `/providers` 列表对比并补录缺失 provider。

---

### 4.5 `model/iroute_conf`

路由配置模型，包括域名、基础/高级路由规则、产品级路由规则。

```go
// model/iroute_conf/route_rule.go
type RouteRule struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    Description string `json:"description"`
    ProductID int64 `json:"product_id"`
    HostNames string `json:"host_names"`
    Paths string `json:"paths"`
    Expression string `json:"expression"`
    ClusterID int64 `json:"cluster_id"`
}

type ProductRouteRule struct {
    ProductID int64 `json:"product_id"`
    BasicRules []*BasicRouteRule `json:"basic_rules"`
    AdvanceRules []*AdvanceRouteRule `json:"advance_rules"`
    DefaultRule *DefaultRouteRule `json:"default_rule"`
}

type BasicRouteRule struct {
    ID int64 `json:"id"`
    Description string `json:"description"`
    ProductID int64 `json:"product_id"`
    HostNames []string `json:"host_names"`
    Paths []string `json:"paths"`
    ClusterID int64 `json:"cluster_id"`
}

type AdvanceRouteRule struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    Description string `json:"description"`
    ProductID int64 `json:"product_id"`
    Expression string `json:"expression"`
    ClusterID int64 `json:"cluster_id"`
}

type RouteRuleStorager interface {
    FetchProductRule(ctx context.Context, productID int64) (*ProductRouteRule, error)
    UpsertProductRule(ctx context.Context, productID int64, rule *ProductRouteRule) error
    // ... 基础/高级/默认规则 CRUD
}

type RouteRuleManager struct {
    txn itxn.TxnStorager
    storager RouteRuleStorager
}
```

`UpsertProductRule` 采用先删除再批量插入的方式实现全量更新：查询并加锁（`SELECT ... FOR UPDATE`），删除该产品下所有高级/基础规则，再重新插入新规则。

```go
// model/iroute_conf/domain.go
type Domain struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    ProductID int64 `json:"product_id"`
    Type int `json:"type"`
    UsingAdvancedRedirect bool `json:"using_advanced_redirect"`
    UsingAdvancedHsts bool `json:"using_advanced_hsts"`
}

type DomainParam struct {
    Name *string `json:"name"`
    ProductID *int64 `json:"product_id"`
    Type *int `json:"type"`
    UsingAdvancedRedirect *bool `json:"using_advanced_redirect"`
    UsingAdvancedHsts *bool `json:"using_advanced_hsts"`
}

type DomainFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    ProductID *int64 `form:"product_id"`
    Type *int `form:"type"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}
```

---

### 4.6 `model/iai_route`

AI 路由规则模型，负责条件表达式构造与 AI 路由规则管理。

```go
// model/iai_route/ai_route.go
type Rule struct {
    ID int64 `json:"id"`
    Name string `json:"name"`
    Basic string `json:"basic"`
    ProductName string `json:"product_name"`
    Idx int64 `json:"idx"`
    CreateTime int64 `json:"create_time"`
    UpdateTime int64 `json:"update_time"`
}

type BasicInfo struct {
    Name string `json:"name"`
    ProductName string `json:"product_name"`
    Cond string `json:"Cond"`
    Targets []*Target `json:"targets"`
}

type Target struct {
    ClusterName string `json:"ClusterName"`
    Model string `json:"Model"`
    Weight int `json:"Weight"`
}

type AIRouteFilter struct {
    ID *int64 `form:"id"`
    Name *string `form:"name"`
    ProductName *string `form:"product_name"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}

type AIRouteRuleStorager interface {
    CreateAIRouteRule(ctx context.Context, param *Rule) (int64, error)
    FetchAIRouteRule(ctx context.Context, filter *AIRouteFilter) (*Rule, error)
    FetchAIRouteRuleList(ctx context.Context, filter *AIRouteFilter) ([]*Rule, int64, error)
    UpdateAIRouteRule(ctx context.Context, filter *AIRouteFilter, param *Rule) (int64, error)
    DeleteAIRouteRule(ctx context.Context, filter *AIRouteFilter) error
}

type AIRouteRuleManager struct {
    txn itxn.TxnStorager
    storager AIRouteRuleStorager
}
```

---

### 4.7 `model/iprotocol`

协议层模型，负责 TLS 证书上传、校验、默认证书切换。

```go
// model/iprotocol/certificate.go
type Certificate struct {
    ID int64 `json:"id"`
    CertName string `json:"cert_name"`
    Description string `json:"description"`
    IsDefault bool `json:"is_default"`
    ExpiredDate string `json:"expired_date"` // 从证书内容解析，只读
    CertFilePath string `json:"cert_file_path"`
    KeyFilePath string `json:"key_file_path"`
}

type CertificateParam struct {
    CertName *string `json:"cert_name"`
    Description *string `json:"description"`
    IsDefault *bool `json:"is_default"`
    CertFilePath *string `json:"cert_file_path"`
    KeyFilePath *string `json:"key_file_path"`
}

type CertificateFilter struct {
    ID *int64 `form:"id"`
    CertName *string `form:"cert_name"`
    IsDefault *bool `form:"is_default"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}

type CertificateStorager interface {
    CreateCertificate(ctx context.Context, param *CertificateParam) (int64, error)
    FetchCertificate(ctx context.Context, filter *CertificateFilter) (*Certificate, error)
    FetchCertificateList(ctx context.Context, filter *CertificateFilter) ([]*Certificate, int64, error)
    UpdateCertificate(ctx context.Context, filter *CertificateFilter, param *CertificateParam) (int64, error)
    DeleteCertificate(ctx context.Context, filter *CertificateFilter) error
}

type CertificateManager struct {
    txn itxn.TxnStorager
    storager CertificateStorager
}
```

---

### 4.8 `model/iversion_control`

配置版本控制模型，负责配置导出时的 MD5 签名与版本号管理。

```go
// model/iversion_control/version_control.go
type ExportData struct {
    Name string `json:"name"`
    Version string `json:"version"`
    Data interface{} `json:"data"`
}

type VersionControlStorager interface {
    FetchConfigVersion(ctx context.Context, name, dataSign string) (string, error)
    CreateConfigVersion(ctx context.Context, name, dataSign, version string) error
}

type VersionControlManager struct {
    txn itxn.TxnStorager
    storager VersionControlStorager
}

func (m *VersionControlManager) ExportConfig(ctx context.Context, generator ConfigGenerator) (*ExportData, error) {
    // 1. 生成配置数据
    data, err := generator(ctx)
    if err != nil {
        return nil, err
    }

    // 2. 计算 MD5 签名
    dataSign := calculateMD5(data)

    // 3. 查询或生成版本号
    version, err := m.storager.FetchConfigVersion(ctx, data.Name, dataSign)
    if err != nil {
        return nil, err
    }
    if version == "" {
        version = generateVersion()
        if err := m.storager.CreateConfigVersion(ctx, data.Name, dataSign, version); err != nil {
            return nil, err
        }
    }

    data.Version = version
    return data, nil
}
```

---

### 4.9 `model/imods`

BFE 模块配置导出模型，包括 `mod-api-key`、`mod-body-process`、AI 路由模块等。

```go
// model/imods/mod_api_key_rule.go
type APIKeyRuleConfig struct {
    // API-Key 模块配置
}

// model/imods/mod_body_process.go
type BodyProcessConfig struct {
    // body 处理模块配置
}

// model/imods/ai_route_exporter.go
type AIRouteExporter struct {
    // AI 路由模块导出逻辑
}
```

---

### 4.10 `model/quota`、`model/entity` 与 `model/rate_limit_policy`

配额/实体/限流模型：EntityType、Entity 已迁移到 `model/entity`；RateLimitPolicy 已迁移到 `model/rate_limit_policy`；QuotaPlan、QuotaBalance、BalanceSync、QuotaResetScheduler 保留在 `model/quota`。

#### 4.10.1 EntityType 模型（`model/entity`）

```go
// model/entity/entity_type.go
type EntityTypeParam struct {
    ID *int64 `json:"id"`
    TypeName *string `json:"type_name"`
    Description *string `json:"description"`
    Level *int `json:"level"`
    CreateTime *int64 `json:"create_time"`
}

type EntityTypeFilter struct {
    TypeName *string `form:"type_name"`
    Level *int `form:"level"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}

type EntityTypeStorager interface {
    CreateEntityType(ctx context.Context, param *EntityTypeParam) (int64, error)
    FetchEntityType(ctx context.Context, filter *EntityTypeFilter) (*EntityTypeParam, error)
    FetchEntityTypeList(ctx context.Context, filter *EntityTypeFilter) ([]*EntityTypeParam, int64, error)
    UpdateEntityType(ctx context.Context, filter *EntityTypeFilter, param *EntityTypeParam) (int64, error)
    DeleteEntityType(ctx context.Context, filter *EntityTypeFilter) error
}

type EntityTypeManager struct {
    txn itxn.TxnStorager
    storager EntityTypeStorager
}

func (m *EntityTypeManager) CreateEntityType(ctx context.Context, param *EntityTypeParam) (int64, error) {
    if param.TypeName == nil || *param.TypeName == "" {
        return 0, xerror.WrapParamErrorWithMsg("type_name is required")
    }

    existing, err := m.storager.FetchEntityType(ctx, &EntityTypeFilter{TypeName: param.TypeName})
    if err != nil {
        return 0, err
    }
    if existing != nil {
        return 0, xerror.WrapRecordExisted("Entity-Type")
    }

    return m.storager.CreateEntityType(ctx, param)
}
```

删除 EntityType 前会检查是否被 Entity 引用。

#### 4.10.2 Entity 模型（`model/entity`）

```go
// model/entity/entity.go
import "github.com/rainway-ai-gateway/ai-gateway-api/model/shared"

type EntityParam struct {
    InnerID *int64 `json:"inner_id"`
    EntityID *string `json:"id"`
    Name *string `json:"name"`
    Type *string `json:"type"`
    ParentID *string `json:"parent_id"`
    AllowModels []string `json:"allow_models"`
    BlockModels []string `json:"block_models"`
    QuotaPlanID *int64 `json:"quota_plan_id"`
    RateLimitPolicyID *int64 `json:"rate_limit_policy_id"`
    RouteRulesID *int64 `json:"route_rules_id"`
    CreateTime *int64 `json:"create_time,omitempty"`
    UpdateTime *int64 `json:"update_time,omitempty"`

    QuotaPlan *shared.QuotaPlanParam `json:"quota_plan,omitempty"`
    RateLimitPolicy *shared.RateLimitPolicyParam `json:"rate_limit_policy,omitempty"`
    RouteRules *shared.RouteRulesParam `json:"route_rules,omitempty"`
}

type EntityFilter struct {
    EntityID *string `form:"id"`
    Name *string `form:"name"`
    Type *string `form:"type"`
    ParentID *string `form:"parent_id"`
    QuotaPlanID *int64 `form:"quota_plan_id"`
    RouteRulesID *int64 `form:"route_rules_id"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
}

type EntityManager struct {
    txn                     itxn.TxnStorager
    storager                EntityStorager
    entityTypeStorager      EntityTypeStorager
    quotaPlanStorager       shared.QuotaPlanStorager
    rateLimitPolicyStorager shared.RateLimitPolicyStorager
    routeRulesStorager      shared.RouteRulesStorager
    quotaBalanceStorager    shared.QuotaBalanceStorager
    quotaCache              quotacache.QuotaCache
}

func (m *EntityManager) CreateEntity(ctx context.Context, param *EntityParam) error {
    return m.txn.AtomExecute(ctx, func(ctx context.Context) error {
        // 1. 参数校验
        if err := m.validateEntity(param); err != nil {
            return err
        }

        // 2. 验证 Entity-Type 存在
        entityType, _ := m.entityTypeStorager.FetchEntityType(ctx, &EntityTypeFilter{TypeName: param.Type})
        if entityType == nil {
            return xerror.WrapParamErrorWithMsg("entity type not found")
        }

        // 3. 验证父 Entity（如果有）
        if param.ParentID != nil && *param.ParentID != "" {
            parent, _ := m.storager.FetchEntity(ctx, &EntityFilter{EntityID: param.ParentID})
            if parent == nil {
                return xerror.WrapParamErrorWithMsg("parent entity not found")
            }
            parentType, _ := m.entityTypeStorager.FetchEntityType(ctx, &EntityTypeFilter{TypeName: parent.Type})
            if parentType.Level >= entityType.Level {
                return xerror.WrapParamErrorWithMsg("parent level must be less than child level")
            }
        }

        // 4. 创建 QuotaPlan（如果有）
        var quotaPlanID *int64
        if param.QuotaPlan != nil {
            id, err := m.quotaPlanStorager.CreateQuotaPlan(ctx, param.QuotaPlan)
            if err != nil {
                return err
            }
            quotaPlanID = &id
        }

        // 5. 创建 RateLimitPolicy（如果有）
        var rateLimitPolicyID *int64
        if param.RateLimitPolicy != nil {
            id, err := m.rateLimitPolicyStorager.CreateRateLimitPolicy(ctx, param.RateLimitPolicy)
            if err != nil {
                return err
            }
            rateLimitPolicyID = &id
        }

        // 6. 创建 RouteRules（如果有）
        var routeRulesID *int64
        if param.RouteRules != nil {
            id, err := m.routeRulesStorager.CreateRouteRules(ctx, shared.RouteRulesTypeEntity, param.EntityID, param.RouteRules)
            if err != nil {
                return err
            }
            routeRulesID = &id
        }

        // 7. 创建 Entity
        param.QuotaPlanID = quotaPlanID
        param.RateLimitPolicyID = rateLimitPolicyID
        param.RouteRulesID = routeRulesID
        _, err := m.storager.CreateEntity(ctx, param)
        return err
    })
}
```

`EntityManager.DeleteEntity` 会检查子 Entity 是否存在，然后级联删除 `quota_balance`、`quota_plan`、`rate_limit_policy`、`route_rules`，最后删除 `entity`。

#### 4.10.3 QuotaPlan 模型

```go
// model/quota/quota_plan.go
type QuotaPlanParam struct {
    ID *int64 `json:"id"`
    Unlimited *bool `json:"unlimited"`
    PassWhenNoEnoughQuota *bool `json:"pass_when_no_enough_quota"`
    Quota *float64 `json:"quota"`       // 由 *int64 扩展为 *float64，支持 RMB 小数
    Unit *string `json:"unit"`           // total_token / RMB
    ResetPeriod *string `json:"reset_period"`
    Balance *QuotaBalanceParam `json:"balance,omitempty"`
}

// QuotaPlan 校验要点：
// - unit 仅允许 total_token 或 RMB；
// - unit = "total_token" 时，quota 必须 >= 0 且为整数；
// - unit = "RMB" 时，quota 必须 >= 0，小数位不超过 8 位（内部定点精度 1e-8 元）；
// - unlimited = true 时，quota 仍可为 0，balance 不返回或返回 0。

type QuotaPlanFilter struct {
    ID *int64 `form:"id"`
}

type QuotaPlanStorager interface {
    CreateQuotaPlan(ctx context.Context, param *QuotaPlanParam) (int64, error)
    FetchQuotaPlan(ctx context.Context, filter *QuotaPlanFilter) (*QuotaPlanParam, error)
    UpdateQuotaPlan(ctx context.Context, filter *QuotaPlanFilter, param *QuotaPlanParam) (int64, error)
    DeleteQuotaPlan(ctx context.Context, filter *QuotaPlanFilter) error
}

type QuotaPlanManager struct {
    txn itxn.TxnStorager
    storager QuotaPlanStorager
    balanceStorager QuotaBalanceStorager
}

func (m *QuotaPlanManager) CreateQuotaPlan(ctx context.Context, param *QuotaPlanParam) (int64, error) {
    var planID int64
    err := m.txn.AtomExecute(ctx, func(ctx context.Context) error {
        id, err := m.storager.CreateQuotaPlan(ctx, param)
        if err != nil {
            return err
        }
        planID = id

        if param.Unlimited != nil && !*param.Unlimited {
            balance := &QuotaBalanceParam{
                QuotaPlanID: &id,
                Used: lib.PFloat64(0),
                Remaining: param.Quota,
            }
            _, err = m.balanceStorager.CreateQuotaBalance(ctx, balance)
            if err != nil {
                return err
            }
        }
        return nil
    })
    return planID, err
}
```

`ResetBalance` 用于重置配额余额：

```go
func (m *QuotaPlanManager) ResetBalance(ctx context.Context, planID int64, newQuota *float64, updateLastResetAt bool) error {
    return m.txn.AtomExecute(ctx, func(ctx context.Context) error {
        plan, err := m.storager.FetchQuotaPlan(ctx, &QuotaPlanFilter{ID: &planID})
        if err != nil {
            return err
        }
        if plan == nil {
            return fmt.Errorf("quota_plan not found")
        }

        if plan.Unlimited != nil && *plan.Unlimited {
            return fmt.Errorf("cannot reset balance for unlimited quota")
        }

        resetQuota := plan.Quota
        if newQuota != nil {
            resetQuota = newQuota
            _, err = m.storager.UpdateQuotaPlan(ctx, &QuotaPlanFilter{ID: &planID}, &QuotaPlanParam{
                Quota: resetQuota,
            })
            if err != nil {
                return err
            }
        }

        balance, err := m.balanceStorager.FetchQuotaBalance(ctx, &QuotaBalanceFilter{QuotaPlanID: &planID})
        if err != nil {
            return err
        }

        updateParam := &QuotaBalanceParam{
            Used: lib.PFloat64(0),
            Remaining: resetQuota,
        }
        now := time.Now()
        if updateLastResetAt {
            updateParam.LastResetAt = &now
        }

        if balance == nil {
            _, err = m.balanceStorager.CreateQuotaBalance(ctx, &QuotaBalanceParam{
                QuotaPlanID: &planID,
                Used: lib.PFloat64(0),
                Remaining: resetQuota,
                LastResetAt: &now,
            })
        } else {
            _, err = m.balanceStorager.UpdateQuotaBalance(ctx, &QuotaBalanceFilter{ID: balance.ID}, updateParam)
        }
        return err
    })
}
```

`updateLastResetAt=true` 用于定时调度；`false` 用于手动重置接口，避免影响调度判断。

#### 4.10.4 QuotaBalance 模型

```go
// model/quota/quota_balance.go
type QuotaBalanceParam struct {
    ID *int64 `json:"id"`
    QuotaPlanID *int64 `json:"quota_plan_id"`
    Used *float64 `json:"used"`           // 由 *int64 扩展为 *float64
    Remaining *float64 `json:"remaining"` // 由 *int64 扩展为 *float64
    LastResetAt *time.Time `json:"last_reset_at"`
}

type QuotaBalanceFilter struct {
    ID *int64 `form:"id"`
    QuotaPlanID *int64 `form:"quota_plan_id"`
}

type QuotaBalanceStorager interface {
    CreateQuotaBalance(ctx context.Context, param *QuotaBalanceParam) (int64, error)
    FetchQuotaBalance(ctx context.Context, filter *QuotaBalanceFilter) (*QuotaBalanceParam, error)
    UpdateQuotaBalance(ctx context.Context, filter *QuotaBalanceFilter, param *QuotaBalanceParam) (int64, error)
    DeleteQuotaBalance(ctx context.Context, filter *QuotaBalanceFilter) error
}
```

#### 4.10.5 RateLimitPolicy 模型（`model/rate_limit_policy`）

```go
// model/rate_limit_policy/rate_limit_policy.go
type RateLimitPolicyParam struct {
    ID *int64 `json:"id"`
    Enabled *bool `json:"enabled"`
    MaxConcurrency *int `json:"max_concurrency"`
    TPMConfigs []*TPMConfigParam `json:"tpm_configs"`
    RPMConfigs []*RPMConfigParam `json:"rpm_configs"`
}

type TPMConfigParam struct {
    Name *string `json:"name"`
    Model *string `json:"model"`
    WindowMinutes *int `json:"window_minutes"`
    MaxTokens *int64 `json:"max_tokens"`
    StepMinutes *int `json:"step_minutes"`
}

type RPMConfigParam struct {
    Name *string `json:"name"`
    Model *string `json:"model"`
    WindowMinutes *int `json:"window_minutes"`
    MaxRequests *int64 `json:"max_requests"`
}

type RateLimitPolicyStorager interface {
    CreateRateLimitPolicy(ctx context.Context, param *RateLimitPolicyParam) (int64, error)
    FetchRateLimitPolicy(ctx context.Context, filter *RateLimitPolicyFilter) (*RateLimitPolicyParam, error)
    UpdateRateLimitPolicy(ctx context.Context, filter *RateLimitPolicyFilter, param *RateLimitPolicyParam) (int64, error)
    DeleteRateLimitPolicy(ctx context.Context, filter *RateLimitPolicyFilter) error
}

type RateLimitPolicyManager struct {
    txn itxn.TxnStorager
    storager RateLimitPolicyStorager
    apiKeyStorager api_key.APIKeyStorager
    entityStorager entity.EntityStorager
    versionControlManager *iversion_control.VersionControlManager
}
```

TPM、RPM 配置以 JSON 字符串形式直接存储在 `rate_limit_policies` 表的 `tpm_configs` / `rpm_configs` 字段中，不存在独立的子配置 Storager。

#### 4.10.6 BalanceSync 与 Scheduler

```go
// model/quota/balance_sync.go
type BalanceSyncManager struct {
    txn             itxn.TxnStorager
    apiKeyStorager  api_key.APIKeyStorager
    balanceStorager QuotaBalanceStorager
    planStorager    QuotaPlanStorager
    entityStorager  entity.EntityStorager
    quotaCache      quotacache.QuotaCache
    clock           Clock   // 可注入的时钟，用于测试
}

func (m *BalanceSyncManager) SyncAllBalances(ctx context.Context) error {
    // 从 Redis 读取每个 API-Key/Entity 的剩余配额，反写 quota_balance 表
}

func (m *BalanceSyncManager) ResetExpiredBalances(ctx context.Context) error {
    // 通过 m.clock.Now() 获取当前时间
    // 按自然周/自然月判断，重置到期计划的 Redis 值与数据库余额
}

// model/quota/scheduler.go
// BalanceSyncer 是调度器依赖的抽象，*BalanceSyncManager 实现该接口。
type BalanceSyncer interface {
    SyncAllBalances(ctx context.Context) error
    ResetExpiredBalances(ctx context.Context) error
}

type QuotaResetScheduler struct {
    txn             itxn.TxnStorager
    balanceSyncMgr  BalanceSyncer
}

func (s *QuotaResetScheduler) Start() {
    // 每分钟执行一次余额同步与到期重置
}
```

---

---

### 4.11 `model/quotacache`

`model/quotacache` 是本次新增的独立包，用于封装 API-Key / Entity 的实时配额 Redis 缓存操作。该包的引入是为了将原本散落在 `endpoints/openapi_v1` 中的 Redis 操作下沉到 model 层，使端点层只负责 HTTP 参数解析与 model 调用。

#### 4.11.1 接口定义

```go
// model/quotacache/quota_cache.go
package quotacache

import "context"

// QuotaCache 封装 API-Key / Entity 的实时配额缓存操作。
type QuotaCache interface {
    // GetRemaining 获取指定对象的实时剩余配额（已按 unit 转换为浮点数）。
    GetRemaining(ctx context.Context, key string, unit *string) (float64, error)

    // SetRemaining 将指定对象的 Redis 剩余量设置为 target。
    SetRemaining(ctx context.Context, key string, quota *float64, unit *string) error

    // ResetToQuota 将 Redis 剩余量重置为 quota 总量。
    ResetToQuota(ctx context.Context, key string, quota *float64, unit *string) error
}
```

#### 4.11.2 Redis 实现

```go
// model/quotacache/quota_cache_redis.go
package quotacache

import (
    "context"
    "strings"

    "github.com/bfenetworks/go-lib/quota"
    "github.com/bfenetworks/bfe/bfe_util/redis_client"
    "github.com/rainway-ai-gateway/ai-gateway-api/stateful"
)

type redisQuotaCache struct {
    client redis_client.Client
}

func NewRedisQuotaCache(client redis_client.Client) QuotaCache {
    return &redisQuotaCache{client: client}
}

func (c *redisQuotaCache) GetRemaining(ctx context.Context, key string, unit *string) (float64, error) {
    if c.client == nil {
        return 0, nil
    }
    redisKey := stateful.AIUsedQuotaKey(key)
    remain, err := c.client.GetInt64(redisKey)
    if err != nil {
        if strings.Contains(err.Error(), "redigo: nil returned") {
            return 0, nil
        }
        return 0, err
    }
    return quota.PtrFromRedisValue(remain, unit), nil
}

func (c *redisQuotaCache) SetRemaining(ctx context.Context, key string, quotaValue *float64, unit *string) error {
    if c.client == nil || quotaValue == nil {
        return nil
    }
    redisKey := stateful.AIUsedQuotaKey(key)
    targetValue := quota.PtrToRedisValue(quotaValue, unit)
    currentValue, err := c.client.GetInt64(redisKey)
    if err != nil {
        if !strings.Contains(err.Error(), "redigo: nil returned") {
            return err
        }
        currentValue = 0
    }
    delta := targetValue - currentValue
    _, err = c.client.IncrBy(redisKey, delta)
    return err
}

func (c *redisQuotaCache) ResetToQuota(ctx context.Context, key string, quotaValue *float64, unit *string) error {
    return c.SetRemaining(ctx, key, quotaValue, unit)
}
```

#### 4.11.3 使用方

`APIKeyManager`、`EntityManager`、`BalanceSyncManager` 通过构造函数注入 `QuotaCache`：

```go
type APIKeyManager struct {
    txn             itxn.TxnStorager
    storager        APIKeyStorager
    quotaPlanStorager shared.QuotaPlanStorager
    quotaCache      quotacache.QuotaCache  // 新增
}
```

在 `CreateAPIKey` / `UpdateAPIKey` / `DeleteAPIKey` 等流程中，DB 事务成功后再调用 `quotaCache.SetRemaining` 或 `quotaCache.ResetToQuota` 维护 Redis；`GetRemainingQuota` 则调用 `quotaCache.GetRemaining`。

---

### 4.12 `model/shared` 与 `model/route_rules`

`model/shared` 提供跨包共享类型与 Storager 接口；`model/route_rules` 提供 Global/Entity/API-Key 路由规则业务逻辑。两者供 API Key、Entity、Global 路由规则等共用。

```go
// model/shared/types.go
type QuotaPlanParam struct {
    ID *int64 `json:"id"`
    Unlimited *bool `json:"unlimited"`
    PassWhenNoEnoughQuota *bool `json:"pass_when_no_enough_quota"`
    Quota *float64 `json:"quota"`           // 支持 total_token（整数）与 RMB（最多 8 位小数）
    Unit *string `json:"unit"`               // total_token / RMB
    ResetPeriod *string `json:"reset_period"`
    Balance *QuotaBalanceParam `json:"balance,omitempty"`
}

type QuotaBalanceParam struct {
    ID *int64 `json:"id"`
    QuotaPlanID *int64 `json:"quota_plan_id"`
    Used *float64 `json:"used"`              // 内部最多 8 位小数，对外统一按 4 位小数展示
    Remaining *float64 `json:"remaining"`    // 内部最多 8 位小数，对外统一按 4 位小数展示
    LastResetAt *time.Time `json:"last_reset_at"`
}

type RateLimitPolicyParam struct {
    ID *int64 `json:"id"`
    Enabled *bool `json:"enabled"`
    MaxConcurrency *int `json:"max_concurrency"`
    TPMConfigs []*TPMConfigParam `json:"tpm_configs"`
    RPMConfigs []*RPMConfigParam `json:"rpm_configs"`
}

type EntitySummary struct {
    EntityID *string `json:"id"`
    Name *string `json:"name"`
    Type *string `json:"type"`
    ParentID *string `json:"parent_id,omitempty"`
}

type BalanceSummary struct {
    Used *float64 `json:"used"`          // 由 *int64 扩展为 *float64
    Remaining *float64 `json:"remaining"` // 由 *int64 扩展为 *float64
}
```

```go
// model/shared/types.go
const (
    RouteRulesTypeAPIKey = "apikey"
    RouteRulesTypeEntity = "entity"
    RouteRulesTypeGlobal = "global"
)

type RouteRulesParam struct {
    ID *int64 `json:"id"`
    Enabled *bool `json:"enabled"`
    Rules []*AiRouteRuleParam `json:"rules"`
}

type AiRouteRuleParam struct {
    Name *string `json:"name"`
    Cond *string `json:"Cond"`
    Targets []*AiRouteTargetParam `json:"targets"`
    Fallbacks []*AiRouteFallbackParam `json:"fallbacks"`
}

type AiRouteTargetParam struct {
    ClusterName *string `json:"ClusterName"`
    Model *string `json:"Model"`
    Weight *int `json:"Weight"`
}

type AiRouteFallbackParam struct {
    ClusterName *string `json:"ClusterName"`
    Model *string `json:"Model"`
}

type RouteRulesStorager interface {
    CreateRouteRules(ctx context.Context, ruleType string, owner *string, param *RouteRulesParam) (int64, error)
    FetchRouteRules(ctx context.Context, ruleType string, owner *string) (*RouteRulesParam, error)
    FetchRouteRulesList(ctx context.Context, filter *RouteRulesFilter) ([]*RouteTableParam, int64, error)
    UpdateRouteRules(ctx context.Context, id int64, param *RouteRulesParam) (int64, error)
    DeleteRouteRules(ctx context.Context, id int64) error
    FetchRouteRulesByID(ctx context.Context, id int64) (*RouteRulesParam, error)
}

type RouteTableParam struct {
    Type *string `json:"type"`
    Owner *string `json:"owner"`
    Enabled *bool `json:"enabled"`
}

type RouteRulesFilter struct {
    Type *string `form:"type"`
    Owner *string `form:"owner"`
    Enabled *bool `form:"enabled"`
    Page *int `form:"page"`
    PageSize *int `form:"page_size"`
    SortBy *string `form:"sort_by"`
    SortOrder *string `form:"sort_order"`
}

#### `model/route_rules/route_rules.go`

```go
// model/route_rules/route_rules.go
type RouteRulesManager struct {
    txn itxn.TxnStorager
    storager RouteRulesStorager
}
```

`RouteRulesManager` 其他方法：
- `EnsureGlobalRouteRules(ctx)`：系统启动时调用，检查 `route_rules` 表中是否存在 `type=global, owner=global` 的记录；不存在则创建默认记录（`enabled=false`、`rules=[]`）。

`RouteRulesManager.validateRouteRules` 校验规则：
- 同一规则列表内 `name` 唯一。
- 每个规则的 `Cond` 非空。
- 每个规则至少有一个 target。
- 每个规则 `targets` 的 `Weight` 之和等于 100。
- `fallbacks` 中每个元素的 `ClusterName` 非空。

---

### 4.13 `model/itxn`

事务抽象接口。

```go
// model/itxn/txn.go
package itxn

import "context"

type TxnStorager interface {
    AtomExecute(ctx context.Context, do func(context.Context) error) error
}
```

---

### 4.14 Provider 与模型访问协议

Provider 独立为 `/providers` 资源后，原 `/model-provider-types` 与 `/tools/get-models-from-provider` 能力被整合：模型访问协议枚举作为 `providers.model_protocols` 的合法性约束在 `providers.md` 中定义；模型发现通过独立的无状态工具接口 `/providers/tools/discover-models` 触发。

- **`/providers`**：独立资源，集中管理模型提供方的接入能力（`model_endpoint`、`models`、`keys`、`instance_pool`、`model_protocols`）。
- **`/providers/tools/discover-models`**：接收 `model_protocol`、`schema`、`addr`、`port`、`uri`、`apikey` 参数，按 `model_protocol` 生成认证头并选择响应解析器，返回模型名列表；不读写 Provider 资源。

Provider 管理不影响 InnerAPI 导出配置的结构，仅改变 `AIConf` 内部生成方式：实例池与 key 明文从 Provider 获取，Cluster 只保留转发策略；同时把 Provider 的 `model_protocols` 透传为 `AIConf.ModelProtocols`。

---

## 5. 典型业务流程

### 5.1 API Key 创建流程

1. Endpoint 生成 `api-key-{seq}` 形式的 ID 与 `{productName}-{random}` 形式的 Key。
2. 填充默认 `QuotaPlan`、`RateLimitPolicy`、`RouteRules`。
3. `APIKeyManager.CreateAPIKey` 在事务中：
   - 校验同产品下 ID 唯一。
   - 校验 `entity_id` 存在。
   - 创建 `QuotaPlan`、可选 `QuotaBalance`。
   - 创建 `RateLimitPolicy`。
   - 创建 `RouteRules`。
   - 插入 `api_key` 记录。

### 5.2 API Key 查询流程

1. `APIKeyManager.FetchAPIKey(ctx, filter)` 在事务中调用 `storager.FetchAPIKeyList`。
2. `populateAssociatedData` 依次填充：
   - `QuotaPlan`（含 `QuotaBalance`）
   - `RateLimitPolicy`
   - `RouteRules`
   - `Entity`

### 5.3 API Key 删除流程

1. 查询待删除记录。
2. 级联删除 `quota_balance`、`quota_plan`、`rate_limit_policy`、`route_rules`。
3. 删除 `api_key` 记录。

### 5.4 Entity 创建流程

与 API Key 类似，但额外校验：
- `EntityType` 存在。
- 父 Entity 存在且父层级高于子层级。

### 5.5 配额余额重置流程

1. `QuotaPlanManager.ResetBalance(ctx, planID, newQuota, updateLastResetAt)`。
2. 不支持 `unlimited` 计划重置。
3. 若传入 `newQuota`，同时更新 `quota_plan.quota`。
4. 查询或创建 `quota_balance`，设置 `used=0`、`remaining=quota`。
5. 根据 `updateLastResetAt` 决定是否更新 `last_reset_at`。

### 5.6 路由规则全量更新流程

1. `RouteRulesManager.UpdateRouteRules(ctx, ruleType, owner, param)`。
2. 校验规则参数。
3. 查询现有规则；不存在则创建，存在则按 ID 全量更新。

### 5.7 产品级路由规则 Upsert 流程

1. `RouteRuleManager.UpsertProductRule(ctx, productID, rule)`。
2. 事务中查询并加锁（`SELECT ... FOR UPDATE`）。
3. 删除该产品下所有基础/高级规则。
4. 重新批量插入新规则。

---

## 6. 文件速查

### 6.1 `model/` 关键文件

| 子包 | 文件 | 说明 |
|------|------|------|
| `api_key` | `api_key.go` | API-Key 模型与业务逻辑 |
| `entity` | `entity_type.go`、`entity_type_manager.go`、`entity.go`、`entity_manager.go` | Entity / Entity-Type 模型与业务逻辑 |
| `ibasic` | `product.go`、`bfe_cluster.go`、`extra_file.go` | 基础资源模型 |
| `iauth` | `authentication.go`、`authorization.go`、`features.go` | 认证授权模型 |
| `icluster_conf` | `cluster.go`、`sub_cluster.go`、`pool.go`、`model_price.go`、`exporter.go` | 集群/子集群/实例池模型；Cluster 只保留转发策略，后端能力通过 provider 引用获取 |
| `iprovider` | `provider.go`、`discover.go` | Provider 模型与模型发现 |
| `imodel_price` | `model_price.go`、`import.go` | 模型定价管理；`provider` 须引用已存在的 Provider |
| `iroute_conf` | `route_rule.go`、`domain.go` | 路由与域名模型 |
| `iai_route` | `ai_route.go`、`checker.go`、`common.go` | AI 路由规则模型 |
| `iprotocol` | `certificate.go` | 证书模型 |
| `iversion_control` | `version_control.go` | 配置版本控制 |
| `imods` | `mod_api_key_rule.go`、`mod_body_process.go`、`ai_route_exporter.go` | BFE 模块配置导出 |
| `quota` | `quota_plan.go`、`quota_plan_manager.go`、`quota_balance.go`、`balance_sync.go`、`scheduler.go` | 配额计划/余额/同步/调度 |
| `rate_limit_policy` | `rate_limit_policy.go`、`rate_limit_policy_manager.go` | 限流策略模型与业务逻辑 |
| `shared` | `types.go`、`route_rules.go` | 跨包共享类型 |
| `route_rules` | `route_rules.go` | Global/Entity/API-Key 路由规则业务逻辑 |
| `itxn` | `txn.go` | 事务抽象 |

### 6.2 `storage/rdb/` 关键文件

| 子包 | 文件 | 说明 |
|------|------|------|
| `basic` | `product.go`、`bfe_cluster.go`、`extra_file.go` | 基础资源存储 |
| `auth` | `authentication.go`、`authorization.go` | 认证授权存储 |
| `api_key` | `api_key.go` | API-Key 存储 |
| `cluster_conf` | `cluster.go`、`sub_cluster.go`、`pool.go`、`model_price.go` | 集群/子集群/实例池存储；实例池数据来自 Provider |
| `provider` | `provider.go` | Provider 存储 |
| `entity` | `entity.go`、`entity_type.go` | Entity / Entity-Type 存储 |
| `route_conf` | `route_rule.go`、`domain.go` | 路由与域名存储 |
| `route_rules` | `route_rules.go` | Global/Entity/API-Key 路由规则存储 |
| `ai_route` | `ai_route.go` | AI 路由规则存储 |
| `protocol` | `certificate.go` | 证书存储 |
| `quota` | `quota_plan.go`、`quota_balance.go` | 配额计划/余额存储 |
| `rate_limit_policy` | `rate_limit_policy.go` | 限流策略存储 |
| `txn` | `txn.go` | 事务实现 |
| `internal/dao` | `internal/curd.go`、`internal/builder.go`、各 `table_*.go` | 通用 DAO 与表映射 |

### 6.3 依赖注入与数据库上下文

| 文件 | 说明 |
|------|------|
| `stateful/container/rdb/components.go` | 各 Manager / Storager 单例初始化 |
| `stateful/config_database.go` | 数据库连接初始化 |
| `lib/xdb.go` | `DBContext`、事务执行、分页工具 |
