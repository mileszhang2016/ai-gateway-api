# Rainway AI Gateway - 报表库设计文档

> 本文档描述**报表库**（如 `bfe_report`）的表结构设计。报表库与控制面库（见 [数据库设计文档.md](./数据库设计文档.md)，34 张控制面表）**分离部署、独立授权**，由 log-reader `mod_log_mysql` 插件写入、报表查询 API（`/report/*`，见 [details/报表查询模块.md](./details/报表查询模块.md)）读取。模块接口定义见 [api-define/OpenAPI接口定义/report.md](../api-define/OpenAPI接口定义/report.md)。

## 1. 概述

| 项目 | 说明 |
|------|------|
| 库定位 | AI 访问日志的报表存储库，独立于 ai-gateway-api 控制面库 |
| 数据生产者 | log-reader `mod_log_mysql` 插件（批量幂等写入明细表）；ai-gateway-api 分钟聚合 JOB（明细 → 聚合表） |
| 数据消费者 | ai-gateway-api 报表查询 API（时序/排行/分布读聚合表，明细查询读明细表） |
| 部署形态 | 轻量形态用 MySQL 8.0（推荐）/5.7.8+；标准形态用 Doris，表结构权威在 `ai-gateway-observability/doris/sqls/`，本文档 §3/§4 的列设计与其对齐 |
| DDL 维护 | 统一在本仓库 `db_ddl_report_mysql.sql`，随本仓库发布，部署流程执行（schema-first：先 DDL 后启用插件）；写入插件不自动建表 |

### 1.1 表清单

| 表名 | 作用 | 数据来源 | 读取方 |
|------|------|----------|--------|
| `bfe_ai_request_log` | 明细表，AI 请求日志宽表（89 列，与 Doris 明细表同名同列） | log-reader 插件 `INSERT ... ON DUPLICATE KEY UPDATE` 幂等覆盖写 | `/report/logs` 明细分页 |
| `bfe_ai_metrics_1m` | 分钟聚合表（37 维 + 24 指标，与 Doris 同名表对齐） | api-gateway-api 聚合 JOB（`DELETE` 窗口 + `INSERT SELECT` 事务，窗口重放幂等） | `/report/overview` / `timeseries` / `rankings` / `distribution` |

数据流：`bfe_ai_request_log`（明细）→ 聚合 JOB 按「上一整分钟」窗口汇总 → `bfe_ai_metrics_1m`（聚合）。时序/排行/分布类查询**只读聚合表**，不扫明细大表。

## 2. 通用约定

- 引擎 InnoDB，字符集 `utf8mb4`；要求 MySQL ≥ 5.7.8（JSON 列），推荐 8.0。
- 两表均按时间列（`log_time` / `ts_min`）**按天 RANGE 分区**；MySQL 无动态分区，分区由 api-gateway-api 分区管理 JOB 维护（提前建 3 天、DROP 超 `RetentionDays` 的分区；非分区形态自动降级 `DELETE ... LIMIT` 分批）。
- **NULL 语义**：写入侧（log-reader）零值规则为「字符串空值 → NULL，数值/布尔零值 → 0」；查询侧过滤空值统一 `IFNULL(col,'') != ''`；聚合 JOB 写入聚合表时维度列 `IFNULL(col,'')` 归一（与 Doris 聚合 JOB 的 COALESCE 语义一致）。
- JSON 列只用于明细页展示，不做查询过滤条件（避免生成列/函数索引的兼容性问题）。

## 3. 明细表 `bfe_ai_request_log`

与 Doris 明细表同名同列；差异仅在类型适配：Doris 的 `ARRAY<STRUCT<...>>` 列用 `JSON` 存储（7 个：`req_headers`、`res_headers`、`ai_route_rule_hits`、`ai_cluster_key_names`、`ai_rate_limit_hits`、`ai_auth_reject_quota_plans`、`ai_auth_hit_quota_plans`），`log_time` 用 `DATETIME`。

### 3.1 DDL

```sql
-- 要求 MySQL >= 5.7.8（JSON 列），推荐 8.0
CREATE TABLE IF NOT EXISTS bfe_ai_request_log (
    hostid                  VARCHAR(256)  NOT NULL DEFAULT '',
    log_time                DATETIME      NOT NULL,
    ai_apikey_id            VARCHAR(256)  DEFAULT NULL,
    ai_requested_model      VARCHAR(128)  DEFAULT NULL,
    logid                   BIGINT        DEFAULT NULL,
    product                 VARCHAR(64)   DEFAULT NULL,
    log_tag                 VARCHAR(64)   DEFAULT NULL,
    -- 客户端连接
    client_ip               VARCHAR(64)   DEFAULT NULL,
    client_network          VARCHAR(16)   DEFAULT NULL,
    is_trust_src_ip         TINYINT       DEFAULT NULL,
    req_num                 INT           DEFAULT NULL,
    session_id              BIGINT        DEFAULT NULL,
    bfe_ip                  VARCHAR(64)   DEFAULT NULL,
    sock_src_ip             VARCHAR(64)   DEFAULT NULL,
    vip                     VARCHAR(64)   DEFAULT NULL,
    vip6                    VARCHAR(128)  DEFAULT NULL,
    -- 错误
    err_code                VARCHAR(64)   DEFAULT NULL,
    err_msg                 VARCHAR(512)  DEFAULT NULL,
    -- 请求（长文本列用 TEXT，见 3.2 修正 1）
    proto                   VARCHAR(16)   DEFAULT NULL,
    header_host             VARCHAR(256)  DEFAULT NULL,
    origin_uri              TEXT          DEFAULT NULL,
    final_uri               TEXT          DEFAULT NULL,
    method                  VARCHAR(16)   DEFAULT NULL,
    content_type            VARCHAR(128)  DEFAULT NULL,
    x_forward_for           TEXT          DEFAULT NULL,
    accept_language         VARCHAR(256)  DEFAULT NULL,
    authorization           TEXT          DEFAULT NULL,
    transfer_encoding       VARCHAR(64)   DEFAULT NULL,
    referrer                TEXT          DEFAULT NULL,
    user_agent              TEXT          DEFAULT NULL,
    delegation              VARCHAR(256)  DEFAULT NULL,
    uid                     VARCHAR(256)  DEFAULT NULL,
    cookie                  TEXT          DEFAULT NULL,
    req_headers             JSON          DEFAULT NULL,
    req_header_len          INT           DEFAULT NULL,
    req_body_len            INT           DEFAULT NULL,
    -- 路由
    cluster                 VARCHAR(256)  DEFAULT NULL,
    sub_cluster             VARCHAR(256)  DEFAULT NULL,
    backend_info            VARCHAR(256)  DEFAULT NULL,
    backend_retry           TINYINT       DEFAULT NULL,
    -- 响应
    res_status_code         SMALLINT      DEFAULT NULL,
    res_header_len          INT           DEFAULT NULL,
    res_body_len            INT           DEFAULT NULL,
    res_content_type        VARCHAR(128)  DEFAULT NULL,
    res_location            TEXT          DEFAULT NULL,
    res_transfer_encoding   VARCHAR(64)   DEFAULT NULL,
    res_headers             JSON          DEFAULT NULL,
    -- 耗时（毫秒）
    all_time                INT           DEFAULT NULL,
    read_client_time        INT           DEFAULT NULL,
    cluster_serve_time      INT           DEFAULT NULL,
    backend_serve_time      INT           DEFAULT NULL,
    write_client_time       INT           DEFAULT NULL,
    connect_backend_time    INT           DEFAULT NULL,
    proxy_delay_time        INT           DEFAULT NULL,
    session_offset_time     INT           DEFAULT NULL,
    -- API Key 标签（写入时由 log-reader 从 ai_apikeytags 打平）
    level1Name              VARCHAR(128)  DEFAULT NULL,
    level1                  VARCHAR(128)  DEFAULT NULL,
    level2Name              VARCHAR(128)  DEFAULT NULL,
    level2                  VARCHAR(128)  DEFAULT NULL,
    level3Name              VARCHAR(128)  DEFAULT NULL,
    level3                  VARCHAR(128)  DEFAULT NULL,
    level4Name              VARCHAR(128)  DEFAULT NULL,
    level4                  VARCHAR(128)  DEFAULT NULL,
    level5Name              VARCHAR(128)  DEFAULT NULL,
    level5                  VARCHAR(128)  DEFAULT NULL,
    -- AI 可观测
    ai_target_model         VARCHAR(128)  DEFAULT NULL,
    ai_stream               TINYINT       DEFAULT NULL,
    ai_input_tokens         BIGINT        DEFAULT NULL,
    ai_output_tokens        BIGINT        DEFAULT NULL,
    ai_total_tokens         BIGINT        DEFAULT NULL,
    ai_cache_read_tokens    BIGINT        DEFAULT NULL,
    ai_cache_write_tokens   BIGINT        DEFAULT NULL,
    ai_audio_input_tokens   BIGINT        DEFAULT NULL,
    ai_audio_output_tokens  BIGINT        DEFAULT NULL,
    ai_image_count          BIGINT        DEFAULT NULL,
    ai_ttft_us              BIGINT        DEFAULT NULL,
    ai_tpot_us              BIGINT        DEFAULT NULL,
    ai_provider             VARCHAR(64)   DEFAULT NULL,
    ai_protocol             VARCHAR(64)   DEFAULT NULL,
    ai_mode                 VARCHAR(64)   DEFAULT NULL,
    ai_retry_count          INT           DEFAULT NULL,
    ai_cost_value           BIGINT        DEFAULT NULL,
    ai_cost_currency        VARCHAR(16)   DEFAULT NULL,
    ai_route_rule_hits      JSON          DEFAULT NULL,
    ai_cluster_key_names    JSON          DEFAULT NULL,
    ai_rate_limit_hits      JSON          DEFAULT NULL,
    ai_auth_reject_reason   VARCHAR(256)  DEFAULT NULL,
    ai_auth_reject_quota_plans JSON       DEFAULT NULL,
    ai_auth_hit_quota_plans JSON          DEFAULT NULL,
    -- 幂等键：与 Doris UNIQUE KEY 一致，log-reader 重发/-b 补读安全
    UNIQUE KEY uk_dedup (hostid, log_time, ai_apikey_id, ai_requested_model),
    KEY idx_model_time (ai_target_model, log_time),
    KEY idx_apikey_time (ai_apikey_id, log_time),
    KEY idx_provider_time (ai_provider, log_time),
    KEY idx_host_time (hostid, log_time),
    KEY idx_status_time (res_status_code, log_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
PARTITION BY RANGE (TO_DAYS(log_time)) (
    PARTITION p_init VALUES LESS THAN (TO_DAYS('${INIT_DATE}'))
);
```

### 3.2 设计要点（经 log-reader LR03 集成测试在真实 MySQL 8.4 上验证）

1. **长文本列用 TEXT**：`origin_uri`/`final_uri`/`x_forward_for`/`authorization`/`referrer`/`user_agent`/`cookie`/`res_location` 若按 Doris 的 VARCHAR 长度建列，utf8mb4 下总长度超过 MySQL 65535 字节行上限（Error 1118）。
2. **幂等键中的字符串列允许 NULL**：`ai_apikey_id`/`ai_requested_model` 为 `DEFAULT NULL`——写入侧零值规则把空字符串映射为 NULL（未认证请求即如此），`NOT NULL` 会使这类写入整批失败（Error 1048）。副作用：NULL 不参与唯一键去重（与 Doris UNIQUE KEY 对 NULL 的语义一致），无 Key 日志重发不去重，可接受。`hostid`/`log_time` 由 log-reader 构造保证非空，保持 `NOT NULL`。
3. **唯一键与 Doris UNIQUE KEY 完全一致**：`(hostid, log_time, ai_apikey_id, ai_requested_model)` + `ON DUPLICATE KEY UPDATE` 覆盖写，log-reader 重发、重启 `-b` 补读不产生重复行。唯一键长度 `(256+256+128)*4+5 ≈ 2565` 字节 < 3072 InnoDB 上限（DYNAMIC 行格式），无需前缀索引；唯一键包含分区列 `log_time`（MySQL 分区表约束）。
4. **索引服务于报表查询过滤维度**：模型/API Key/提供商/Host/状态码 + 时间，与 `/report/*` 过滤参数对应。
5. **pb 中已有、本表不含的新字段**（如 `ai_image_input_tokens`、`ai_video_count`）：待 Doris 侧表升级时两侧同步加列（见 §6 演进规则）。

### 3.3 明细表生命周期

按天分区滚动：分区管理 JOB 每 6h 巡检，向前建 3 天分区、`DROP PARTITION` 清理超过 `RetentionDays`（默认 7 天）的历史分区（DROP PARTITION 秒级、无碎片）。**JOB 必须先于数据到达建好分区**，否则对应日期的写入被直接拒绝（Error 1526）；JOB 启动时立即补建一次。

## 4. 聚合表 `bfe_ai_metrics_1m`

维度集合与指标列与 Doris `bfe_ai_metrics_1m` 完全一致（Doris DDL 见 `ai-gateway-observability/doris/sqls/bfe_ai_metrics_1m.sql`）：37 个维度列（`ts_min` 分钟桶 + 16 个基础维度 + 10 个标签打平列 + 限流三元组 + 认证拒绝原因 + 5 个配额计划槽位）+ 24 个 SUM 指标列（请求数、错误数、认证拒绝数、进出/总/缓存/音频/图片 Token、TTFT/TPOT 累计、请求响应字节、限流命中、后端重试、耗时累计、成本累计）。

### 4.1 DDL 骨架

```sql
CREATE TABLE IF NOT EXISTS bfe_ai_metrics_1m (
    ts_min             DATETIME      NOT NULL,
    hostid             VARCHAR(256)  NOT NULL DEFAULT '',
    ai_apikey_id       VARCHAR(128)  NOT NULL DEFAULT '',
    ai_requested_model VARCHAR(128)  NOT NULL DEFAULT '',
    ai_target_model    VARCHAR(128)  NOT NULL DEFAULT '',
    ai_stream          TINYINT       NOT NULL DEFAULT 0,
    product            VARCHAR(64)   NOT NULL DEFAULT '',
    cluster            VARCHAR(64)   NOT NULL DEFAULT '',
    sub_cluster        VARCHAR(64)   NOT NULL DEFAULT '',
    backend_info       VARCHAR(256)  NOT NULL DEFAULT '',
    method             VARCHAR(16)   NOT NULL DEFAULT '',
    res_status_code    SMALLINT      NOT NULL DEFAULT 0,
    err_code           VARCHAR(64)   NOT NULL DEFAULT '',
    header_host        VARCHAR(256)  NOT NULL DEFAULT '',
    ai_provider        VARCHAR(64)   NOT NULL DEFAULT '',
    ai_protocol        VARCHAR(64)   NOT NULL DEFAULT '',
    ai_mode            VARCHAR(64)   NOT NULL DEFAULT '',
    ai_cost_currency   VARCHAR(16)   NOT NULL DEFAULT '',
    level1Name         VARCHAR(128)  NOT NULL DEFAULT '',
    level1             VARCHAR(128)  NOT NULL DEFAULT '',
    /* ... level2~level5 同构（name/value 成对）... */
    rate_limit_policy_id VARCHAR(128) NOT NULL DEFAULT '',
    rate_limit_type    VARCHAR(32)   NOT NULL DEFAULT '',
    rate_limit_rule_name VARCHAR(128) NOT NULL DEFAULT '',
    ai_auth_reject_reason VARCHAR(256) NOT NULL DEFAULT '',
    ai_auth_reject_quota_plans_slot1 VARCHAR(128) NOT NULL DEFAULT '',
    /* ... slot2~slot5 ... */
    -- 聚合指标（24 列，BIGINT）
    request_count      BIGINT        DEFAULT NULL,
    error_count        BIGINT        DEFAULT NULL,
    /* ... 其余 22 指标与 Doris 完全一致 ... */
    KEY idx_ts (ts_min),
    KEY idx_model_ts (ai_target_model, ts_min),
    KEY idx_apikey_ts (ai_apikey_id, ts_min),
    KEY idx_provider_ts (ai_provider, ts_min)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
PARTITION BY RANGE (TO_DAYS(ts_min)) (
    PARTITION p_init VALUES LESS THAN (TO_DAYS('${INIT_DATE}'))
);
```

### 4.2 设计要点

1. **不设唯一键/主键**：37 个维度列（含多个 VARCHAR(256)）无法构成 InnoDB 唯一键（3072 字节上限），且幂等性由聚合 JOB 的「`DELETE` 窗口 + `INSERT SELECT` 事务」保证，不依赖约束；InnoDB 隐式 rowid 作为主键。
2. **维度列全部 `NOT NULL DEFAULT ''`**：聚合 JOB 写入时 `IFNULL(col,'')` 归一，保证 GROUP BY 与过滤谓词行为对齐 Doris；维度 NOT NULL 使每行定长部分可控。
3. **维度列长度取 Doris 定义与明细表的交集**（如 `ai_apikey_id` 聚合表 128 vs 明细表 256，截断风险由聚合 SQL 侧保证口径一致）。
4. 索引围绕 `ts_min` 与排行热维度，与查询 `WHERE ts_min BETWEEN ... GROUP BY <维度>` 模式匹配。

### 4.3 聚合语义

每周期（默认 60s）对上一整分钟窗口：

```
BEGIN
  DELETE FROM bfe_ai_metrics_1m WHERE ts_min = <上一整分钟>;
  INSERT INTO bfe_ai_metrics_1m SELECT DATE_FORMAT(log_time,'%Y-%m-%d %H:%i:00'), ... 
    FROM bfe_ai_request_log
    WHERE log_time >= <上一分钟起点> AND log_time < <本分钟起点>
    GROUP BY ts_min, <全部 37 维度>;
COMMIT
```

窗口重放幂等（重复执行同一周期结果不变）；多副本 api 部署用 `GET_LOCK('report_agg_job', 0)` 抢锁防重。

## 5. 账号与权限

| 账号 | 使用者 | 权限 |
|------|--------|------|
| 写入账号（如 `report`） | log-reader `mod_log_mysql` 插件 | 报表库明细表 INSERT/UPDATE（幂等覆盖需要 UPDATE）；**无 DDL/DELETE** |
| 查询/维护账号（如 `report_rw`） | ai-gateway-api 查询 API + 聚合/分区 JOB | 报表库明细表/聚合表 SELECT、聚合表 INSERT/DELETE、明细表 DELETE（非分区降级清理）、ALTER TABLE（分区管理） |

网络策略只放通报表库端口；两账号与控制面库账号完全隔离，凭据泄露爆炸半径可控。

## 6. 演进与容量

- **列演进规则**：pb 新增字段时，明细表加列与 Doris 侧同步评估；聚合表维度/指标集合与 Doris `bfe_ai_metrics_1m` 同发布同演进（DDL 与聚合 JOB、查询代码同 PR 落地）。
- **容量建议**：MySQL 形态目标量级 ≤ 日均百万级请求；超量建议走 Doris 标准形态（批量 INSERT 压力远小于 Kafka→Doris 对 Doris 的压力，但单表 + 日分区在千万级/日会出现索引写入放大与聚合 JOB 耗时问题）。
- **清理即 DROP PARTITION**：明细与聚合表均按天分区，过期清理秒级完成；非分区形态（部分 RDS）降级 `DELETE ... LIMIT` 分批，由 JOB 自动探测适配。

## 7. 相关文档

- [数据库设计文档.md](./数据库设计文档.md) — 控制面库 34 张表（与本文档互补）
- [details/报表查询模块.md](./details/报表查询模块.md) — 查询 API 模块设计（JOB、双后端、鉴权、装配）
- [api-define/OpenAPI接口定义/report.md](../api-define/OpenAPI接口定义/report.md) — `/report/*` 接口定义
- log-reader 侧：`log-reader/doc/modifications/2026-09-15-add-mod-log-mysql-plugin/design-changes.md`（写入插件与 89 列映射权威）
