# Infinity AI Gateway - 总体设计文档

## 1. 概述

### 1.1 定位

`ai-gateway-api` 是 AI 网关的**控制面核心组件**，负责对外暴露管理面 OpenAPI 与数据面 InnerAPI，完成策略/配置的创建、存储、版本控制和下发。它向上为 Dashboard 和自动化脚本提供管理接口，向下为 BFE 数据面和 Conf Agent 生成可消费的配置快照。

本文档用于帮助读者和大模型快速建立对 `ai-gateway-api` 系统设计的整体认知。关于数据库、存储层、模型层、接口层的详细设计，请分别阅读同目录下的：

- 《数据库设计文档.md》
- 《存储层设计文档.md》
- 《模型层设计文档.md》
- 《接口层设计文档.md》

### 1.2 项目上下文

AI 网关整体由以下核心组件构成：

| 组件 | 角色 | 说明 |
|------|------|------|
| **AI Gateway API** | 控制面 | 即本项目，负责配置管理和配置下发 |
| **Dashboard** | 管理控制台 | Web UI，调用 OpenAPI 完成可视化操作 |
| **BFE** | 数据面 | 流量转发与访问控制 |
| **Conf Agent** | 配置代理 | 从 InnerAPI 拉取最新配置并触发 BFE 热加载 |
| **Service Controller** | 服务发现 | 发现并同步 Kubernetes 后端服务 |

### 1.3 功能范围

`ai-gateway-api` 当前代码实现的功能范围如下：

- **API-Key 管理**：创建、查询、更新、删除 API-Key；支持配额计划、限流策略和路由规则；创建时支持传入外部 `key`，列表/详情中 `quota_plan` 包含 `balance`。
- **Entity 管理**：创建、查询、更新、删除 Entity，支持层级结构、配额/限流和路由规则。
- **Entity-Type 管理**：创建、查询、更新、删除 Entity 类型定义。
- **配额计划管理**：查询和重置配额余额。
- **限流策略管理**：TPM、RPM、并发限制配置。
- **路由规则管理**：API-Key/Entity 级路由规则、Global 路由表、路由表列表查询；AI 路由规则不再通过 `/ai-route-rules` 独立暴露。
- **AI 网关实例池管理（`/alb-pool`）**：获取和更新默认 AI 网关实例池。
- **认证授权管理（`/auth`）**：用户、Session Key、Token；Token Scope 仅保留 `System`/`Support`，移除产品线授权。
- **证书管理（`/certificates`）**：证书创建、列表、详情、设置默认证书、删除；创建/更新时仅需上传证书与密钥内容，`expired_date` 由服务端从证书中解析，不再要求上传文件名。
- **集群管理（`/clusters`）**：集群一键创建、查询、更新、删除；支持 `llm_config`（含 `models`、`model_mappings`、`keys` 多 API-Key 数组、`key_policy` Key 路由策略等）用于 AI 模型转发；集群就绪状态、子集群、调度矩阵等内部概念不再对外暴露。
- **模型提供商类型（`/model-provider-types`）**：查询系统支持的 AI 模型提供商类型。
- **工具接口（`/tools/get-models-from-provider`）**：从指定提供商代理拉取模型列表。
- **配置导出（InnerAPI）**：向 BFE/Conf Agent 导出 TLS/Server/路由、GSLB、证书、附加文件、API-Key、请求体处理、限流策略、AI 路由等配置。

---

## 2. 架构总览

### 2.1 分层架构

系统采用经典的三层架构：

```
┌─────────────────────────────────────────────────────────────┐
│ 接口层 (endpoints) │
│ OpenAPI v1 (/open-api/v1) · InnerAPI v1 (/inner-api/v1) │
├─────────────────────────────────────────────────────────────┤
│ 模型层 (model) │
│ 业务逻辑 · 事务编排 · 参数校验 · 级联操作 │
├─────────────────────────────────────────────────────────────┤
│ 存储层 (storage/rdb) │
│ DAO · Storage 实现 · 关系型数据库读写 │
└─────────────────────────────────────────────────────────────┘
```

| 层次 | 主要职责 | 关键包 |
|------|----------|--------|
| **接口层** | 处理 HTTP 请求、参数绑定与校验、权限校验、调用模型层、返回统一响应 | `endpoints/openapi_v1`、`endpoints/innerapi_v1`、`endpoints/middleware` |
| **模型层** | 封装业务逻辑、事务管理、调用存储层接口；不直接依赖底层数据库 | `model/*` |
| **存储层** | 负责关系型数据库的读写；向上暴露接口，由模型层通过事务抽象调用 | `storage/rdb/*` |

### 2.2 组件交互关系

```
                HTTP Request
                     │
        ┌────────────┴────────────┐
        ▼ ▼
  /open-api/v1 /inner-api/v1
  (管理面 OpenAPI) (数据面 InnerAPI)
        │ │
        └────────────┬────────────┘
                     ▼
           endpoints/middleware
         (Recovery / Logger / CORS /
          Product Probe / User Probe)
                     │
                     ▼
            endpoints/xreq.Endpoint
                     │
                     ▼
              model/* Manager
                     │
                     ▼
         model/itxn.TxnStorager
                     │
        ┌────────────┼────────────┐
        ▼ ▼ ▼
   storage/rdb storage/rdb storage/rdb
   /cluster_conf /quota /route_conf
        │ │ │
        └────────────┼────────────┘
                     ▼
              storage/rdb/internal/dao
                     │
                     ▼
                MySQL / SQLite
```

- **接口层**通过 `stateful/container` 中注册的全局单例 Manager 调用模型层，不直接访问存储层。
- **模型层**只依赖同包或 `model/shared` 中定义的 `XxxStorager` 接口，以及 `model/itxn.TxnStorager` 事务抽象。
- **存储层**实现上述接口，内部通过 `storage/rdb/internal/dao` 操作具体的数据库表。

### 2.3 配置与运行时

- **配置加载**：`main.go` 调用 `stateful.LoadConfig` 加载 TOML 配置文件（默认 `conf/ai_gateway_api.toml`）。
- **数据库初始化**：`Config.InitDB()` 根据配置创建 `sql.DB` 连接池，支持 MySQL 与 SQLite。
- **依赖注入初始化**：`stateful/container/rdb.Init()` 按依赖顺序实例化所有 Storager 与 Manager，并注入 `stateful/container` 全局变量。
- **HTTP 服务启动**：`endpoints.RegisterRouters` 注册 OpenAPI 与 InnerAPI 路由，最终通过 `negroni` + `graceful` 启动 HTTP 服务。

---

## 3. 模块组织

### 3.1 顶层目录结构

```
ai-gateway-api/
├── main.go # 服务入口：配置加载、初始化、启动 HTTP
├── conf/ # 配置文件（TOML、i18n、AI 模板等）
├── data/ # 数据文件
├── docs/ # 用户文档
├── design-docs/ # 设计文档
│ ├── api-define/ # 接口定义文档
│ └── sys-design/ # 系统设计文档（本文档所在目录）
├── endpoints/ # 接口层
│ ├── middleware/ # HTTP 中间件
│ ├── openapi_v1/ # 管理面 OpenAPI v1
│ └── innerapi_v1/ # 数据面 InnerAPI v1
├── lib/ # 公共库（xreq、xerror、xdb 等）
├── model/ # 模型层
│ ├── iauth/ # 认证授权
│ ├── ibasic/ # 基础配置（product、bfe_cluster、extra_file）
│ ├── icluster_conf/ # 集群/API-Key/实例池
│ ├── iroute_conf/ # 路由规则/域名
│ ├── iai_route/ # AI 路由规则
│ ├── iprotocol/ # TLS 证书
│ ├── iversion_control/ # 配置版本控制
│ ├── imods/ # 模块配置导出（mod-api-key、mod-body-process 等）
│ ├── quota/ # Entity/Entity-Type/Quota/RateLimit
│ ├── shared/ # 跨包共享类型与模型
│ └── itxn/ # 事务抽象接口
├── stateful/ # 配置、数据库、日志、依赖注入容器
│ └── container/ # 全局组件容器
├── static/ # 前端静态资源
├── storage/rdb/ # 存储层
│ ├── internal/dao/ # DAO 层
│ ├── auth/ # 认证授权存储实现
│ ├── basic/ # 基础配置存储实现
│ ├── cluster_conf/ # 集群/API-Key/实例池存储实现
│ ├── protocol/ # 证书存储实现
│ ├── quota/ # Quota 相关存储实现
│ ├── route_conf/ # 路由规则存储实现
│ ├── txn/ # 事务实现
│ └── version_control/ # 版本控制存储实现
├── test/ # 测试相关
└── version/ # 版本信息
```

### 3.2 关键包职责

| 包 | 职责 |
|----|------|
| `endpoints` | 根路由注册、全局中间件、静态文件服务 |
| `endpoints/openapi_v1` | 管理面 OpenAPI 路由组织与子包合并 |
| `endpoints/innerapi_v1` | 数据面 InnerAPI 导出接口注册 |
| `endpoints/middleware` | Recovery、Logger、CORS、Product Probe、User Probe |
| `lib/xreq` | Endpoint 抽象、请求参数绑定、响应渲染 |
| `lib/xdb` | `DBContext`、事务封装、SQL 操作辅助 |
| `model/*` | 业务模型、Manager、Storager 接口、Param/Filter |
| `storage/rdb/*` | Storager 的 RDB 实现、DAO 层 |
| `stateful` | 配置加载、数据库连接、日志初始化 |
| `stateful/container` | 全局 Manager/Storager 单例声明 |
| `stateful/container/rdb` | 基于 RDB 的组件初始化与依赖注入 |

---

## 4. 数据库设计（概要）

`ai-gateway-api` 当前代码实现中共使用 25 张关系型数据表，按业务模块划分如下：

| 模块 | 表名 |
|------|------|
| 基础配置 | `bfe_clusters`、`products`、`domains`、`users`、`user_products`、`config_versions` |
| 集群与实例池 | `clusters`、`sub_clusters`、`pools`、`lb_matrices` |
| 路由规则 | `route_basic_rules`、`route_advance_rules`、`route_default_rules`、`route_cases`、`ai_route_rules`、`route_rules` |
| 证书与附加文件 | `certificates`、`extra_files` |
| API Key / Entity / 配额 / 限流 | `api_keys`、`api_key_tokens`、`entity_types`、`entities`、`quota_plans`、`quota_balances`、`rate_limit_policies` |

### 4.1 关键约定

- 底层驱动为标准库 `database/sql`，SQL 构建器使用 `github.com/didi/gendry`。
- 支持 MySQL 5.7+ 与 SQLite。
- 所有表均包含 `created_at` 与 `updated_at` 字段。
- 数据库 DDL **未声明物理 FOREIGN KEY 外键约束**，表间关系由应用层代码维护。
- AI 相关表使用 `utf8mb4`；历史表主要使用 `utf8`。

### 4.2 核心表简介

| 表 | 作用 |
|----|------|
| `api_keys` | API-Key 主表，通过 `quota_plan_id`、`rate_limit_policy_id`、`route_rules_id`、`entity_id` 关联配额、限流、路由规则与 Entity |
| `entities` | Entity 实体表，支持层级结构（`parent_id`）与模型白/黑名单 |
| `entity_types` | Entity 类型定义表，定义类型名、层级级别 |
| `quota_plans` | 配额计划表，记录是否无限配额、配额总量、重置周期等 |
| `quota_balances` | 配额余额表，记录已用/剩余量与上次重置时间 |
| `rate_limit_policies` | 限流策略表，记录 TPM/RPM 配置与最大并发数 |
| `route_rules` | API-Key/Entity/Global 级 AI 路由规则表 |
| `ai_route_rules` | 历史 AI 路由规则明细表，OpenAPI 不再通过 `/ai-route-rules` 暴露 |
| `certificates` | TLS 证书表 |
| `extra_files` | 附加文件表，用于 InnerAPI 导出 |

更多表结构、字段含义、索引设计和 DDL 详见《数据库设计文档.md》。

---

## 5. 存储层设计（概要）

存储层位于 `storage/rdb`，采用 **DAO + Storage** 两层结构。

### 5.1 架构角色

| 组件 | 职责 |
|------|------|
| `storage/rdb/internal/dao` | 按表提供 `T<Table>One/List/Create/Update/Delete` 函数，直接操作数据库 |
| `storage/rdb/*` | 实现 `model/*` 中定义的 `XxxStorager` 接口，完成 DAO 结果与业务模型的转换 |
| `storage/rdb/txn` | 实现 `model/itxn.TxnStorager` 事务抽象 |

### 5.2 DAO 通用模式

每个 DAO 文件遵循统一模板：

1. 表名常量：`const t<Table>TableName = "<table_name>"`
2. 结果结构体：`T<Table>`，字段使用 `db:"column_name"` 标签
3. 参数结构体：`T<Table>Param`，字段多为指针类型，包含 `_orderby`、`_limit` 控制字段
4. CRUD 函数：`T<Table>One`、`T<Table>List`、`T<Table>Create`、`T<Table>Update`、`T<Table>Delete`

### 5.3 Storage 实现组织

| 存储包 | 对应模型包 | 覆盖表 |
|--------|------------|--------|
| `storage/rdb/auth` | `model/iauth` | `users`、`user_products` |
| `storage/rdb/basic` | `model/ibasic` | `bfe_clusters`、`products`、`extra_files` |
| `storage/rdb/cluster_conf` | `model/icluster_conf` | `clusters`、`sub_clusters`、`pools`、`api_keys`、`lb_matrices` |
| `storage/rdb/protocol` | `model/iprotocol` | `certificates` |
| `storage/rdb/quota` | `model/quota`、`model/shared` | `entity_types`、`entities`、`quota_plans`、`quota_balances`、`rate_limit_policies`、`route_rules` |
| `storage/rdb/route_conf` | `model/iroute_conf` | `domains`、`route_basic_rules`、`route_advance_rules`、`route_default_rules`、`route_cases` |
| `storage/rdb/ai_route` | `model/iai_route` | `ai_route_rules` |
| `storage/rdb/version_control` | `model/iversion_control` | `config_versions` |

更多 DAO 清单、Storage 接口定义、事务实现和典型示例详见《存储层设计文档.md》。

---

## 6. 模型层设计（概要）

模型层位于 `model/`，采用 **Manager + Storager 接口** 的分层模式。

### 6.1 核心抽象

| 类型 | 说明 |
|------|------|
| `XxxParam` | 创建/更新入参，字段多为指针类型以区分“未传”与“零值” |
| `XxxFilter` | 查询条件，包含分页字段 `Page` / `PageSize` |
| `XxxStorager` | 定义持久化操作契约，由 `storage/rdb/` 各子包实现 |
| `XxxManager` | 业务逻辑实现，聚合 `itxn.TxnStorager` 与其它 `Storager` |

### 6.2 模型包职责

| 模型包 | 主要职责 |
|--------|----------|
| `model/iauth` | 用户/Token 认证、产品线授权、Feature-Action 权限定义 |
| `model/ibasic` | Product、BFECluster、ExtraFile 业务逻辑 |
| `model/icluster_conf` | Cluster、SubCluster、Pool、API-Key 业务逻辑 |
| `model/iroute_conf` | 基础/高级路由规则、Domain 业务逻辑 |
| `model/iai_route` | AI 路由规则模型与校验 |
| `model/iprotocol` | TLS 证书模型与业务逻辑 |
| `model/imods` | mod-api-key、mod-body-process、AI 路由导出配置组装 |
| `model/quota` | EntityType、Entity、QuotaPlan、QuotaBalance、RateLimitPolicy、BalanceSync、QuotaResetScheduler |
| `model/shared` | 跨包共享类型与接口（含 `RouteRulesStorager`） |
| `model/route_rules` | `RouteRulesManager`：Global/Entity/API-Key 路由规则业务逻辑 |
| `model/itxn` | 事务抽象接口 `TxnStorager` |
| `model/iversion_control` | 配置版本控制与增量导出 |

### 6.3 事务管理

```go
// model/itxn/txn.go
type TxnStorager interface {
    AtomExecute(ctx context.Context, do func(context.Context) error) error
}
```

`storage/rdb/txn` 提供基于 `database/sql` 的实现；Manager 通过 `container.TxnStoragerSingleton` 编排跨多个 Storager 的原子操作。

### 6.4 版本控制

`model/iversion_control` 为配置变更提供统一的版本号管理，InnerAPI 导出接口通过 `version` 参数实现增量同步：当请求版本与当前版本一致时返回 `Data: nil`。

更多模型结构、接口定义、Manager 初始化、典型业务流程详见《模型层设计文档.md》。

---

## 7. 接口层设计（概要）

接口层位于 `endpoints/`，统一使用 `xreq.Endpoint` 抽象：

```go
type Endpoint struct {
    Path string
    Method string
    Handler Handler
    Authorizer func(*http.Request) error
    RegisterHandler func(*mux.Router) *mux.Route
}
```

### 7.1 两大接口族

| 接口族 | 前缀 | 用途 | 中间件 |
|--------|------|------|--------|
| **OpenAPI v1** | `/open-api/v1` | 管理面接口，供 Dashboard/管理员使用 | `McProductProbe` + `McUserProbe` |
| **InnerAPI v1** | `/inner-api/v1` | 数据面导出接口，供 BFE/Conf Agent 拉取配置 | `McUserProbe` |

### 7.2 路由注册

- 根路由在 `endpoints/router.go` 中注册：全局 Recovery、Logger、CORS，静态文件服务。
- OpenAPI 路由在 `endpoints/openapi_v1/endpoints.go` 中合并各子包 Endpoint 切片注册。
- InnerAPI 路由在 `endpoints/innerapi_v1/endpoints.go` 中统一注册导出接口。

### 7.3 OpenAPI 主要模块

| 子包 | 路径 | 功能 |
|------|------|------|
| `api_key` | `/api-keys` | API-Key 管理 |
| `entity` | `/entities` | Entity 管理 |
| `entity_type` | `/entity-types` | Entity-Type 管理 |
| `global_route_rules` | `/global-route-rules` | Global 路由表 |
| `route_tables` | `/route-tables` | 路由表列表 |
| `product_cluster` | `/clusters` | 集群管理 |
| `model_provider_type` | `/model-provider-types` | AI 模型提供商类型查询 |
| `tool` | `/tools/get-models-from-provider` | 从指定提供商获取模型列表 |
| `certificate` | `/certificates` | 证书管理 |
| `bfe_pool` | `/alb-pool` | 默认 AI 实例池 |
| `auth` | `/auth`、`/meta` | 用户、Session Key、Token（Scope 收敛为 System/Support） |
| `route` | `/expression/verify` | BFE 表达式校验（需 `FeatureRoute + ActionRead`） |

### 7.4 InnerAPI 主要导出接口

| 路径 | 功能 |
|------|------|
| `/configs/tls_conf/server_data_conf` | 导出 TLS/Server/路由规则配置 |
| `/configs/gslb_data/gslb` | 导出 GSLB 调度配置 |
| `/configs/gslb_data/cluster_table` | 导出集群表配置 |
| `/configs/protocol/server_cert_conf` | 导出证书配置 |
| `/configs/extra_files/{filename}` | 导出附加文件 |
| `/configs/mod-api-key` | 导出 API-Key 及配额配置 |
| `/configs/mod-body-process` | 导出请求体处理配置 |
| `/configs/rate-limit-policy` | 导出限流策略配置 |
| `/configs/ai-route` | 导出 AI 路由配置 |

更多接口清单、中间件执行顺序、参数绑定方式、鉴权机制和典型代码示例详见《接口层设计文档.md》。

---

## 8. 依赖注入配置

系统使用**全局容器 + 手动依赖注入**模式。所有可复用的 Manager 与 Storager 单例声明在 `stateful/container/components.go`，初始化逻辑集中在 `stateful/container/rdb/components.go:Init()`。

### 8.1 容器组件

```go
// stateful/container/components.go（节选）
var (
    // 事务与版本控制
    TxnStoragerSingleton itxn.TxnStorager
    VersionControlStoragerSingleton iversion_control.VersionControlStorager

    // 基础/集群/路由/证书/认证存储
    ProductStoragerSingleton ibasic.ProductStorager
    BFEClusterStoragerSingleton ibasic.BFEClusterStorager
    ClusterStoragerSingleton icluster_conf.ClusterStorager
    PoolStoragerSingleton icluster_conf.PoolStorager
    SubClusterStoragerSingleton icluster_conf.SubClusterStorager
    APIKeyStorager icluster_conf.APIKeyStorager
    CertificateStoragerSingleton iprotocol.CertificateStorager
    AuthenticateStoragerSingleton iauth.AuthenticateStorager
    AuthorizeStoragerSingleton iauth.AuthorizeStorager
    ExtraFileStoragerSingleton ibasic.ExtraFileStorager
    AIRouteRuleStorager iai_route.AIRouteRuleStorager

    // Manager
    ExtraFileManager *ibasic.ExtraFileManager
    ProductManager *ibasic.ProductManager
    DomainManager *iroute_conf.DomainManager
    BFEClusterManager *ibasic.BFEClusterManager
    VersionControlManager *iversion_control.VersionControlManager
    RouteRuleManager *iroute_conf.RouteRuleManager
    ClusterManager *icluster_conf.ClusterManager
    SubClusterManager *icluster_conf.SubClusterManager
    CertificateManager *iprotocol.CertificateManager
    AuthenticateManager *iauth.AuthenticateManager
    AuthorizeManager *iauth.AuthorizeManager
    PoolManager *icluster_conf.PoolManager
    APIKeyRuleManager *imods.APIKeyRuleManager
    ModBodyProcessManager *imods.ModBodyProcessManager
    APIKeyManager *icluster_conf.APIKeyManager
    AIRouteRuleManager *iai_route.AIRouteRuleManager

    // Quota 管理
    EntityTypeStorager quota.EntityTypeStorager
    EntityStorager quota.EntityStorager
    QuotaPlanStorager quota.QuotaPlanStorager
    QuotaBalanceStorager quota.QuotaBalanceStorager
    RateLimitPolicyStorager quota.RateLimitPolicyStorager
    RouteRulesStorager shared.RouteRulesStorager

    EntityTypeManager *quota.EntityTypeManager
    EntityManager *quota.EntityManager
    QuotaPlanManager *quota.QuotaPlanManager
    RateLimitPolicyManager *quota.RateLimitPolicyManager
    RouteRulesManager *route_rules.RouteRulesManager
    AIRouteExporter *imods.AIRouteExporter
    BalanceSyncManager *quota.BalanceSyncManager
    QuotaResetScheduler *quota.QuotaResetScheduler
)
```

### 8.2 初始化顺序

`stateful/container/rdb/components.go:Init()` 按以下顺序完成初始化：

1. **事务与基础存储**：`TxnStoragerSingleton`、`VersionControlStoragerSingleton`、`RouteRuleStoragerSingleton`、各类基础/集群/认证存储。
2. **基础 Manager**：`ExtraFileManager`、`VersionControlManager`、`BFEClusterManager`、`CertificateManager`、`ProductManager`、`AIRouteRuleManager`、`RouteRuleManager` 等。
3. **集群相关 Manager**：`ClusterManager`、`SubClusterManager`、`DomainManager`、`PoolManager`。
4. **认证授权 Manager**：`AuthenticateManager`、`AuthorizeManager`。
5. **Quota 相关存储与 Manager**：EntityType、Entity、QuotaPlan、QuotaBalance、RateLimitPolicy、RouteRules 及其 Manager。
6. **模块导出 Manager**：`APIKeyRuleManager`、`ModBodyProcessManager`、`AIRouteExporter`。
7. **API-Key Manager**：注入 Quota/RateLimit/Entity/Balance 适配器，实现 API-Key 与 Quota 体系的打通。
8. **余额同步与周期重置**：`BalanceSyncManager`、`QuotaResetScheduler`，并启动调度器。
9. **Global 路由表默认记录**：调用 `RouteRulesManager.EnsureGlobalRouteRules` 确保 `route_rules` 表中存在 `type=global, owner=global` 的默认记录；若已存在则跳过。

### 8.3 适配器模式

由于 API-Key 等历史模型位于 `model/icluster_conf`，而 Quota 相关存储位于 `model/quota`，代码中通过**适配器**（如 `quota.NewQuotaPlanStoragerAdapter`、`quota.NewEntityStoragerAdapter`）将 `quota` 包中的 Storager 接口转换为 `icluster_conf` 包所需的接口，实现跨包复用。

---

## 9. 启动流程

```
main.go
  │
  ├─ flag.Parse() # 解析命令行参数
  ├─ stateful.LoadConfig() # 加载 TOML 配置文件
  ├─ config.Init() # 初始化路径变量
  ├─ config.InitLog() # 初始化日志
  ├─ config.Depends.Init() # 初始化依赖（i18n、nav_tree 等）
  ├─ config.InitDB() # 初始化数据库连接池
  ├─ rdb.Init() # 依赖注入：初始化所有 Manager/Storager，并确保默认 Global 路由表存在
  │
  └─ serverStartUp()
       ├─ NewMonitorServerWithRun() # 启动监控端口
       ├─ endpoints.RegisterRouters()# 注册路由
       └─ graceful.Run() # 启动 HTTP 服务
```

---

## 10. 设计原则与约定

1. **分层解耦**：接口层只依赖模型层 Manager；模型层只依赖 Storager 接口和事务抽象；存储层只负责数据库读写。
2. **接口导向**：模型层通过 `XxxStorager` 接口定义持久化契约，便于未来替换存储实现。
3. **统一 Endpoint 抽象**：所有 HTTP 接口使用 `xreq.Endpoint` 描述，统一注册、鉴权和中间件处理。
4. **子包自治**：每个业务子包独立维护自己的 Endpoint、Action、Manager、Storager；顶层 `endpoints.go` 仅做合并。
5. **全局容器**：Manager 与 Storager 作为全局单例存在 `stateful/container`，通过 `rdb.Init()` 完成一次性初始化。
6. **版本控制**：配置变更统一由 `iversion_control` 管理版本，支持 InnerAPI 增量导出。
7. **无物理外键**：表间关系由应用层维护，DDL 不声明 FOREIGN KEY，便于分库分表与灵活变更。
8. **事务编排**：跨表/跨 Storager 的业务操作通过 `TxnStorager.AtomExecute` 保证原子性。

---

## 11. 相关文档索引

| 主题 | 文档路径 |
|------|----------|
| 接口定义（OpenAPI） | `ai-gateway-api/design-docs/api-define/OpenAPI接口定义/README.md` |
| 接口定义（InnerAPI） | `ai-gateway-api/design-docs/api-define/InnerAPI接口定义/README.md` |
| 数据库设计详情 | `ai-gateway-api/design-docs/sys-design/数据库设计文档.md` |
| 存储层设计详情 | `ai-gateway-api/design-docs/sys-design/存储层设计文档.md` |
| 模型层设计详情 | `ai-gateway-api/design-docs/sys-design/模型层设计文档.md` |
| 接口层设计详情 | `ai-gateway-api/design-docs/sys-design/接口层设计文档.md` |
