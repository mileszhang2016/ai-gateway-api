# Rainway AI Gateway - 数据库设计文档

## 1. 概述

本文档描述 `ai-gateway-api` 当前代码实现中使用的数据库表结构，覆盖基础配置、集群与实例池、路由规则、证书与附加文件、API Key / Entity / 配额 / 限流等全部持久化表。

### 1.1 数据库引擎与访问方式

| 项目 | 说明 |
|------|------|
| 底层驱动 | 标准库 `database/sql` |
| SQL 构建器 | `github.com/didi/gendry`（builder + scanner） |
| 支持数据库 | MySQL 5.7+、SQLite |
| 连接管理 | `stateful/config_database.go`：`NewDB()` / `BFEDB()` |
| 事务封装 | `lib/xdb.go`：`DBContext` / `Transaction` |
| ORM 风格 | 无 ORM，通过 `db` tag 将 Go struct 映射到表字段 |

### 1.2 通用约定

- 所有表均包含 `created_at` 与 `updated_at` 字段。
- MySQL 中 `updated_at` 默认使用 `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`；SQLite 通过 trigger 模拟该行为。
- 数据库 DDL **未声明物理 FOREIGN KEY 外键约束**，表间关系均为逻辑外键，由应用层代码维护。
- 历史表主要使用 `utf8` 字符集；AI 相关表（`api_keys`、`entity_types`、`entities`、`quota_plans`、`rate_limit_policies`、`route_rules` 等）使用 `utf8mb4`。

### 1.3 表清单

按业务模块划分，共 26 张表：

| 模块 | 表名 |
|------|------|
| 基础配置 | `bfe_clusters`、`products`、`domains`、`users`、`user_products`、`config_versions` |
| Provider | `providers` |
| 集群与实例池 | `clusters`、`sub_clusters`、`pools`、`lb_matrices` |
| 路由规则 | `route_basic_rules`、`route_advance_rules`、`route_default_rules`、`route_cases`、`ai_route_rules`、`route_rules` |
| 证书与附加文件 | `certificates`、`extra_files` |
| API Key / Entity / 配额 / 限流 | `api_keys`、`api_key_tokens`、`entity_types`、`entities`、`quota_plans`、`rate_limit_policies` |
| 模型定价 | `model_prices` |
| 操作日志 | `operation_logs` |

---

## 2. 基础配置表

### 2.1 `bfe_clusters`

BFE 集群配置表。

```sql
CREATE TABLE `bfe_clusters` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `pool_name` varchar(255) NOT NULL DEFAULT '',
  `capacity` bigint(20) NOT NULL,
  `enabled` tinyint(1) NOT NULL DEFAULT 1,
  `gtc_enabled` tinyint(1) NOT NULL DEFAULT 1,
  `gtc_manual_enabled` tinyint(1) NOT NULL DEFAULT 1,
  `exempt_traffic_check` tinyint(1) NOT NULL DEFAULT 0,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_uni` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL、唯一 | BFE 集群名 |
| `pool_name` | varchar(255) | NOT NULL DEFAULT '' | 关联 pool 名 |
| `capacity` | bigint(20) | NOT NULL | 容量 |
| `enabled` | tinyint(1) | NOT NULL DEFAULT 1 | 是否启用 |
| `gtc_enabled` | tinyint(1) | NOT NULL DEFAULT 1 | GTC 是否启用 |
| `gtc_manual_enabled` | tinyint(1) | NOT NULL DEFAULT 1 | GTC 手动启用 |
| `exempt_traffic_check` | tinyint(1) | NOT NULL DEFAULT 0 | 是否豁免流量检查 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 2.2 `products`

产品线配置表。

```sql
CREATE TABLE `products` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `mail_list` varchar(4096) NOT NULL,
  `contact_person` varchar(4096) NOT NULL,
  `sms_list` varchar(4096) NOT NULL DEFAULT 'no sms',
  `description` varchar(1024) NOT NULL DEFAULT 'no desc',
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_uni` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL、唯一 | 产品名 |
| `mail_list` | varchar(4096) | NOT NULL | 邮件列表（分号拼接） |
| `contact_person` | varchar(4096) | NOT NULL | 联系人（分号拼接） |
| `sms_list` | varchar(4096) | NOT NULL DEFAULT 'no sms' | 短信列表 |
| `description` | varchar(1024) | NOT NULL DEFAULT 'no desc' | 描述 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 2.3 `domains`

域名配置表。

```sql
CREATE TABLE `domains` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `product_id` bigint(20) NOT NULL,
  `type` int(11) NOT NULL,
  `using_advanced_redirect` tinyint(1) NOT NULL DEFAULT 0,
  `using_advanced_hsts` tinyint(1) NOT NULL DEFAULT 0,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_uni` (`name`),
  KEY `product_id` (`product_id`),
  KEY `type` (`type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL、唯一 | 域名 |
| `product_id` | bigint(20) | NOT NULL、索引 | 所属产品 ID |
| `type` | int(11) | NOT NULL、索引 | 域名类型 |
| `using_advanced_redirect` | tinyint(1) | NOT NULL DEFAULT 0 | 是否启用高级重定向 |
| `using_advanced_hsts` | tinyint(1) | NOT NULL DEFAULT 0 | 是否启用 HSTS |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 2.4 `users`

用户与 Token 共用表，通过 `type` 字段区分。

```sql
CREATE TABLE `users` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `type` tinyint(1) NOT NULL DEFAULT 0,
  `password` varchar(255) NOT NULL DEFAULT '',
  `ticket` varchar(20) NOT NULL DEFAULT '',
  `ticket_created_at` datetime NOT NULL DEFAULT '0000-01-01 00:00:00',
  `scopes` varchar(2048) NOT NULL DEFAULT '',
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_uni` (`name`, `type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL | 用户名 / Token 名 |
| `type` | tinyint(1) | NOT NULL DEFAULT 0 | 0=普通用户，1=Token |
| `password` | varchar(255) | NOT NULL DEFAULT '' | 密码 |
| `ticket` | varchar(20) | NOT NULL DEFAULT '' | Session Key / Token 值 |
| `ticket_created_at` | datetime | NOT NULL DEFAULT '0000-01-01 00:00:00' | Session 创建时间 |
| `scopes` | varchar(2048) | NOT NULL DEFAULT '' | 权限范围 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 2.5 `user_products`

用户/Token 与产品绑定关系表。

```sql
CREATE TABLE `user_products` (
  `user_id` bigint(20) NOT NULL,
  `product_id` bigint(20) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`user_id`, `product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `user_id` | bigint(20) | 联合主键 | 用户/Token ID |
| `product_id` | bigint(20) | 联合主键 | 产品 ID |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 2.6 `config_versions`

配置版本信息表。

```sql
CREATE TABLE `config_versions` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `data_sign` varchar(255) NOT NULL,
  `version` varchar(255) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL | 配置名 |
| `data_sign` | varchar(255) | NOT NULL | 数据签名 |
| `version` | varchar(255) | NOT NULL | 版本号 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

---

## 3. Provider 与集群实例池表

### 3.1 `providers`

Provider 配置表，集中管理下游模型提供方的接入能力。

```sql
CREATE TABLE `providers` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `description` varchar(1024) NOT NULL DEFAULT 'no desc',
  `model_endpoint` text,
  `models` text,
  `keys` text,
  `instance_pool` text,
  `model_protocols` text,
  `time_zone` varchar(255) NOT NULL DEFAULT 'Asia/Shanghai',
  `tiers` text,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_index` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL、唯一 | Provider 唯一标识；合法命名参考 `ClusterName` |
| `description` | varchar(1024) | NOT NULL DEFAULT 'no desc' | 描述 |
| `model_endpoint` | text | NULL | 模型发现端点 JSON，默认 `{schema: "https", uri: "/v1/models"}` |
| `models` | text | NULL | 支持的模型列表 JSON |
| `keys` | text | NULL | API Key 明文列表 JSON；元素为 `{name, key}` |
| `instance_pool` | text | NULL | 后端实例池 JSON；元素为 `{name, addr, weight, port}` |
| `model_protocols` | text | NULL | 支持的模型访问协议 JSON；首期枚举：`openai`、`anthropic` |
| `time_zone` | varchar(255) | NOT NULL DEFAULT 'Asia/Shanghai' | 计算时段所使用的时区；IANA 时区名 |
| `tiers` | text | NULL | 时段 tier 定义列表 JSON；元素为 `{name, time_ranges}`；**初期 `name` 只支持 `peak`** |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

> 说明：`time_zone` / `tiers` 描述该 provider 的时段规则，供 InnerAPI 导出 `AIConf.ModelTable` 时使用。同一 provider 下所有模型共享该规则。

> 说明：`providers` 表使用 `utf8mb4` 字符集以支持模型名等多语言内容。Provider 被 `/clusters` 引用时不可删除；`/model-prices` 中的同名 provider 不再阻塞删除。

### 3.2 `clusters`

AI 集群配置表。

```sql
CREATE TABLE `clusters` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `description` varchar(1024) NOT NULL DEFAULT 'no desc',
  `product_id` bigint(20) NOT NULL,
  `protocol` varchar(255) NOT NULL DEFAULT 'http',
  `max_idle_conn_per_host` smallint(6) NOT NULL DEFAULT 2,
  `timeout_conn_serv` int(11) NOT NULL DEFAULT 50000,
  `timeout_response_header` int(11) NOT NULL DEFAULT 50000,
  `timeout_readbody_client` int(11) NOT NULL DEFAULT 30000,
  `timeout_read_client_again` int(11) NOT NULL DEFAULT 30000,
  `timeout_write_client` int(11) NOT NULL DEFAULT 60000,
  `healthcheck_schem` varchar(16) NOT NULL DEFAULT 'http',
  `healthcheck_interval` int(11) NOT NULL DEFAULT 1000,
  `healthcheck_failnum` int(11) NOT NULL DEFAULT 10,
  `healthcheck_host` varchar(255) NOT NULL,
  `healthcheck_uri` varchar(255) NOT NULL,
  `healthcheck_statuscode` int(11) NOT NULL DEFAULT 200,
  `clientip_carry` tinyint(4) NOT NULL DEFAULT 0,
  `port_carry` tinyint(1) NOT NULL DEFAULT 0,
  `max_retry_in_cluster` tinyint(4) NOT NULL DEFAULT 3,
  `max_retry_cross_cluster` tinyint(4) NOT NULL DEFAULT 0,
  `ready` tinyint(1) NOT NULL DEFAULT 1,
  `hash_strategy` int(11) NOT NULL DEFAULT 0,
  `cookie_key` varchar(255) NOT NULL DEFAULT 'BAIDUID',
  `hash_header` varchar(255) NOT NULL DEFAULT 'Cookie:BAIDUID',
  `session_sticky` tinyint(1) NOT NULL DEFAULT 0,
  `req_write_buffer_size` int(11) NOT NULL DEFAULT 512,
  `req_flush_interval` int(11) NOT NULL DEFAULT 0,
  `res_flush_interval` int(11) NOT NULL DEFAULT 20,
  `cancel_on_client_close` tinyint(1) NOT NULL DEFAULT 0,
  `failure_status` tinyint(1) NOT NULL DEFAULT 0,
  `max_conns_per_host` int(11) NOT NULL DEFAULT 0,
  `llm_config` text,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_index` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL、唯一 | 集群名 |
| `description` | varchar(1024) | NOT NULL DEFAULT 'no desc' | 描述 |
| `product_id` | bigint(20) | NOT NULL | 所属产品 ID |
| `protocol` | varchar(255) | NOT NULL DEFAULT 'http' | 协议：http/https |
| `max_idle_conn_per_host` | smallint(6) | NOT NULL DEFAULT 2 | 每主机最大空闲连接 |
| `timeout_conn_serv` | int(11) | NOT NULL DEFAULT 50000 | 连接后端超时 |
| `timeout_response_header` | int(11) | NOT NULL DEFAULT 50000 | 响应头超时 |
| `timeout_readbody_client` | int(11) | NOT NULL DEFAULT 30000 | 读客户端 body 超时 |
| `timeout_read_client_again` | int(11) | NOT NULL DEFAULT 30000 | 再次读客户端超时 |
| `timeout_write_client` | int(11) | NOT NULL DEFAULT 60000 | 写客户端超时 |
| `healthcheck_schem` | varchar(16) | NOT NULL DEFAULT 'http' | 健康检查协议 |
| `healthcheck_interval` | int(11) | NOT NULL DEFAULT 1000 | 健康检查间隔 |
| `healthcheck_failnum` | int(11) | NOT NULL DEFAULT 10 | 健康检查失败次数 |
| `healthcheck_host` | varchar(255) | NOT NULL | 健康检查 Host |
| `healthcheck_uri` | varchar(255) | NOT NULL | 健康检查 URI |
| `healthcheck_statuscode` | int(11) | NOT NULL DEFAULT 200 | 健康检查状态码 |
| `clientip_carry` | tinyint(4) | NOT NULL DEFAULT 0 | 是否透传客户端 IP |
| `port_carry` | tinyint(1) | NOT NULL DEFAULT 0 | 是否透传端口 |
| `max_retry_in_cluster` | tinyint(4) | NOT NULL DEFAULT 3 | 集群内最大重试 |
| `max_retry_cross_cluster` | tinyint(4) | NOT NULL DEFAULT 0 | 跨集群最大重试 |
| `ready` | tinyint(1) | NOT NULL DEFAULT 1 | 是否就绪 |
| `hash_strategy` | int(11) | NOT NULL DEFAULT 0 | 哈希策略 |
| `cookie_key` | varchar(255) | NOT NULL DEFAULT 'BAIDUID' | Cookie Key |
| `hash_header` | varchar(255) | NOT NULL DEFAULT 'Cookie:BAIDUID' | 哈希头部 |
| `session_sticky` | tinyint(1) | NOT NULL DEFAULT 0 | 会话保持 |
| `req_write_buffer_size` | int(11) | NOT NULL DEFAULT 512 | 请求写缓冲区 |
| `req_flush_interval` | int(11) | NOT NULL DEFAULT 0 | 请求刷新间隔 |
| `res_flush_interval` | int(11) | NOT NULL DEFAULT 20 | 响应刷新间隔 |
| `cancel_on_client_close` | tinyint(1) | NOT NULL DEFAULT 0 | 客户端关闭时取消 |
| `failure_status` | tinyint(1) | NOT NULL DEFAULT 0 | 失败状态 |
| `max_conns_per_host` | int(11) | NOT NULL DEFAULT 0 | 每主机最大连接 |
| `llm_config` | text | NULL | LLM 转发策略 JSON；当前包含 `models`、`model_mappings`、`keys`（Key 引用与权重数组，元素为 `{name, weight}`）、`key_policy`、`key_affinity`（会话级 Key 亲和性配置，包含 `enabled`、`ttl`、`redis_prefix`、`penalty_enable`）、`provider`（必填，引用 `providers.name`）、`match_prefix`、`strip_prefix` 等字段；`model_endpoint`、`provider_type` 已移除，key 明文下沉到 Provider |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

> 说明：`ready`、`max_retry_cross_cluster`、`hash_strategy`、`session_sticky` 等字段仍保留在 `clusters` 表中供数据面导出使用，但管理面 OpenAPI 不再对外暴露；对外模型通过接口层映射简化。

### 3.3 `lb_matrices`

负载均衡矩阵表，用于集群调度权重配置。

```sql
CREATE TABLE `lb_matrices` (
  `cluster_id` bigint(20) NOT NULL AUTO_INCREMENT,
  `lb_matrix` varchar(8192) NOT NULL,
  `product_id` bigint(20) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`cluster_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `cluster_id` | bigint(20) | 主键、自增 | 集群 ID |
| `lb_matrix` | varchar(8192) | NOT NULL | 权重矩阵 JSON |
| `product_id` | bigint(20) | NOT NULL | 所属产品 ID |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 3.4 `sub_clusters`

子集群表。

```sql
CREATE TABLE `sub_clusters` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `cluster_id` bigint(20) NOT NULL,
  `product_id` bigint(20) NOT NULL,
  `description` varchar(1024) NOT NULL DEFAULT 'no desc',
  `bns_name_id` bigint(20) NOT NULL,
  `enabled` tinyint(1) NOT NULL DEFAULT 1,
  `role` varchar(32) NOT NULL DEFAULT '',
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `cluster_id` (`cluster_id`),
  UNIQUE KEY `name_product_index` (`name`, `product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL | 子集群名 |
| `cluster_id` | bigint(20) | NOT NULL、索引 | 所属集群 ID |
| `product_id` | bigint(20) | NOT NULL | 所属产品 ID |
| `description` | varchar(1024) | NOT NULL DEFAULT 'no desc' | 描述 |
| `bns_name_id` | bigint(20) | NOT NULL | 关联 pool ID |
| `enabled` | tinyint(1) | NOT NULL DEFAULT 1 | 是否启用 |
| `role` | varchar(32) | NOT NULL DEFAULT '' | 角色 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 3.5 `pools`

实例池表。Provider 与 Cluster 分离后，实例池数据源自 `providers.instance_pool`；创建/更新 Cluster 时，系统根据 `llm_config.provider` 读取对应 Provider 的实例池并同步生成 `pools` 记录。

```sql
CREATE TABLE `pools` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `product_id` bigint(20) NOT NULL DEFAULT 0,
  `ready` tinyint(1) NOT NULL DEFAULT 1,
  `instance_detail` mediumtext,
  `type` tinyint(4) NOT NULL DEFAULT 1,
  `tag` tinyint(4) NOT NULL DEFAULT 0,
  `role` varchar(32) NOT NULL DEFAULT 'COMMON',
  `epp_server` mediumtext,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_uni` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL、唯一 | 池名 |
| `product_id` | bigint(20) | NOT NULL DEFAULT 0 | 所属产品 ID |
| `ready` | tinyint(1) | NOT NULL DEFAULT 1 | 是否就绪 |
| `instance_detail` | mediumtext | NULL | 实例详情 JSON |
| `type` | tinyint(4) | NOT NULL DEFAULT 1 | 类型 |
| `tag` | tinyint(4) | NOT NULL DEFAULT 0 | 标签 |
| `role` | varchar(32) | NOT NULL DEFAULT 'COMMON' | 角色 |
| `epp_server` | mediumtext | NULL | EPP 配置 JSON |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

---

## 4. 路由规则表

### 4.1 `route_basic_rules`

基础路由规则表。

```sql
CREATE TABLE `route_basic_rules` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `description` varchar(1024) NOT NULL DEFAULT '',
  `product_id` bigint(20) NOT NULL,
  `host_names` text NOT NULL,
  `paths` text NOT NULL,
  `cluster_id` bigint(20) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `product_id` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `description` | varchar(1024) | NOT NULL DEFAULT '' | 描述 |
| `product_id` | bigint(20) | NOT NULL、索引 | 所属产品 ID |
| `host_names` | text | NOT NULL | host 列表 JSON |
| `paths` | text | NOT NULL | path 列表 JSON |
| `cluster_id` | bigint(20) | NOT NULL | 目标集群 ID |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 4.2 `route_advance_rules`

高级路由规则表。

```sql
CREATE TABLE `route_advance_rules` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `description` varchar(1024) NOT NULL DEFAULT '',
  `product_id` bigint(20) NOT NULL,
  `expression` varchar(4096) CHARACTER SET utf8 COLLATE utf8_bin NOT NULL,
  `cluster_id` bigint(20) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `product_id` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL | 规则名 |
| `description` | varchar(1024) | NOT NULL DEFAULT '' | 描述 |
| `product_id` | bigint(20) | NOT NULL、索引 | 所属产品 ID |
| `expression` | varchar(4096) binary | NOT NULL | BFE 条件表达式 |
| `cluster_id` | bigint(20) | NOT NULL | 目标集群 ID |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 4.3 `route_default_rules`

默认转发规则表，每个产品一条记录。

```sql
CREATE TABLE `route_default_rules` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `cmd` varchar(20) NOT NULL,
  `params` text,
  `product_id` bigint(20) NOT NULL,
  `route_action` text,
  `description` varchar(1024) NOT NULL DEFAULT '',
  `created_at` datetime NOT NULL DEFAULT '0000-01-01 00:00:00',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `product_id` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `cmd` | varchar(20) | NOT NULL | 命令 |
| `params` | text | NULL | 命令参数 JSON |
| `product_id` | bigint(20) | NOT NULL、唯一 | 所属产品 ID |
| `route_action` | text | NULL | 转发动作 JSON |
| `description` | varchar(1024) | NOT NULL DEFAULT '' | 描述 |
| `created_at` | datetime | NOT NULL DEFAULT '0000-01-01 00:00:00' | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 4.4 `route_cases`

路由测试用例表。该表在 DDL 中定义，当前代码中无对应 DAO 文件。

```sql
CREATE TABLE `route_cases` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `description` varchar(1024) NOT NULL DEFAULT '',
  `product_id` bigint(20) NOT NULL,
  `url` varchar(4096) NOT NULL,
  `method` varchar(255) NOT NULL DEFAULT '',
  `protocol` varchar(255) NOT NULL DEFAULT '',
  `header` varchar(4096) NOT NULL,
  `body` varchar(4096) NOT NULL DEFAULT '',
  `expect_cluster` varchar(255) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `product_id` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `description` | varchar(1024) | NOT NULL DEFAULT '' | 描述 |
| `product_id` | bigint(20) | NOT NULL、索引 | 所属产品 ID |
| `url` | varchar(4096) | NOT NULL | 测试 URL |
| `method` | varchar(255) | NOT NULL DEFAULT '' | 请求方法 |
| `protocol` | varchar(255) | NOT NULL DEFAULT '' | 协议 |
| `header` | varchar(4096) | NOT NULL | 请求头 JSON |
| `body` | varchar(4096) | NOT NULL DEFAULT '' | 请求体 |
| `expect_cluster` | varchar(255) | NOT NULL | 期望集群 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 4.5 `ai_route_rules`

AI 路由规则表。

```sql
CREATE TABLE `ai_route_rules` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL DEFAULT '',
  `basic` text NOT NULL,
  `product_name` varchar(255) NOT NULL DEFAULT '',
  `idx` bigint(20) NOT NULL DEFAULT 0,
  `created_at` datetime NOT NULL DEFAULT '0000-01-01 00:00:00',
  `updated_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name_idx` (`name`),
  KEY `idx` (`idx`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL DEFAULT ''、唯一 | 规则名 |
| `basic` | text | NOT NULL | 基础配置 JSON |
| `product_name` | varchar(255) | NOT NULL DEFAULT '' | 产品线名 |
| `idx` | bigint(20) | NOT NULL DEFAULT 0、索引 | 排序索引 |
| `created_at` | datetime | NOT NULL DEFAULT '0000-01-01 00:00:00' | 创建时间 |
| `updated_at` | datetime | 自动更新 | 更新时间 |

### 4.6 `route_rules`

API Key / Entity / Global 级路由规则表。

```sql
CREATE TABLE `route_rules` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `type` VARCHAR(32) NOT NULL DEFAULT 'api_key',
  `owner` VARCHAR(64) NOT NULL,
  `enabled` TINYINT(1) DEFAULT 0,
  `rules` TEXT,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_type_owner` (`type`, `owner`),
  INDEX `idx_enabled` (`enabled`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='路由规则表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | 主键、自增 | 主键 ID |
| `type` | VARCHAR(32) | NOT NULL DEFAULT 'api_key' | 规则类型：api_key / entity / global |
| `owner` | VARCHAR(64) | NOT NULL | 所有者标识：api_key_id / entity_id / global |
| `enabled` | TINYINT(1) | DEFAULT 0、索引 | 是否启用 |
| `rules` | TEXT | NULL | 路由规则列表 JSON 数组 |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | 自动更新 | 更新时间 |

**默认数据：**

系统初始化后应存在一条默认 Global 路由表记录：

```sql
INSERT IGNORE INTO `route_rules` (`type`, `owner`, `enabled`, `rules`)
VALUES ('global', 'global', 0, '[]');
```

> 代码层面：`stateful/container/rdb/components.go:Init()` 通过 `RouteRulesManager.EnsureGlobalRouteRules` 在启动时自动检查并创建该记录；DDL 脚本中也包含该 INSERT，确保全新数据库与代码行为一致。

**rules JSON 格式说明：**

```json
[
    {
        "name": "apikey-default",
        "Cond": "default_t()",
        "targets": [
            {
                "ClusterName": "cluster_apikey",
                "Model": "",
                "Weight": 100
            }
        ],
        "fallbacks": []
    }
]
```

**targets 元素结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `ClusterName` | string | 后端集群名称 |
| `Model` | string | 模型名称，空字符串表示透传原始模型 |
| `Weight` | int | 权重，单个 target 时为 100，多个 target 时总和为 100 |

**fallbacks 元素结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `ClusterName` | string | 后端集群名称 |
| `Model` | string | 模型名称，空字符串表示透传原始模型 |

---

## 5. 证书与附加文件表

### 5.1 `certificates`

TLS 证书表。

```sql
CREATE TABLE `certificates` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `cert_name` varchar(255) NOT NULL,
  `description` varchar(1024) NOT NULL DEFAULT 'no desc',
  `is_default` tinyint(1) NOT NULL DEFAULT 0,
  `expired_date` varchar(255) NOT NULL COMMENT '从证书内容解析的过期日期，只读',
  `cert_file_path` varchar(255) NOT NULL,
  `key_file_path` varchar(255) NOT NULL,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name` (`cert_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `cert_name` | varchar(255) | NOT NULL、唯一 | 证书名 |
| `description` | varchar(1024) | NOT NULL DEFAULT 'no desc' | 描述 |
| `is_default` | tinyint(1) | NOT NULL DEFAULT 0 | 是否默认证书 |
| `expired_date` | varchar(255) | NOT NULL | 过期日期，由服务端从证书内容解析，OpenAPI 只读 |
| `cert_file_path` | varchar(255) | NOT NULL | 证书文件路径 |
| `key_file_path` | varchar(255) | NOT NULL | 密钥文件路径 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

### 5.2 `extra_files`

附加文件表。

```sql
CREATE TABLE `extra_files` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `product_id` bigint(20) NOT NULL DEFAULT 0,
  `description` varchar(1024) NOT NULL DEFAULT '',
  `md5` varchar(64) NOT NULL,
  `content` mediumtext,
  `created_at` datetime NOT NULL,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `product_id` (`product_id`),
  UNIQUE KEY `name_product` (`name`, `product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `name` | varchar(255) | NOT NULL | 文件名 |
| `product_id` | bigint(20) | NOT NULL DEFAULT 0、索引 | 所属产品 ID |
| `description` | varchar(1024) | NOT NULL DEFAULT '' | 描述 |
| `md5` | varchar(64) | NOT NULL | 文件 MD5 |
| `content` | mediumtext | NULL | 文件内容 |
| `created_at` | datetime | NOT NULL | 创建时间 |
| `updated_at` | timestamp | 自动更新 | 更新时间 |

---

## 6. API Key / Entity / 配额 / 限流表

### 6.1 `api_keys`

AI API Key 表。

```sql
CREATE TABLE `api_keys` (
  `inner_id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '内部id',
  `id` varchar(255) NOT NULL DEFAULT '' COMMENT 'API-Key标识',
  `enable` boolean NOT NULL DEFAULT false COMMENT 'api keys开关',
  `api_key` varchar(128) NOT NULL DEFAULT '' COMMENT '具体的key',
  `description` varchar(512) DEFAULT '' COMMENT 'API-Key描述',
  `unlimited_quota` tinyint(1) DEFAULT 0 COMMENT '是否无限配额：0-有限，1-无限',
  `product_name` varchar(255) NOT NULL DEFAULT '' COMMENT '产品线名称',
  `expired_time` bigint NOT NULL DEFAULT -1 COMMENT '过期时间（Unix时间戳，-1表示永不过期）',
  `allowed_models` text COMMENT '允许的模型（JSON数组）',
  `subnet` text COMMENT '允许的客户端子网（JSON数组）',
  `entity_id` varchar(64) DEFAULT NULL COMMENT '挂载的Entity ID',
  `quota_plan_id` bigint DEFAULT NULL COMMENT '配额计划ID',
  `rate_limit_policy_id` bigint DEFAULT NULL COMMENT '限流策略ID',
  `route_rules_id` bigint DEFAULT NULL COMMENT '路由规则ID',
  `created_at` datetime NOT NULL DEFAULT '0000-01-01 00:00:00' COMMENT '创建时间',
  `updated_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`inner_id`),
  UNIQUE KEY `uk_id` (`id`),
  UNIQUE KEY `uk_api_key` (`api_key`),
  INDEX `idx_product_name` (`product_name`),
  INDEX `idx_api_keys_entity_id` (`entity_id`),
  INDEX `idx_api_keys_quota_plan_id` (`quota_plan_id`),
  INDEX `idx_api_keys_rate_limit_policy_id` (`rate_limit_policy_id`),
  INDEX `idx_api_keys_route_rules_id` (`route_rules_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='api keys';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `inner_id` | bigint(20) | 主键、自增 | 内部主键 |
| `id` | varchar(255) | NOT NULL DEFAULT ''、唯一 | API-Key 业务标识 |
| `enable` | boolean | NOT NULL DEFAULT false | 开关 |
| `api_key` | varchar(128) | NOT NULL DEFAULT ''、唯一 | 实际 key 值；长度与 `api-keys.md` 中 `key` 的 1-128 字符限制保持一致，同时避免 utf8mb4 唯一索引超过 InnoDB 3072 字节上限 |
| `description` | varchar(512) | DEFAULT '' | 描述 |
| `unlimited_quota` | tinyint(1) | DEFAULT 0 | 是否无限配额 |
| `product_name` | varchar(255) | NOT NULL DEFAULT ''、索引 | 产品线名 |
| `expired_time` | bigint | NOT NULL DEFAULT -1 | 过期时间 Unix 秒，-1 表示永不过期 |
| `allowed_models` | text | NULL | 允许模型 JSON 数组 |
| `subnet` | text | NULL | 允许子网 JSON 数组 |
| `entity_id` | varchar(64) | DEFAULT NULL、索引 | 挂载 Entity ID |
| `quota_plan_id` | bigint | DEFAULT NULL、索引 | 配额计划 ID |
| `rate_limit_policy_id` | bigint | DEFAULT NULL、索引 | 限流策略 ID |
| `route_rules_id` | bigint | DEFAULT NULL、索引 | 路由规则 ID |
| `created_at` | datetime | NOT NULL DEFAULT '0000-01-01 00:00:00' | 创建时间 |
| `updated_at` | datetime | 自动更新 | 更新时间 |

### 6.2 `api_key_tokens`

API Key Token 生成记录表。

```sql
CREATE TABLE `api_key_tokens` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `api_key` varchar(128) NOT NULL DEFAULT '',
  `created_at` datetime NOT NULL DEFAULT '0000-01-01 00:00:00',
  `updated_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_key` (`api_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `api_key` | varchar(128) | NOT NULL DEFAULT ''、唯一 | 存储的 api_key；长度与 `api-keys.md` 中 `key` 的 1-128 字符限制保持一致 |
| `created_at` | datetime | NOT NULL DEFAULT '0000-01-01 00:00:00' | 创建时间 |
| `updated_at` | datetime | 自动更新 | 更新时间 |

### 6.3 `entity_types`

Entity 类型定义表。

```sql
CREATE TABLE `entity_types` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `type_name` VARCHAR(32) NOT NULL,
  `description` VARCHAR(256) DEFAULT '',
  `level` INT NOT NULL DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_type_name` (`type_name`),
  INDEX `idx_level` (`level`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Entity类型定义表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | 主键、自增 | 主键 ID |
| `type_name` | VARCHAR(32) | NOT NULL、唯一 | 类型标识 |
| `description` | VARCHAR(256) | DEFAULT '' | 类型描述 |
| `level` | INT | NOT NULL DEFAULT 1、索引 | 层级级别（1-5，数字越小级别越高） |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | 自动更新 | 更新时间 |

### 6.4 `entities`

Entity 实体表。

```sql
CREATE TABLE `entities` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `entity_id` VARCHAR(64) NOT NULL,
  `name` VARCHAR(128) NOT NULL,
  `type` VARCHAR(32) NOT NULL,
  `parent_id` VARCHAR(64) DEFAULT NULL,
  `allow_models` TEXT,
  `block_models` TEXT,
  `quota_plan_id` BIGINT DEFAULT NULL,
  `rate_limit_policy_id` BIGINT DEFAULT NULL,
  `route_rules_id` BIGINT DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_entity_id` (`entity_id`),
  UNIQUE KEY `uk_name` (`name`),
  INDEX `idx_parent_id` (`parent_id`),
  INDEX `idx_type` (`type`),
  INDEX `idx_quota_plan_id` (`quota_plan_id`),
  INDEX `idx_rate_limit_policy_id` (`rate_limit_policy_id`),
  INDEX `idx_route_rules_id` (`route_rules_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Entity实体表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | 主键、自增 | 主键 ID |
| `entity_id` | VARCHAR(64) | NOT NULL、唯一 | Entity 唯一业务标识 |
| `name` | VARCHAR(128) | NOT NULL、唯一 | Entity 名称 |
| `type` | VARCHAR(32) | NOT NULL、索引 | Entity 类型（关联 entity_types.type_name） |
| `parent_id` | VARCHAR(64) | DEFAULT NULL、索引 | 父 Entity ID |
| `allow_models` | TEXT | NULL | 允许访问的模型白名单 JSON 数组 |
| `block_models` | TEXT | NULL | 禁止访问的模型黑名单 JSON 数组 |
| `quota_plan_id` | BIGINT | DEFAULT NULL、索引 | 配额计划 ID |
| `rate_limit_policy_id` | BIGINT | DEFAULT NULL、索引 | 限流策略 ID |
| `route_rules_id` | BIGINT | DEFAULT NULL、索引 | 路由规则 ID |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | 自动更新 | 更新时间 |

### 6.5 `quota_plans`

配额计划表。

```sql
CREATE TABLE `quota_plans` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `unlimited` TINYINT(1) DEFAULT 1,
  `pass_when_no_enough_quota` TINYINT(1) DEFAULT 0,
  `quota` DECIMAL(18,8) DEFAULT 0,
  `unit` VARCHAR(32) DEFAULT 'total_token',
  `reset_period` VARCHAR(16) DEFAULT 'never',
  `last_reset_at` DATETIME DEFAULT NULL COMMENT '上次重置时间',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  INDEX `idx_unlimited` (`unlimited`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配额计划表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | 主键、自增 | 主键 ID |
| `unlimited` | TINYINT(1) | DEFAULT 1、索引 | 是否无限配额 |
| `pass_when_no_enough_quota` | TINYINT(1) | DEFAULT 0 | 配额不足时是否放行 |
| `quota` | DECIMAL(18,8) | DEFAULT 0 | 配额总量；`unit=total_token` 时存储为整数，`unit=RMB` 时最多保留 8 位小数（内部定点精度 1e-8 元） |
| `unit` | VARCHAR(32) | DEFAULT 'total_token' | 配额单位：`total_token`、`RMB` |
| `reset_period` | VARCHAR(16) | DEFAULT 'never' | 重置周期：never/weekly/monthly |
| `last_reset_at` | DATETIME | DEFAULT NULL | **上次重置时间**，周期重置据此判断是否跨越自然周/月边界；由 `quota_balances` 表迁移而来 |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | 自动更新 | 更新时间 |

### 6.6 `model_prices`

模型定价表，用于维护各 provider 下模型的能力、限制与价格，是 InnerAPI `AIConf.ModelTable` 的唯一数据源。

```sql
CREATE TABLE `model_prices` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `provider` VARCHAR(255) NOT NULL,
  `model` VARCHAR(255) NOT NULL,
  `base_model` VARCHAR(255) NOT NULL,
  `mode` VARCHAR(50) NOT NULL DEFAULT 'chat',
  `capabilities` JSON,
  `supported_parameters` JSON,
  `limits` JSON,
  `prices` JSON NOT NULL,
  `tier_prices` JSON,
  `price_currency` VARCHAR(10) NOT NULL DEFAULT 'RMB',
  `metadata` JSON,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_provider_model_mode` (`provider`, `model`, `mode`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模型定价表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | 主键、自增 | 主键 ID |
| `provider` | VARCHAR(255) | NOT NULL | Provider / Cluster 标识；仅作为价格归集标识，不强制引用 `providers.name` |
| `model` | VARCHAR(255) | NOT NULL | 模型名 |
| `base_model` | VARCHAR(255) | NOT NULL | 归一化模型名 |
| `mode` | VARCHAR(50) | NOT NULL DEFAULT 'chat' | 请求模式：chat、completion、responses、image_generation 等 |
| `capabilities` | JSON | NULL | 能力列表 |
| `supported_parameters` | JSON | NULL | 支持的请求参数列表 |
| `limits` | JSON | NULL | 限制对象 |
| `prices` | JSON | NOT NULL | 默认价格对象；至少包含一个价格字段；未命中 tier 时作为 fallback |
| `tier_prices` | JSON | NULL | 分时段价格对象；key 为 tier name（**初期只支持 `peak`**），value 为价格对象；内部键名须为 `prices` 枚举 |
| `price_currency` | VARCHAR(10) | NOT NULL DEFAULT 'RMB' | 价格货币；v0.4 固定为 RMB |
| `metadata` | JSON | NULL | 元数据 |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | 自动更新 | 更新时间 |

**唯一索引**：`(provider, model, mode)`。

> 说明：`capabilities`、`supported_parameters`、`limits`、`prices`、`tier_prices`、`metadata` 为 JSON 字段，由 Storage 层负责序列化与反序列化。`tier_prices` 中的 tier name 与 provider 的 `tiers` 不做强制引用校验；若引用了未定义的 tier name，可记录告警但不阻塞写入。`model_prices` 变更不会同步触发 InnerAPI 推送，由 BFE/Conf Agent 按自身周期拉取配置。

### 6.8 `rate_limit_policies`

限流策略表。

```sql
CREATE TABLE `rate_limit_policies` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `enabled` TINYINT(1) DEFAULT 0,
  `max_concurrency` INT DEFAULT -1,
  `tpm_configs` TEXT,
  `rpm_configs` TEXT,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  INDEX `idx_enabled` (`enabled`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='限流策略表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | 主键、自增 | 主键 ID |
| `enabled` | TINYINT(1) | DEFAULT 0、索引 | 是否启用 |
| `max_concurrency` | INT | DEFAULT -1 | 最大并发数，-1 表示不限制 |
| `tpm_configs` | TEXT | NULL | TPM 限流配置 JSON 数组 |
| `rpm_configs` | TEXT | NULL | RPM 限流配置 JSON 数组 |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | 自动更新 | 更新时间 |

**tpm_configs JSON 格式说明：**

```json
[
    {
        "name": "global_tpm",
        "model": "*",
        "window_minutes": 1,
        "max_tokens": 1000000,
        "step_minutes": 1
    },
    {
        "name": "gpt4_tpm",
        "model": "gpt-4",
        "window_minutes": 1,
        "max_tokens": 500000,
        "step_minutes": 1
    }
]
```

**rpm_configs JSON 格式说明：**

```json
[
    {
        "name": "global_rpm",
        "model": "*",
        "window_minutes": 1,
        "max_requests": 60
    },
    {
        "name": "gpt4_rpm",
        "model": "gpt-4",
        "window_minutes": 1,
        "max_requests": 30
    }
]
```

---

## 6.9 `operation_logs`

AI 网关配置操作日志表，用于持久化控制面写操作审计记录。

```sql
CREATE TABLE `operation_logs` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `log_id` varchar(64) NOT NULL COMMENT '请求唯一标识，与 access log 中的 LogID 一致',
  `operator_type` tinyint(4) NOT NULL DEFAULT '0' COMMENT '操作者类型：0=user, 1=token',
  `operator_id` bigint(20) NOT NULL DEFAULT '0' COMMENT '操作者在对应表中的主键 ID',
  `operator_name` varchar(255) NOT NULL DEFAULT '' COMMENT '操作者名称',
  `action` varchar(32) NOT NULL COMMENT '操作动作：create/update/delete/reset/...',
  `resource_type` varchar(64) NOT NULL COMMENT '资源类型：entity/api_key/provider/...',
  `resource_id` varchar(255) NOT NULL DEFAULT '' COMMENT '被操作资源业务 ID',
  `resource_name` varchar(512) NOT NULL DEFAULT '' COMMENT '被操作资源名称',
  `resource_parent_id` varchar(255) NOT NULL DEFAULT '' COMMENT '资源父级业务 ID',
  `status` tinyint(4) NOT NULL DEFAULT '1' COMMENT '操作结果：1=success, 2=failed',
  `error_msg` varchar(1024) NOT NULL DEFAULT '' COMMENT '失败时的简要错误信息',
  `change_summary` mediumtext COMMENT '变更摘要 JSON（脱敏后）',
  `request_path` varchar(512) NOT NULL DEFAULT '' COMMENT '请求路径',
  `request_method` varchar(16) NOT NULL DEFAULT '' COMMENT '请求方法',
  `client_ip` varchar(64) NOT NULL DEFAULT '' COMMENT '客户端 IP',
  `user_agent` varchar(512) NOT NULL DEFAULT '' COMMENT 'User-Agent',
  `created_at` datetime NOT NULL COMMENT '操作发生时间',
  PRIMARY KEY (`id`),
  KEY `idx_operator` (`operator_type`, `operator_id`),
  KEY `idx_resource` (`resource_type`, `resource_id`),
  KEY `idx_action` (`action`),
  KEY `idx_created_at` (`created_at`),
  KEY `idx_log_id` (`log_id`),
  KEY `idx_resource_parent` (`resource_parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI 网关配置操作日志表';
```

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | bigint(20) | 主键、自增 | 主键 ID |
| `log_id` | varchar(64) | NOT NULL | 请求唯一标识，与 access log 中的 LogID 一致 |
| `operator_type` | tinyint(4) | NOT NULL DEFAULT 0 | 操作者类型：`0`=用户，`1`=Token |
| `operator_id` | bigint(20) | NOT NULL DEFAULT 0 | 操作者在 `users` 表中的主键 ID |
| `operator_name` | varchar(255) | NOT NULL DEFAULT '' | 操作者名称 |
| `action` | varchar(32) | NOT NULL | 操作动作：`create` / `update` / `delete` / `reset` / `import` / `bind` / `unbind` |
| `resource_type` | varchar(64) | NOT NULL | 资源类型：`entity` / `entity_type` / `api_key` / `provider` / `cluster` / `route` / `domain` / `certificate` / `quota_plan` / `rate_limit_policy` / `model_price` / `user` / `token` |
| `resource_id` | varchar(255) | NOT NULL DEFAULT '' | 被操作资源业务 ID |
| `resource_name` | varchar(512) | NOT NULL DEFAULT '' | 被操作资源名称 |
| `resource_parent_id` | varchar(255) | NOT NULL DEFAULT '' | 资源父级业务 ID（如 entity 层级中的父节点） |
| `status` | tinyint(4) | NOT NULL DEFAULT 1 | 操作结果：`1`=成功，`2`=失败 |
| `error_msg` | varchar(1024) | NOT NULL DEFAULT '' | 失败时的简要错误信息 |
| `change_summary` | mediumtext | NULL | 变更摘要 JSON，已脱敏 |
| `request_path` | varchar(512) | NOT NULL DEFAULT '' | 请求路径 |
| `request_method` | varchar(16) | NOT NULL DEFAULT '' | 请求方法 |
| `client_ip` | varchar(64) | NOT NULL DEFAULT '' | 客户端 IP |
| `user_agent` | varchar(512) | NOT NULL DEFAULT '' | User-Agent |
| `created_at` | datetime | NOT NULL | 操作发生时间 |

> 说明：`operation_logs` 由 `model/ioperlog` 异步批量写入；`change_summary` 在写入前已调用 `MaskSensitiveFields` 完成脱敏。

---

## 7. 表关系

数据库 DDL 未声明物理 FOREIGN KEY，所有关系为逻辑外键，由应用层代码维护。

### 7.1 产品与从属资源（1:N）

`products.id` 被以下表引用：

| 引用表 | 字段 |
|--------|------|
| `domains` | `product_id` |
| `clusters` | `product_id` |
| `lb_matrices` | `product_id` |
| `sub_clusters` | `product_id` |
| `pools` | `product_id` |
| `route_basic_rules` | `product_id` |
| `route_advance_rules` | `product_id` |
| `user_products` | `product_id` |
| `extra_files` | `product_id` |

产品删除时，业务代码会级联清理上述相关表数据。

### 7.2 Provider 与 Cluster / ModelPrice

`providers.name` 被以下表/字段逻辑引用：

| 主表 | 从表 | 字段 | 说明 |
|------|------|------|------|
| `providers` | `clusters` | `clusters.llm_config.provider` | Cluster 通过 `llm_config.provider` 引用 Provider，获取后端实例池与 key 明文 |
| `providers` | `model_prices` | `model_prices.provider` | 模型定价记录按名称关联 Provider，不强制引用已存在的 Provider |

删除 Provider 前，业务代码须校验无 `clusters` 记录引用；`model_prices` 同名 provider 不再阻塞删除。

> 补充：InnerAPI 导出 `AIConf.ModelTable` 时，根据 `clusters.llm_config.provider` 读取 `providers.time_zone` / `providers.tiers`，并与 `model_prices.prices` / `model_prices.tier_prices` 拼接后下发给 BFE。

### 7.3 集群与子集群 / 实例池

| 主表 | 从表 | 字段 | 说明 |
|------|------|------|------|
| `clusters` | `sub_clusters` | `sub_clusters.cluster_id` | 子集群挂载到集群 |
| `pools` | `sub_clusters` | `sub_clusters.bns_name_id` | 子集群对应实例池 |
| `providers` | `pools` | 间接：通过 cluster 创建时读取 `providers.instance_pool` 生成 | 实例池数据源头为 Provider |

### 7.4 用户/Token 与产品（N:M）

| 中间表 | 字段 | 说明 |
|--------|------|------|
| `user_products` | `user_id`, `product_id` | `user_id` 同时引用 `users.id`（用户或 Token） |

### 7.5 API Key / Entity 与配额/限流/路由规则

| 主表 | 被引用字段 | 引用表 / 字段 |
|------|-----------|---------------|
| `quota_plans` | `id` | `api_keys.quota_plan_id`、`entities.quota_plan_id` |
| `rate_limit_policies` | `id` | `api_keys.rate_limit_policy_id`、`entities.rate_limit_policy_id` |
| `route_rules` | `id` | `api_keys.route_rules_id`、`entities.route_rules_id` |
| `entities` | `entity_id` | `api_keys.entity_id` |
| `entity_types` | `type_name` | `entities.type` |

---

## 8. 初始数据

数据库初始化脚本（`db_ddl.sql` / `db_ddl_sqlite.sql`）会插入以下基础数据：

- `users`：系统管理员账号。
- `products`：默认产品线，例如 `BFE`、`AI_product`。
- `pools`：默认 AI 网关实例池，例如 `BFE.aipool`。
- `bfe_clusters`：默认 BFE 集群，例如 `BFE-AI_product.szyf`。

---

## 9. 备注

1. **无物理外键**：所有表关系靠业务代码维护，删除时需注意级联清理（例如 `DeleteAPIKey` 会同步删除关联的 quota、rate_limit、route_rules 等）。
2. **`users` 表复用**：普通用户与 API Token 共用 `users` 表，通过 `type` 字段区分：0=普通用户，1=Token。
3. **`route_cases` 表**：仅在 DDL 中定义，当前代码中无对应 DAO 与业务代码引用。
4. **MySQL 与 SQLite**：字段类型等价；SQLite 通过 trigger 模拟 `updated_at` 自动更新。
5. **JSON 字段**：`providers.model_endpoint`、`providers.models`、`providers.keys`、`providers.instance_pool`、`providers.model_protocols`、`providers.tiers`、`instance_detail`、`lb_matrix`、`llm_config`、`allowed_models`、`subnet`、`tpm_configs`、`rpm_configs`、`rules`、`basic`、`params`、`route_action`、`host_names`、`paths`、`model_prices.capabilities`、`model_prices.supported_parameters`、`model_prices.limits`、`model_prices.prices`、`model_prices.tier_prices`、`model_prices.metadata` 等字段均为 JSON 或文本序列化存储。
